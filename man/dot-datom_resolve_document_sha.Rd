% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_write.R
\name{.datom_resolve_document_sha}
\alias{.datom_resolve_document_sha}
\title{Resolve the document_sha to Record and Whether to Upload}
\usage{
.datom_resolve_document_sha(
  conn,
  name,
  data_sha,
  new_document_sha,
  change_type,
  current
)
}
\arguments{
\item{conn}{A \code{datom_conn} object.}

\item{name}{Set name.}

\item{data_sha}{Canonical content hash (the storage address).}

\item{new_document_sha}{SHA-256 of the payload bytes just written to the clone.}

\item{change_type}{\code{"metadata_only"} or \code{"full"} (never \code{"none"}).}

\item{current}{The current metadata (from \code{\link[=.datom_has_changes]{.datom_has_changes()}}), or NULL.}
}
\value{
List with \code{document_sha} (character or NULL) and \code{upload} (logical).
}
\description{
The set analogue of \code{\link[=.datom_resolve_parquet_sha]{.datom_resolve_parquet_sha()}}, kept beside it so the two
cannot drift: the decision is the same decision, and both are the one place
where "these are new bytes, so hash them" is the wrong answer.
}
\details{
\strong{Recomputing the hash from freshly emitted bytes while reusing the stored
object records a hash of bytes nobody stored.} Nothing fails at write time --
it surfaces much later as a \emph{refused read of a valid version}, when the
integrity gate compares the stored payload against a hash taken from a
different serialization of the same content. Sets reach that state far more
easily than tables do: for a table it takes an \code{arrow} upgrade, while for a set
an ordinary tag-value reorder is enough, because several payload spellings
share one \code{data_sha}.

Cases, mirroring the parquet ones:
\itemize{
\item \code{metadata_only} -- the \code{data_sha} is unchanged, so the payload object already
exists; carry the current metadata's \code{document_sha} forward and do not
upload. Structurally unreachable for a set today (a set's hashed fields are
\code{data_sha}, \code{hash_algo} and \code{kind}, so unchanged content means an unchanged
version), and handled anyway rather than assumed away.
\item \code{full} where a prior version already recorded a \code{document_sha} for this exact
\code{data_sha} -- reuse it and do \strong{not} re-upload.
\item \code{full} otherwise -- upload these bytes and record their hash.
}
}
\keyword{internal}
