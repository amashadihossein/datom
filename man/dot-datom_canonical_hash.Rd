% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-sha.R
\name{.datom_canonical_hash}
\alias{.datom_canonical_hash}
\title{Compute the datom-cv1 Canonical Content Hash}
\usage{
.datom_canonical_hash(data)
}
\arguments{
\item{data}{A data frame with at least one row and one column.}
}
\value{
A list with \code{data_sha} (character) and \code{column_hashes} (an ordered
list of \code{list(name, sha)} in column order, computed once and reused for
both \code{data_sha} and the persisted column index).
}
\description{
The I/O-free identity engine for \code{datom-cv1}. Computes \code{data_sha} from the
in-memory logical values only -- no parquet write, no CSV, no temp files,
no \code{as.data.frame()} or coercion, and never invokes arrow. Columns are read
via \code{data[[i]]} / \code{names(data)} and dimensions via \code{nrow()} / \code{ncol()}, so
two frames with equal values hash identically regardless of container class
(tibble vs data.frame vs grouped_df), row names, or arrow version.
}
\details{
Before encoding, every column is scanned through \code{\link[=.datom_hash_recourse]{.datom_hash_recourse()}};
if any are unsupported the function aborts \strong{once}, listing every offender
with its class and canonical recourse. This fires during \code{data_sha}
computation (step 1 of \code{\link[=datom_write]{datom_write()}}), before any git or storage
mutation, so a refusal leaves no partial state.

The final hash is
\code{sha256( "datom-cv1" || f64le(nrow) || f64le(ncol) || concat(col_digest_hex...) )}.
}
\keyword{internal}
