% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/member.R
\name{.datom_drop_empty_tags}
\alias{.datom_drop_empty_tags}
\title{Drop Tag Keys Whose Value Is Empty}
\usage{
.datom_drop_empty_tags(tags)
}
\arguments{
\item{tags}{A named list, or \code{NULL}.}
}
\value{
\code{tags} with empty-valued keys removed; \code{NULL} unchanged. A
non-list is returned untouched, so the validator reports the type rather
than this function failing on it.
}
\description{
The one tidy rule this file owns: a key that points at nothing is removed,
because a tag with no values is spelled by omitting the key. Note this is
about a tag \emph{value}, not about a member having no tags at all -- a member with
no tags is the ordinary case and is simply accepted. Nothing is lost here --
an empty value states no fact -- and leaving it in would be worse than
cosmetic:
a present key with an empty value hashes differently from an absent key, so
the same fact would mint two different \code{data_sha} values.
}
\details{
Covers both empty spellings R produces. \code{character(0)} is the documented one;
\code{NULL} is what an absent value looks like when a tag map is composed
programmatically (\code{list(domain = f())} where \code{f()} returned nothing), and it
means exactly the same thing. Note the encoder still refuses a \code{NULL} value,
and must: there it arrives from a parsed file rather than from a caller, so
there is no caller intent to tidy toward.

Full canonicalization -- sorting keys, sorting and deduplicating values,
unboxing single values, ordering members -- is \strong{not} done here. It belongs
to the set write, so that canonical form has exactly one implementation.
}
\keyword{internal}
