% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sync.R
\name{tbit_sync}
\alias{tbit_sync}
\title{Sync Files to tbit Repository}
\usage{
tbit_sync(conn, manifest, continue_on_error = TRUE)
}
\arguments{
\item{conn}{A \code{tbit_conn} object from \code{\link[=tbit_get_conn]{tbit_get_conn()}}.}

\item{manifest}{Data frame from \code{\link[=tbit_sync_manifest]{tbit_sync_manifest()}}, with columns
\code{name}, \code{file}, \code{format}, \code{file_sha}, \code{status}.}

\item{continue_on_error}{If \code{TRUE} (default), continues processing
remaining tables when one fails. If \code{FALSE}, stops on first error.}
}
\value{
The manifest data frame augmented with \code{result} and \code{error} columns.
\code{result} is \code{"success"}, \code{"skipped"}, or \code{"error"}.
}
\description{
Processes new/changed files from a manifest produced by
\code{\link[=tbit_sync_manifest]{tbit_sync_manifest()}}. Imports each file via \code{rio::import()}, converts to
a data frame, and calls \code{\link[=tbit_write]{tbit_write()}} to store as parquet in S3 with git
metadata. Updates the local \code{.tbit/manifest.json} after each successful
write.
}
