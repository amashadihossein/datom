% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forward-compat.R
\name{.datom_carry_unknown_fields}
\alias{.datom_carry_unknown_fields}
\title{Carry Unrecognised Top-Level Fields Onto a Rebuilt Document}
\usage{
.datom_carry_unknown_fields(rebuilt, prior, known)
}
\arguments{
\item{rebuilt}{The document this build assembled, a named list.}

\item{prior}{The document that was already on disk, a named list, or \code{NULL} /
anything unparsed when there was none -- in which case there is nothing to
carry and \code{rebuilt} is returned unchanged.}

\item{known}{Character vector of field names this build can place.}
}
\value{
\code{rebuilt}, with the unrecognised fields of \code{prior} appended.
}
\description{
Copies onto \code{rebuilt} every top-level field of \code{prior} whose name is not in
\code{known}, so a field this build cannot place survives being rewritten.
}
\details{
\strong{Only unrecognised fields are carried, and that narrowness is the design.}
A field datom knows about keeps exactly the behaviour it has today, including
disappearing when this write does not set it. \code{original_format} is the case
that makes the difference concrete: a table first imported from a CSV and
later written straight from a data frame has no format to declare, and the
row is meant to stop claiming one. Carrying every absent field forward
instead of only the unplaceable ones would leave that claim standing against
a version it does not describe -- a wrong statement, which is worse than a
missing one.

Where \code{rebuilt} already has a field, \code{rebuilt} wins. That cannot happen for a
genuinely unrecognised field, since this build only writes names it knows;
stating the precedence costs one term and removes the question.

Top-level only, at each level separately. A field nested inside a value datom
does understand -- inside \code{custom}, or inside the manifest's \code{summary} block
-- is not this function's business: \code{custom} is carried whole as one
recognised field, and \code{summary} is a derived aggregate that is meant to be
recomputed.

A field whose value is JSON \code{null} gets no special handling. Absence in a
datom document is spelled by omitting the key, never by nulling it, so such a
field is already off-convention; it is carried, but a \code{null} re-serialises as
an empty object rather than as \code{null}.
}
\keyword{internal}
