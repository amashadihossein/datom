% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest-upgrade.R
\name{.datom_manifest_upgrade_v1_to_v2}
\alias{.datom_manifest_upgrade_v1_to_v2}
\title{Upgrade a v1 Manifest to v2}
\usage{
.datom_manifest_upgrade_v1_to_v2(manifest)
}
\arguments{
\item{manifest}{Parsed manifest document (a named list) declaring v1.}
}
\value{
The same document in v2 shape. The version is stamped by
\code{\link[=.datom_manifest_upgrade]{.datom_manifest_upgrade()}}, not here, so a step is never mistaken for the
thing that records the result.
}
\description{
v1 is every manifest written before the artifact namespace existed: the
artifact list sits under \code{tables} and no entry declares what kind of artifact
it is. v2 renames that key to \code{artifacts} and types every entry with
\code{kind = "table"}, which is what all of them are -- sets did not exist.
}
\details{
The rename is done \strong{in place} (\code{names()} assignment rather than
add-then-remove), so the key keeps its position in the document and any
sibling key this build does not recognise survives untouched.

A v1 document with no \code{tables} key at all is left with no artifact key. That
is deliberate: an absent key and an empty one are different states -- a
truncated document versus a repo with nothing in it -- and flattening them
here would destroy the distinction a later self-healing read depends on.

\strong{Frozen.} See the file header.
}
\keyword{internal}
