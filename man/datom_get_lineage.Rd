% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query.R
\name{datom_get_lineage}
\alias{datom_get_lineage}
\title{Get Lineage for a Table}
\usage{
datom_get_lineage(conn, name, version = NULL, depth = c("source", "parents"))
}
\arguments{
\item{conn}{A \code{datom_conn} object from \code{\link[=datom_get_conn]{datom_get_conn()}}.}

\item{name}{Table name.}

\item{version}{Optional metadata_sha (datom version). If NULL, reads
current metadata. If provided, fetches the versioned metadata snapshot.}

\item{depth}{One of \code{"source"} (default) or \code{"parents"}.}
}
\value{
For \code{depth = "source"}: the table's recorded \code{source_lineage} --
a list of source-table descriptors (each with \code{project}, \code{table},
\code{version_sha}), or \code{NULL} if the field is absent.
For \code{depth = "parents"}: list of parent entries (each with \code{source},
\code{table}, \code{version}, \code{data_sha}), or \code{NULL} if no lineage is recorded.
}
\description{
Reads lineage metadata for a table. Depending on \code{depth}, returns either
the pre-computed transitive source list (\code{source_lineage}) or the immediate
parent list (\code{parents}). Both fields are stored flat in the table's
metadata -- no walking or cross-project resolution is performed.
}
\details{
The two fields answer different questions:
\itemize{
\item \code{"source"}: "what raw datasets does this table ultimately depend on?"
(audit, regulatory disclosure, reproducibility scope). Derived at write
time as the deduplicated union of the parents' \code{source_lineage} fields.
\item \code{"parents"}: "what did this table come from one step back?"
(debugging, diff, replay). Equivalent to \code{\link[=datom_get_parents]{datom_get_parents()}}.
}
}
\examples{
# Offline, self-contained: a bare git repo stands in for GitHub and a
# local directory for object storage.
if (requireNamespace("git2r", quietly = TRUE)) {
  tmp <- tempfile("datom-example-")
  remote <- file.path(tmp, "remote.git")
  dir.create(remote, recursive = TRUE)
  git2r::init(remote, bare = TRUE)

  store <- datom_store(
    data = datom_store_local(file.path(tmp, "storage")),
    github_pat = "example-token", # role selector; a local remote needs none
    data_repo_url = remote,
    validate = FALSE
  )
  datom_init_repo(file.path(tmp, "repo"), "example_project", store)
  conn <- datom_get_conn(file.path(tmp, "repo"), store)

  datom_write(conn, data = datom_example_data("dm"), name = "dm")
  datom_get_lineage(conn, "dm", depth = "parents")
  datom_get_lineage(conn, "dm", depth = "source")

  unlink(tmp, recursive = TRUE)
}
}
\seealso{
\code{\link[=datom_get_parents]{datom_get_parents()}} for a direct shorthand for the \code{"parents"} depth.
}
