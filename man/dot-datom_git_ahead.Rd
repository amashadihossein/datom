% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/repo.R
\name{.datom_git_ahead}
\alias{.datom_git_ahead}
\title{Commits on This Branch the Remote Does Not Have}
\usage{
.datom_git_ahead(path)
}
\arguments{
\item{path}{Repository path.}
}
\value{
Integer count of unpushed commits, or \code{NA_integer_} when it cannot be
determined -- no upstream tracking ref yet, or the comparison failed.
\code{NA} means \emph{cannot prove there is nothing to publish}, so callers push: that
is exactly the state of a branch that has never been pushed.
}
\description{
The ahead half of the count \code{.datom_check_git_current()} already computes for
its behind half: \code{git2r::ahead_behind()} element \verb{[[1]]} is ahead, \verb{[[2]]} is
behind. No new git machinery.
}
\details{
\strong{Does not fetch.} The comparison is against the cached remote-tracking ref,
so a stale ref can only cause an unnecessary push -- and a push pulls first
and is idempotent, so the cost of being wrong in that direction is a round
trip. Fetching here would instead make a clean-tree call fail when offline.
}
\keyword{internal}
