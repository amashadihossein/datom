% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-sha.R
\name{.datom_encode_character}
\alias{.datom_encode_character}
\title{Encode a Character Payload for Canonical Hashing}
\usage{
.datom_encode_character(x)
}
\arguments{
\item{x}{A vector coercible to character (character or factor).}
}
\value{
A raw vector: \code{length(x)} mask bytes followed by the
NUL-terminated UTF-8 value bytes.
}
\description{
The character encoder for the \code{chr} column kind (character and factor
columns). Emits a one-byte-per-row NA mask (\code{0x01} where \code{is.na()},
\code{0x00} otherwise) followed by each value re-encoded to UTF-8 via
\code{enc2utf8()} and NUL-terminated. The leading mask makes \code{NA} and the
empty string \code{""} distinguishable (both have an empty value section, but
\code{NA} sets its mask byte). No Unicode normalization is applied, so NFC and
NFD forms of the same text encode differently (a documented, benign
limitation).
}
\keyword{internal}
