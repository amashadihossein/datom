% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set.R
\name{.datom_read_set_member}
\alias{.datom_read_set_member}
\title{Normalize One Member Record Read Back from a Payload}
\usage{
.datom_read_set_member(m, at, name)
}
\arguments{
\item{m}{One parsed member record.}

\item{at}{Position label used in error messages, e.g. \code{"members[[2]]"}.}

\item{name}{The set's name, for error messages.}
}
\value{
The member record, normalized.
}
\description{
Normalizes representation in \code{id} and \code{tags}, then makes the one refusal the
read owns: an \code{id} field that is not a single non-empty string after
normalization aborts as a malformed document, naming the member.
}
\details{
\strong{Why \code{id} is refused where a tag value is tolerated.} \code{id} values are
spliced into storage keys and compared against project names, and
\code{.datom_validate_members()} enforces that contract on \strong{write only} -- so the
read is the only place a payload's \code{id} is ever checked. Normalizing without
refusing would silently accept a document \code{datom_write_set()} cannot produce,
and a caller comparing a list against a string would conclude that a member of
this project belongs to another one.

Fields outside the four are left alone rather than refused: a newer datom may
have added one, and this build never reads it.
}
\keyword{internal}
