% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_write.R
\name{.datom_lookup_history_parquet_sha}
\alias{.datom_lookup_history_parquet_sha}
\title{Most-recent version_history parquet_sha for a data_sha}
\usage{
.datom_lookup_history_parquet_sha(conn, name, data_sha)
}
\arguments{
\item{conn}{A \code{datom_conn} object (developer, with local path).}

\item{name}{Table name.}

\item{data_sha}{Canonical content hash to match.}
}
\value{
Character \code{parquet_sha}, or NULL.
}
\description{
Scans the developer's local \code{version_history.json} (newest-first) for the
most recent entry whose \code{data_sha} matches and that carries a non-empty
\code{parquet_sha}. Returns NULL when none is found -- which happens for pre-cv1
histories, whose entries predate \code{parquet_sha} being recorded. Reads the local git clone (offline-friendly); a stale
clone is tolerated because the subsequent git push serializes concurrent
writers (a behind clone fails to push before it can upload).
}
\keyword{internal}
