% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_write.R
\name{.datom_build_set_metadata}
\alias{.datom_build_set_metadata}
\title{Build the Metadata Document for a Set Write}
\usage{
.datom_build_set_metadata(payload, document_sha = NULL)
}
\arguments{
\item{payload}{The set payload: a list with \code{members} (an unnamed list of
member records) and optional set-level \code{tags}. Must already be tidied and
validated -- this builder hashes what it is given.}

\item{document_sha}{SHA-256 of the stored payload bytes, or NULL. Declared
either way, mirroring how \code{\link[=.datom_build_metadata]{.datom_build_metadata()}} declares \code{parquet_sha}
for \code{\link[=datom_write]{datom_write()}} to populate: the byte hash is not knowable until the
payload has been serialized, and it is excluded from \code{metadata_sha}, so the
deferred assignment cannot move a version. \strong{Nothing computes one until the
set write path exists}, so today it arrives NULL from every caller.}
}
\value{
Named list of exactly the seven fields a set's \code{metadata.json}
carries.
}
\description{
A set's \code{metadata.json} is a collapsed version of a table's: seven fields and
no more. Everything a table carries that describes a rectangle (\code{nrow},
\code{ncol}, \code{colnames}, \code{column_hashes}), the provenance axis (\code{table_type},
\code{parents}, \code{source_lineage}), the stored-parquet facts (\code{parquet_sha},
\code{size_bytes}) and the user-metadata channel (\code{custom}) are all \strong{omitted, not
nulled} -- a set's members and its user metadata both live in the payload as
tags, and no counter reads a set's byte size.
}
\details{
Kept beside \code{\link[=.datom_build_metadata]{.datom_build_metadata()}} on purpose: the two documents are close
enough that a field copied from the wrong one is easy to miss, and two of the
values here are exactly that kind of trap.
\itemize{
\item \code{data_sha} comes from \code{\link[=.datom_canonical_set_hash]{.datom_canonical_set_hash()}}, the \code{datom-sv1}
identity engine, \strong{not} from the table hash. Computed here rather than
passed in, so a caller cannot hand a set a table-regime hash.
\item \code{hash_algo} is the literal \code{"datom-sv1"}. The encoder embeds that string
inside the digest but nothing stamps the field, so the builder must. A
copied \code{"datom-cv1"} would leave a set claiming one regime while hashing
under the other, and no hash comparison would notice.
}
}
\keyword{internal}
