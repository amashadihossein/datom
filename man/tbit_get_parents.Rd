% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query.R
\name{tbit_get_parents}
\alias{tbit_get_parents}
\title{Get Parent Lineage for a Table}
\usage{
tbit_get_parents(conn, name, version = NULL)
}
\arguments{
\item{conn}{A \code{tbit_conn} object from \code{\link[=tbit_get_conn]{tbit_get_conn()}}.}

\item{name}{Table name.}

\item{version}{Optional metadata_sha (tbit version). If NULL, reads
current metadata. If provided, fetches the versioned metadata snapshot
from S3.}
}
\value{
List of parent entries (each with \code{source}, \code{table}, \code{version}),
or \code{NULL} if no lineage is recorded.
}
\description{
Reads the \code{parents} field from a table's metadata. Returns the lineage
entries recorded at write time by dp_dev or other callers. For imported
tables or derived tables with no recorded lineage, returns \code{NULL}.
}
