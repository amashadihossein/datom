% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set-edit.R
\name{datom_update_members}
\alias{datom_update_members}
\title{Repoint a set's members at newer versions}
\usage{
datom_update_members(
  x,
  conn,
  member = NULL,
  tags = NULL,
  version_from = NULL,
  version_to = NULL
)
}
\arguments{
\item{x}{A \code{datom_set} from \code{\link[=datom_get_set]{datom_get_set()}}, or a \code{datom_set_draft} from
\code{\link[=datom_assemble_set]{datom_assemble_set()}}.}

\item{conn}{A \code{datom_conn} from \code{\link[=datom_get_conn]{datom_get_conn()}}, or a list of them -- one
per project the selected members belong to.}

\item{member}{Optional: the member to repoint, as its name, a member record,
or a link. \code{NULL} (the default) selects every member.}

\item{tags}{Optional named list of labels narrowing the selection, e.g.
\code{list(type = "input")}. A member matches when it carries every label listed.}

\item{version_from}{Optional version, or a prefix of one, narrowing the
selection to the member pinned at it.}

\item{version_to}{Optional exact version to move to. \code{NULL} (the default)
means whatever that artifact's project reports as current. Requires the
selection to resolve to a single member.}
}
\value{
\code{x} with the matching members repointed, and what changed appended to
its \code{datom_edits} attribute.
}
\description{
Moves members of a set forward to the versions that are current now, and
returns the set with those pointers changed. \strong{Nothing is written}: the
report you see is the dry run, and the set is stored only when you pass the
result to \code{\link[=datom_write_set]{datom_write_set()}}.
}
\details{
This is the operation a product needs when its inputs move on -- a hundred
members of which thirty upstream tables have advanced. Doing it by hand is
list surgery on what \code{\link[=datom_get_set]{datom_get_set()}} returned, and two of the obvious
spellings are silently wrong: filtering members by name drops \emph{every} version
of that name, and rebuilding a pointer from its name plus a new version drops
that member's labels.
}
\section{Which connection to pass}{

One per project the set spans, since access in datom is per project. A set
whose members all live in the project that owns it needs only that one
connection; a product drawing on three studies needs three, in a list.

A set's projects can be listed \strong{offline, with no connection at all}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{unique(datom_list_members(x)$project)
}\if{html}{\out{</div>}}

A \code{datom_set_draft} carries the connection it was opened with, and this verb
\strong{ignores} it: a draft carries exactly one while this call legitimately spans
several, and quietly preferring the embedded one would make the same call
behave differently depending on how \code{x} was produced.
}

\section{Which members move}{

With no \code{member}, \strong{every} member -- refreshing everything is the common case
and rerunning it changes nothing. Otherwise select one the way
\code{\link[=datom_fetch_member]{datom_fetch_member()}} does: by name, by a member record, or by a link. \code{tags}
and \code{version_from} narrow either a name or the sweep, so
\code{tags = list(release = "live")} repoints the labelled members and leaves the
rest pinned.

A name that matches more than one member \strong{aborts}, because the request
cannot be honoured as typed; a \emph{sweep} that meets the same pair skips it and
says so, because refusing a whole refresh over one frozen baseline would make
the first update on such a set an error.
}

\section{Which version each member moves to}{

\code{version_to} omitted, each selected member moves to the version its own
project reports as current, and every move is reported before anything is
written. That is the one place datom infers "newest", and the reason it is
allowed here is in the verb's name: a pointer \emph{constructor} requires an
explicit version (\code{\link[=datom_member]{datom_member()}} refuses to guess), while a verb whose whole
meaning is \emph{move this forward} states the time-dependence up front and then
says what it picked. What the set records is still an exact version, so a
script that later reads that set is as reproducible as ever.

\code{version_to} supplied, the selected member moves to exactly that version --
a rollback to a known-good, or a deliberate step to something that is not the
newest. It requires the selection to resolve to one member, because one
explicit version across several artifacts is not a meaning. It is also what
makes this verb better than removing and re-adding a member: \strong{the labels come
with it}, where re-adding makes you retype them.
}

\section{What it declines to do, and how}{
\tabular{ll}{
   Situation \tab Response \cr
   a member's project has no supplied connection \tab the whole call is refused, naming that project \cr
   a member's artifact no longer appears in its project \tab reported, and its pin is left alone \cr
   two members share a name and a project \tab both skipped and reported \cr
}


The first refuses because whether those members moved is unknowable, and
reporting them as unchanged would state something nothing checked. The second
does not, because the answer \emph{is} known: the pinned version is immutable and
still reads, so the set stays writable.
}

\section{What comes back}{

The class it was handed -- a \code{datom_set} for a \code{datom_set} and a
\code{datom_set_draft} for a draft -- with matching members repointed and each
moved member's labels byte-identical to what they were.

When something moved, a \code{datom_set}'s \code{version} and \code{data_sha} are emptied:
they described the payload it was read as, and that is no longer what the
object holds. When nothing moved they are left alone, because the object still
describes exactly that stored version.

The returned object also carries a log of what changed, which
\code{\link[=datom_write_set]{datom_write_set()}} uses for the commit message when you pass no \code{message} of
your own -- so \verb{git log} names what moved instead of saying \verb{Update \{name\}}.
\code{\link[=datom_remove_members]{datom_remove_members()}} adds to the same log, so editing both ways before you
write produces one message describing both. Passing \code{x$members} rather than \code{x}
to the write loses that and nothing else.
}

\examples{
# Offline, self-contained: a bare git repo stands in for GitHub and a
# local directory for object storage.
if (requireNamespace("git2r", quietly = TRUE)) {
  tmp <- tempfile("datom-example-")
  remote <- file.path(tmp, "remote.git")
  dir.create(remote, recursive = TRUE)
  git2r::init(remote, bare = TRUE)

  store <- datom_store(
    data = datom_store_local(file.path(tmp, "storage")),
    github_pat = "example-token", # role selector; a local remote needs none
    data_repo_url = remote,
    validate = FALSE
  )
  # A product repo declares itself as one and names the single set it owns.
  datom_init_repo(file.path(tmp, "repo"), "example_project", store,
                  mode = "product", set = "example_product")

  conn <- datom_get_conn(file.path(tmp, "repo"), store)

  dm <- datom_example_data("dm")
  datom_write(conn, data = dm, name = "dm")
  datom_write_set(conn, list(
    datom_member(conn, "dm", datom_history(conn, "dm")$version[1],
                 tags = list(type = "input"))
  ))

  # The table moves on, so the set now cites an older version of it.
  datom_write(conn, data = dm[-1, , drop = FALSE], name = "dm")

  x <- datom_get_set(conn, "example_product")
  x <- datom_update_members(x, conn)

  # The label came with it, and nothing is stored until the write.
  print(datom_list_members(x))
  datom_write_set(conn, x)

  unlink(tmp, recursive = TRUE)
}
}
\seealso{
\code{\link[=datom_remove_members]{datom_remove_members()}} to drop members instead,
\code{\link[=datom_write_set]{datom_write_set()}} to store the result, \code{\link[=datom_list_members]{datom_list_members()}} to see what a
set holds, \code{\link[=datom_member]{datom_member()}} to build a pointer from scratch.
}
