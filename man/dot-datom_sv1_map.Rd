% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hashable-set.R
\name{.datom_sv1_map}
\alias{.datom_sv1_map}
\title{Encode a Map for datom-sv1}
\usage{
.datom_sv1_map(m, what = "map")
}
\arguments{
\item{m}{A named list, or \code{NULL}.}

\item{what}{Key path used in error messages.}
}
\value{
A raw vector of 32 bytes.
}
\description{
\verb{map(m) = h(0x03 || concat(str(k) || strset(m[k]) for k in sort(keys(m), radix)))}.
}
\details{
One encoder serves both slots of a member record -- the \code{id} and the \code{tags}
-- so a fifth \code{id} field added later is just another key: no positional
convention to maintain, and no absent-versus-empty question. \code{id} values are
single strings, encoded as one-element string sets; enforcing "exactly these
four keys, each single-valued" is validation's job, not the encoder's.

An absent map (\code{NULL}) and an empty map both encode as \code{h(0x03)}. Writers
never emit an empty map, but the encoder must not depend on that.
}
\keyword{internal}
