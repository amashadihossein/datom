% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest-rebuild.R
\name{.datom_storage_artifact_names}
\alias{.datom_storage_artifact_names}
\title{Artifact Names Present in Storage}
\usage{
.datom_storage_artifact_names(conn)
}
\arguments{
\item{conn}{A \code{datom_conn} object.}
}
\value{
Character vector of artifact names, possibly empty. One storage
listing, recursive.
}
\description{
Enumerates artifacts from a storage listing, by the one signal that
identifies one: a \code{{name}/.metadata/metadata.json} object. Deliberately
independent of the manifest, because the manifest is the document under
suspicion whenever this is called.
}
\details{
The clone-side equivalent is \code{\link[=.datom_clone_artifact_names]{.datom_clone_artifact_names()}}. They are not
interchangeable and neither can stand in for the other: a storage-only reader
has no clone at all, and the clone can hold an artifact whose upload has not
happened yet.

\strong{The listing returns FULL keys} -- including the \verb{\{prefix\}/datom/} portion
-- while every other part of datom's business logic speaks in keys relative to
the datom namespace root. Mixing the two shapes double-prefixes silently and
does not error, so the root is stripped here, once, against the same builder
the backends use.
}
\keyword{internal}
