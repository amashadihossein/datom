% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sync.R
\name{.datom_read_manifest}
\alias{.datom_read_manifest}
\title{Read a Manifest and Check Its Schema Version}
\usage{
.datom_read_manifest(conn, scope = c("storage", "clone"))
}
\arguments{
\item{conn}{A \code{datom_conn} object.}

\item{scope}{\code{"storage"} for the copy in data storage
(\code{.metadata/manifest.json}), \code{"clone"} for the git-tracked copy
(\code{.datom/manifest.json}). Both exist; they can differ, and which one a
caller wants is a real choice rather than a default.}
}
\value{
A list with:
\itemize{
\item \code{ok} -- \code{TRUE} when the manifest was read and parsed.
\item \code{absent} -- \code{TRUE} only when the document is \emph{known} not to exist. That
is decided for \code{scope = "clone"}, where testing a local path is free.
For \code{scope = "storage"} it is always \code{FALSE}, meaning "not known to be
absent": separating a missing object from an unreachable store would
cost an extra request on every read and no caller distinguishes them.
\item \code{manifest} -- the parsed document, or \code{NULL} when \code{ok} is \code{FALSE}.
\item \code{error} -- the condition that stopped the read, or \code{NULL}. The whole
condition rather than its text, so a caller can re-signal the original
failure unchanged instead of manufacturing a look-alike.
}
}
\description{
The single manifest read. Every reader that takes a manifest \emph{into} datom
goes through this, so the compatibility check happens once and cannot be
softened by a caller's error handling.
}
\details{
Two kinds of failure, handled deliberately differently:
\itemize{
\item \strong{An IO failure is returned as data} (\code{ok = FALSE}), because each caller
has its own policy: \code{datom_list()} and \code{datom_summary()} abort,
\code{datom_status()} reports the manifest unavailable and carries on, and the
clone readers fall back to an empty manifest when the file does not exist
yet.
\item \strong{A schema refusal is thrown}, so the "upgrade datom" message reaches the
user intact. Placed inside a caller's \code{tryCatch} it would be reworded as
"could not read manifest" at two sites and downgraded to a warning at a
third -- see \code{dev/engineering-notes.md}. Throwing from in here means there
is no handler for a caller to put it inside.
}
}
\keyword{internal}
