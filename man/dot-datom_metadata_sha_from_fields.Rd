% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-sha.R
\name{.datom_metadata_sha_from_fields}
\alias{.datom_metadata_sha_from_fields}
\title{Hash an Already-Selected Set of Metadata Fields}
\usage{
.datom_metadata_sha_from_fields(fields)
}
\arguments{
\item{fields}{Named list of fields to hash, already filtered to the identity
set by \code{\link[=.datom_compute_metadata_sha]{.datom_compute_metadata_sha()}}.}
}
\value{
Character SHA-256 hash.
}
\description{
The canonical-form half of \code{metadata_sha}, split from field selection so that
each half is testable on its own: this function decides how a chosen set of
fields becomes bytes and knows nothing about which fields are identity.
}
\details{
Sorts field names by C-locale byte order (\code{method = "radix"}) before hashing
so the result is deterministic regardless of field insertion order \strong{and}
regardless of the host's \code{LC_COLLATE} (default collation sorts differ between
\code{C} and e.g. \code{en_US.UTF-8}, which would otherwise make the same metadata hash
differently on different machines). Sorting here rather than relying on the
declared order of \code{.datom_metadata_identity_fields} is deliberate: it means
hash stability does not depend on how that constant happens to be written, so
re-ordering it for readability cannot silently change every recorded version.
}
\keyword{internal}
