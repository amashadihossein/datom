% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set-members.R
\name{.datom_id_text}
\alias{.datom_id_text}
\title{One \code{id} Field as Text, or \code{NA}}
\usage{
.datom_id_text(id, field)
}
\arguments{
\item{id}{A member's \code{id} map.}

\item{field}{One of \code{project}, \code{name}, \code{kind}, \code{version}.}
}
\value{
A single string, or \code{NA_character_}.
}
\description{
A member read by \code{\link[=datom_get_set]{datom_get_set()}} always has four single-string \code{id} fields
-- the read refuses a payload where one is not. This exists for the other
input: a \code{datom_set} assembled by hand, which is supported and untrusted. \code{NA}
rather than an abort so a listing still shows the member; the abort belongs to
whoever tries to \emph{resolve} it.
}
\keyword{internal}
