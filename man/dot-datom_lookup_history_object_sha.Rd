% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_write.R
\name{.datom_lookup_history_object_sha}
\alias{.datom_lookup_history_object_sha}
\title{Most-recent version_history Stored-Object Hash for a data_sha}
\usage{
.datom_lookup_history_object_sha(conn, name, data_sha, field)
}
\arguments{
\item{conn}{A \code{datom_conn} object (developer, with local path).}

\item{name}{Artifact name.}

\item{data_sha}{Canonical content hash to match.}

\item{field}{\code{"parquet_sha"} (a table's stored parquet) or \code{"document_sha"}
(a set's stored JSON payload).}
}
\value{
The recorded hash, or NULL.
}
\description{
Scans the developer's local \code{version_history.json} (newest-first) for the
most recent entry whose \code{data_sha} matches and that carries a non-empty hash
in \code{field}. Returns NULL when none is found. Reads the local git clone
(offline-friendly); a stale clone is tolerated because the subsequent git push
serializes concurrent writers (a behind clone fails to push before it can
upload).
}
\details{
One scan serves both kinds, because the question is identical in each case --
\emph{has this exact content already been stored, and under which byte hash?} --
and only the field name differs. Two copies would eventually disagree about
what counts as a usable recorded value, and the reuse decision they feed is
the one place where getting that wrong records a hash of bytes nobody stored.
}
\keyword{internal}
