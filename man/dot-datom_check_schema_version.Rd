% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-validate.R
\name{.datom_check_schema_version}
\alias{.datom_check_schema_version}
\title{Check a Document's Declared Schema Version}
\usage{
.datom_check_schema_version(meta, source)
}
\arguments{
\item{meta}{Parsed document (a named list). A non-list or \code{NULL} is treated
as carrying no \code{schema_version}, i.e. v1.}

\item{source}{Path or key of the document, used in the message so the user
knows which file is too new.}
}
\value{
Invisible resolved schema version as an integer. Aborts otherwise.
}
\description{
Reader-side compatibility check for one metadata or manifest document.
Called wherever such a document enters datom from storage or from the local
clone, so that a repo written by a \emph{newer} datom fails with an actionable
message instead of degrading silently -- an older reader would otherwise
find none of the fields it expects and report an empty repo.
}
\details{
The check is deliberately asymmetric:
\itemize{
\item \strong{Newer than this build} -- abort, pointing at the upgrade. Continuing
would mean interpreting a format this build does not know.
\item \strong{Absent} -- treated as v1 and tolerated, so every repo written before
\code{schema_version} existed keeps working unchanged.
\item \strong{Equal or older} -- proceed.
}

A present-but-unusable value (a string, a fraction, \code{NA}, a vector) aborts
as a corrupt document rather than being coerced. Coercion here would compare
garbage against the supported version and could silently read as
"supported"; and in R a comparison against \code{NA} propagates into \verb{if()} as an
opaque "missing value where TRUE/FALSE needed" error rather than anything a
user can act on.

Both aborts carry a condition class so every call site is provably the same
failure: \code{datom_schema_unsupported} for a too-new document,
\code{datom_schema_invalid} for an unusable value.
}
\keyword{internal}
