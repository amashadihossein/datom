% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hashable.R
\name{.datom_column_kind}
\alias{.datom_column_kind}
\title{Classify a Column for Canonical Hashing}
\usage{
.datom_column_kind(x)
}
\arguments{
\item{x}{A single column (vector) from a data frame.}
}
\value{
One of the kind tags \code{"i64"}, \code{"chr"}, \code{"date"}, \code{"time"},
\code{"drtn"}, \code{"num"} for a supported column, or \code{NULL} when unsupported.
}
\description{
The single supported-type classifier underneath the \code{datom-cv1} hash. It
returns the dispatch \emph{kind} for a hashable column or \code{NULL} for an
unsupported one. Both the all-offenders gate (\code{.datom_hash_recourse()})
and the per-column encoder in \code{.datom_canonical_hash()} consume this one
function, so a column the gate accepts can never be one the encoder cannot
encode.
}
\details{
Dispatch is evaluated in a fixed order (reordering can silently change
hashes): \code{bit64::integer64}, factor, \code{Date} (incl. \code{data.table::IDate}),
\code{POSIXct}, \code{difftime}/\code{hms}, \code{data.table::ITime}, then
\code{haven_labelled}/\code{labelled}/\code{labelled_spss} (stripped to their underlying
type and re-classified), then any other explicitly-classed column is
refused, then unclassed atomics (logical/integer/double as \code{"num"},
character as \code{"chr"}), and finally any other type is refused.

Detection uses \code{inherits()} / \code{typeof()} / \code{is.object()} class-string
matching only -- it adds no new package dependency (\code{bit64}, \code{data.table},
\code{haven} are recognised by their class strings, not by being loaded).
}
\keyword{internal}
