% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set.R
\name{datom_write_set}
\alias{datom_write_set}
\title{Write a datom Set}
\usage{
datom_write_set(
  conn,
  members,
  tags = NULL,
  name = NULL,
  message = NULL,
  include_paths = NULL
)
}
\arguments{
\item{conn}{A \code{datom_conn} object from \code{\link[=datom_get_conn]{datom_get_conn()}}, scoped to the
product repo (developer role) -- or a \code{datom_set_draft} from
\code{\link[=datom_assemble_set]{datom_assemble_set()}}, which already carries its connection, name, members
and tags, so a pipe ends \verb{|> datom_write_set()} with nothing typed. The
checks below are the same either way.}

\item{members}{A list of member records from \code{\link[=datom_member]{datom_member()}}, each pinning one
artifact version and optionally carrying its own tags -- or a \code{datom_set}
from \code{\link[=datom_get_set]{datom_get_set()}}, to write back a set that was read. Hand-assembled
lists are refused.}

\item{tags}{Optional named list of set-level text labels -- facts about the
collection itself, such as a description. Same grammar as a member's tags: a
value is one string or several, text only.}

\item{name}{The set's name. Defaults to the \verb{set:} field in
\code{.datom/project.yaml}; when supplied it must equal it.}

\item{message}{Optional commit message.}

\item{include_paths}{Optional character vector of repo-relative paths -- your
own code, \code{renv.lock}, build state -- staged into the \strong{same commit} as the
set. Never mirrored to storage: the storage namespace holds datom artifacts
and nothing else. See the section below.}
}
\value{
Invisibly, a list with \code{name}, \code{data_sha}, \code{metadata_sha} (the
version), \code{member_count} (the count after normalisation), \code{action}
(\code{"none"} or \code{"full"}) and \code{commit_sha}.
}
\description{
Writes a \strong{set}: a versioned, citable, content-addressed collection of
pointers at existing datom artifacts. A set holds no data of its own -- its
payload is the member list plus text labels -- so writing one neither copies
nor moves anything a member contains.
}
\details{
One repo holds one set. The repo declares which, in \code{.datom/project.yaml}:

\if{html}{\out{<div class="sourceCode yaml">}}\preformatted{mode: product
set: study001-adam
}\if{html}{\out{</div>}}

Both are checked before anything is hashed or written, so a repo that has not
declared itself a product repo is refused with nothing left behind.
}
\section{What a set carries, and what it does not}{

User metadata is \strong{tags}, and there is no \verb{metadata =} parameter: a
description is a tag, and a second channel for the same thing would be two
places to look. There is no view or navigation configuration either -- a
folder-like hierarchy is a projection a consumer computes over tags, and any
number of them cost nothing precisely because none is stored.

A set records \strong{no} \code{parents} and \strong{no} \code{source_lineage}. Members are
references, not derivation: lineage flows through tables, and the set is how
you \emph{found} a table rather than how data reached it.
}

\section{Versions, and what moves one}{

The version covers the \strong{whole payload}, members and tags alike. So editing a
tag or a description mints a new version, which is intended: a set exists to be
citable, and "same citation, different labels" would be a lie to whoever cited
it. What does \strong{not} mint a version is a purely syntactic edit -- reordering
tag values or members, repeating a label, or writing a single label as a
one-element array. Those are normalised on the way in, so re-writing an
identical payload is a no-op.
}

\section{Where the payload lives}{

Two copies, at two deliberately different addresses. Git holds
\code{{name}/set.json} at one stable path, modified in place, so git carries the
history and \verb{git diff} between two versions shows which members changed.
Storage holds the same bytes content-addressed at \verb{\{name\}/\{data_sha\}.json}, so
a reader with no clone can fetch an exact version. Any past version is still
reconstructible from the clone alone with
\verb{git show <commit>:\{name\}/set.json}.
}

\section{Carrying your code and environment into the same commit}{

\code{include_paths} stages paths you name into the \strong{one} commit that carries the
payload and its metadata. So checking out a set version's commit yields the
data pointers, the logic that produced them \strong{and} the environment they ran in
-- one clone, one checkout, the whole product. The joint version is
\strong{structural}: nothing records a link between the set and your files, because
the commit \emph{is} the link.

\if{html}{\out{<div class="sourceCode r">}}\preformatted{datom_write_set(conn, members,
                include_paths = c("R", "dp", "renv.lock"))
}\if{html}{\out{</div>}}

Four refusals, all of them before anything is hashed or written, so a refusal
leaves nothing behind: a path that does not exist, a path outside the clone, a
path datom owns (\verb{.datom/}, the set, any artifact directory), and a path
\code{.gitignore} excludes. The last one matters because git stages an ignored path
silently and without complaint, which would leave the set version claiming a
commit that omits exactly the file you named. Refusals win over the no-op
below, since they are settled before change detection runs.

\strong{An unchanged set is still a no-op, however dirty those paths are.} No
commit, no version, and a message pointing at \code{\link[=datom_repo_commit]{datom_repo_commit()}}, which is
the verb for committing your own content at a moment you chose. A data write
that quietly committed work in progress is the thing datom's explicit file
lists exist to prevent, and idempotency must not become a side door into it.
}

\section{Editing a set that already exists}{

Read it, change it, write it back. \code{members} accepts a \code{datom_set} from
\code{\link[=datom_get_set]{datom_get_set()}} directly, so the loop needs no unpacking:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{x <- datom_get_set(conn, "study001-adam")
x$members <- c(x$members, list(datom_member(conn, "lb", v)))
datom_write_set(conn, x)
}\if{html}{\out{</div>}}

The set's own \code{tags} come along with it unless \code{tags} is supplied, so a
read-append-write cannot silently drop the description. Passing
\code{x$members} instead works too, and there \code{tags} is yours to carry.
}

\examples{
# Offline, self-contained: a bare git repo stands in for GitHub and a
# local directory for object storage.
if (requireNamespace("git2r", quietly = TRUE)) {
  tmp <- tempfile("datom-example-")
  remote <- file.path(tmp, "remote.git")
  dir.create(remote, recursive = TRUE)
  git2r::init(remote, bare = TRUE)

  store <- datom_store(
    data = datom_store_local(file.path(tmp, "storage")),
    github_pat = "example-token", # role selector; a local remote needs none
    data_repo_url = remote,
    validate = FALSE
  )
  # A product repo declares itself as one and names the single set it owns.
  datom_init_repo(file.path(tmp, "repo"), "example_project", store,
                  mode = "product", set = "example_product")

  conn <- datom_get_conn(file.path(tmp, "repo"), store)

  # A set points at versions that already exist.
  datom_write(conn, data = datom_example_data("dm"), name = "dm")
  datom_write(conn, data = datom_example_data("lb"), name = "lb")

  members <- list(
    datom_member(conn, "dm", datom_history(conn, "dm")$version[1],
                 tags = list(type = "input")),
    datom_member(conn, "lb", datom_history(conn, "lb")$version[1],
                 tags = list(type = "output", domain = c("safety", "labs")))
  )

  datom_write_set(
    conn, members,
    tags = list(description = "Example product for STUDY-001")
  )

  print(datom_list(conn))

  unlink(tmp, recursive = TRUE)
}
}
\seealso{
\code{\link[=datom_member]{datom_member()}} to declare a member, \code{\link[=datom_assemble_set]{datom_assemble_set()}} to build
a set a member at a time, \code{\link[=datom_write]{datom_write()}} for tables.
}
