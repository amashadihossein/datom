% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query.R
\name{datom_history}
\alias{datom_history}
\title{Show Version History}
\usage{
datom_history(conn, name, n = 10, short_hash = FALSE)
}
\arguments{
\item{conn}{A \code{datom_conn} object from \code{\link[=datom_get_conn]{datom_get_conn()}}.}

\item{name}{Table name.}

\item{n}{Maximum number of versions to return. Default 10.}

\item{short_hash}{If TRUE (default), truncates version and data SHA
columns to 8 characters for readability. Set to FALSE for full hashes.}
}
\value{
Data frame with columns: version, data_sha, timestamp, author,
commit_message.
}
\description{
Shows version history for a table by reading \code{version_history.json}
from S3. Returns the most recent \code{n} versions.
}
\examples{
\dontrun{
tmp <- tempfile("datom_hist_")
store <- datom_store(
  data = datom_store_local(path = file.path(tmp, "storage")),
  github_pat = "ghp_examplePATforDemoPurposesOnly1234",
  data_repo_url = "https://github.com/example/my-project",
  validate = FALSE
)
datom_init_repo(
  path = file.path(tmp, "repo"),
  project_name = "example_project",
  store = store
)
conn <- datom_get_conn(path = file.path(tmp, "repo"), store = store)
datom_write(conn, data = datom_example_data("dm"), name = "dm")
datom_history(conn, "dm")
unlink(tmp, recursive = TRUE)
}
}
