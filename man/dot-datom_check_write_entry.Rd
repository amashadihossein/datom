% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forward-compat.R
\name{.datom_check_write_entry}
\alias{.datom_check_write_entry}
\title{Everything a Write Must Clear Before It Starts}
\usage{
.datom_check_write_entry(conn, artifact = NULL)
}
\arguments{
\item{conn}{A \code{datom_conn} object.}

\item{artifact}{Name of the single artifact this write touches, or \code{NULL} to
check every artifact present in the clone.}
}
\value{
Invisibly \code{NULL}. Aborts on any refusal.
}
\description{
The one entry sequence for every write route. Ordered, and the order matters:
}
\details{
\enumerate{
\item \strong{The floor} -- refuse if this repo has declared this build too old.
\item \strong{The manifest, read through the one shared reader}, which checks the
declared format and then converts an older document in memory. Refusing a
format from the future has to happen before the conversion, because there
is no conversion step for a version this build has never heard of.
\item \strong{The shape the conversion reached.} If the artifact list is still absent
after the chain has run, this document belongs to a lineage this build
cannot produce -- refuse rather than overwrite it in an older shape.
\item \strong{The vocabulary}, on the manifest's top level, on each of its artifact
entries, and on each per-artifact metadata document this write will touch.
}

\strong{Why step 3 is worded as "still absent after the chain" and not "absent".}
A current build meeting a pre-rename repo finds no artifact list either, and a
rule that refused on that would deadlock the very upgrade it exists to
protect: no repo could ever move forward. The discriminator is not the key, it
is whether the chain can \emph{reach} the shape. Note the deliberate asymmetry with
the reader, which will one day \emph{rebuild} on this same condition -- same
evidence, opposite response, because reads limp and writes stop.

\strong{Which per-artifact documents get checked depends on the route}, which is
why \code{artifact} exists. A table write or a metadata-only sync touches one
artifact; the mirror-everything route touches all of them, and it is the route
with no artifact name in its arguments at all. Both cases enumerate through
\code{\link[=.datom_clone_artifact_names]{.datom_clone_artifact_names()}}, the same helper the mirror route itself uses,
so the door cannot end up inspecting a different set than the one that gets
written.

\strong{Callable more than once, and one route calls it twice.} Nothing here
mutates anything, so re-running it is free. The metadata-only route pulls from
the remote as its first act -- after this check has already read the clone --
so a collaborator's newer document can arrive in that pull; that route runs
the sequence again afterwards. See \code{.datom_sync_metadata()}.
}
\keyword{internal}
