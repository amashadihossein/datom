% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest-rebuild.R
\name{.datom_rebuild_member_count}
\alias{.datom_rebuild_member_count}
\title{How Many Members a Set's Current Payload Holds}
\usage{
.datom_rebuild_member_count(conn, name, data_sha)
}
\arguments{
\item{conn}{A \code{datom_conn} object.}

\item{name}{Set name.}

\item{data_sha}{The current version's content hash -- the payload's address.}
}
\value{
An integer count, or \code{NULL}.
}
\description{
The third storage read a set's row costs. Only a set needs it, and only a
rebuild pays it: the healthy writer knows the count from the payload it just
canonicalized.
}
\details{
Returns \code{NULL} for anything that is not a readable payload -- an unusable
\code{data_sha}, a missing object, a document that will not parse. That is the same
trade the rest of this file makes: an absent count is a gap
\code{datom_validate()} owns, while a stand-in count would be a statement about the
set's contents that nothing supports.
}
\keyword{internal}
