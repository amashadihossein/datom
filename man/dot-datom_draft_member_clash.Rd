% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set-draft.R
\name{.datom_draft_member_clash}
\alias{.datom_draft_member_clash}
\title{Is This Member Already in the Draft, and Is It the Same Member?}
\usage{
.datom_draft_member_clash(members, record)
}
\arguments{
\item{members}{The draft's members so far.}

\item{record}{The record about to be added.}
}
\value{
A list with \code{status} -- \code{"new"}, \code{"duplicate"} or \code{"conflict"} -- and,
for the last two, \code{at}: the position of the member already in the draft.
}
\description{
Answers the question the write answers twice, one step earlier, so a repeat
lands on the line that introduced it.
}
\details{
\strong{Both of the write's rules are here, and they are deliberately different
rules.} \code{.datom_order_set_members()} drops an \strong{exact} repeat -- same \code{id}
\emph{and} same tags -- silently, because the digest it dedupes on covers tags. The
same \code{id} with \strong{different} tags survives that and is then refused by
\code{.datom_check_set_payload()}, because merging the labels and picking one entry
both guess. So an exact repeat is a duplicate to skip, and a same-version
disagreement is an error.

\strong{The comparison uses the write's own two mechanisms rather than restating
them}: the \code{project} / \code{name} / \code{version} key the payload check keys on, and
the \code{datom-sv1} member digest the dedup keys on. \code{identical()} on the two
records is the spelling to avoid, and it fails in the direction that refuses
working input: the encoder sorts a tag map's keys and encodes each value as a
sorted, deduplicated \strong{set}, so \code{domain = c("a", "b")} and \code{c("b", "a")} are
one member to the write and to the digest, while \code{identical()} reads them as a
disagreement and aborts.

That is also why nothing needs tidying first. Every spelling the write's tidy
step collapses is a spelling the digest is already blind to, so a record can be
compared -- and stored in the draft -- exactly as the caller supplied it.
}
\keyword{internal}
