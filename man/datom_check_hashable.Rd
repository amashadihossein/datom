% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hashable.R
\name{datom_check_hashable}
\alias{datom_check_hashable}
\title{Check Whether a Table Can Be Hashed by datom}
\usage{
datom_check_hashable(data)
}
\arguments{
\item{data}{A data frame to check.}
}
\value{
Invisibly, a data frame with one row per column of \code{data} and
columns:
\describe{
\item{\code{column}}{Column name.}
\item{\code{class}}{Collapsed class string, or \code{typeof()} when unclassed.}
\item{\code{status}}{\code{"ok"} or \code{"unsupported"}.}
\item{\code{recourse}}{\code{NA} when ok, otherwise how to make the column
hashable.}
}
}
\description{
Pre-flight check for the datom table contract. Reports, per column, whether
\code{datom_write()} can hash it and -- when it cannot -- exactly what to do
about it. Run this before a write to fix a table in one pass instead of
discovering offenders one error at a time.
}
\details{
datom identifies a table version by a canonical hash of its contents
(\code{data_sha}), which requires every column to be a supported type: logical,
integer, double, character, factor, \code{Date}, \code{POSIXct}, \code{difftime}/\code{hms},
\code{data.table::ITime}/\code{IDate}, \code{bit64::integer64}, or a labelled vector over
one of those. List columns (including nested data frames, blobs, and
\code{POSIXlt}), \code{complex}, \code{raw}, \code{sf} geometry, \code{units}, and \code{zoo}/\code{chron}
columns are refused with specific advice.

The advice printed here is the same single-source recourse text
\code{datom_write()} would abort with, so the two can never disagree.
}
\examples{
# A clean table: every column is a supported type
clean <- data.frame(
  id = 1:3,
  score = c(1.5, 2.5, 3.5),
  label = c("a", "b", "c"),
  grp = factor(c("x", "y", "x")),
  day = as.Date(c("2026-01-01", "2026-01-02", "2026-01-03"))
)
datom_check_hashable(clean)

# An offending table: a list column and a complex column
messy <- data.frame(id = 1:2)
messy$notes <- list(c("a", "b"), "c")
messy$z <- c(1 + 2i, 3 + 4i)
report <- datom_check_hashable(messy)
report[report$status == "unsupported", c("column", "recourse")]

}
\seealso{
\code{\link[=datom_write]{datom_write()}}
}
