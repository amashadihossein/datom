% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set-edit.R
\name{.datom_current_artifact_versions}
\alias{.datom_current_artifact_versions}
\title{Each Artifact's Current Version in One Project, in One Read}
\usage{
.datom_current_artifact_versions(conn)
}
\arguments{
\item{conn}{A connection to the project.}
}
\value{
A named character vector of artifact name to current version, with
\code{NA} for an entry that records none. Empty when the project has no
artifacts.
}
\description{
The manifest carries \code{current_version} per artifact, so learning what moved
costs \strong{one read per project} rather than one per member. Only the members
that actually move then pay a snapshot read, through \code{\link[=datom_member]{datom_member()}}, which
is what keeps the new pointer trustworthy.
}
\details{
The manifest is read directly rather than through \code{\link[=datom_list]{datom_list()}} for two
reasons: \code{datom_list()} abbreviates that column to 8 characters by default,
which is not a version a member can record, and its abort would name S3 on a
local backend.

An unreadable manifest \strong{refuses}. It is the one answer that cannot be
reported: a member whose project could not be read is a member whose state is
unknown, which is the same situation as a missing connection.
}
\keyword{internal}
