% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conn.R
\name{datom_attach_gov}
\alias{datom_attach_gov}
\title{Attach Governance to an Existing Project}
\usage{
datom_attach_gov(
  conn,
  gov_store,
  gov_repo_url = NULL,
  gov_local_path = NULL,
  create_repo = FALSE,
  repo_name = NULL,
  github_org = NULL,
  private = TRUE
)
}
\arguments{
\item{conn}{A \code{datom_conn} object for a developer role. Must come from a
no-governance project (i.e. \code{is.null(conn$gov_root)}). Idempotent
re-call is supported only for matching \code{gov_repo_url}.}

\item{gov_store}{A governance store component (\code{datom_store_s3()} or
\code{datom_store_local()}). Where dispatch/ref/migration_history files are
mirrored.}

\item{gov_repo_url}{GitHub remote URL for the shared governance repo.
Mutually exclusive with \code{create_repo = TRUE}. Use \code{datom_init_gov()}
first if no gov repo exists in your organisation yet.}

\item{gov_local_path}{Optional override for the local gov clone directory.
Defaults to a sibling of the data clone, named after the gov repo.}

\item{create_repo}{If \code{TRUE}, create a fresh GitHub repo for governance
via the API and seed it. Mutually exclusive with \code{gov_repo_url}.}

\item{repo_name}{GitHub repo name when \code{create_repo = TRUE}.}

\item{github_org}{GitHub organisation slug (when \code{create_repo = TRUE}).}

\item{private}{Whether the created repo should be private (default \code{TRUE}).
Ignored when \code{create_repo = FALSE}.}
}
\value{
A fresh \code{datom_conn} with governance fields populated. The input
\code{conn} becomes stale; rebind to the returned value.
}
\description{
Promotes a no-governance datom project to gov-attached status. After this
call, the project participates in the governance layer: its dispatch and
ref are registered under \verb{projects/\{name\}/} in the shared gov repo,
\code{datom_projects()} and friends can discover it, and migration is enabled
(Phase 19).
}
\details{
This is the on-ramp from the lightweight gov-on-demand mode to the full
two-tier model. Typical trigger: promoting from a local/laptop filesystem
backend to a shared S3 bucket.

Once attached, governance cannot be detached. There is no
\code{datom_detach_gov()}. Calling \code{datom_attach_gov()} on an already-attached
project is a no-op (idempotent) provided \code{gov_repo_url} matches what is
already recorded; mismatched URL is a hard error (no swap).
}
\examples{
\dontrun{
# Started with a no-gov project on a laptop
conn <- datom_get_conn(path = ".", store = my_no_gov_store)

# Promote when ready to share with a team
gov_s3 <- datom_store_s3(bucket = "acme-gov", access_key = "...", secret_key = "...")
conn <- datom_attach_gov(
  conn,
  gov_store = gov_s3,
  gov_repo_url = "https://github.com/acme/acme-gov.git"
)
}
}
