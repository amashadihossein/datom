% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/store.R
\name{datom_store}
\alias{datom_store}
\title{Create a datom Store}
\usage{
datom_store(
  governance = NULL,
  data,
  github_pat = NULL,
  data_repo_url = NULL,
  gov_repo_url = NULL,
  gov_local_path = NULL,
  github_org = NULL,
  validate = TRUE
)
}
\arguments{
\item{governance}{A store component (e.g., \code{datom_store_s3()}) for governance
files (dispatch, ref, migration history), or \code{NULL} for a no-governance
store. A no-governance store represents a project that has not yet been
promoted via \code{datom_attach_gov()}; \code{gov_repo_url} and \code{gov_local_path}
must also be \code{NULL} in that case.}

\item{data}{A store component (e.g., \code{datom_store_s3()}) for data files
(manifest, tables, metadata).}

\item{github_pat}{GitHub personal access token. If provided, role is
\code{"developer"}. If NULL, role is \code{"reader"}.}

\item{data_repo_url}{GitHub remote URL for the data repository. Required when
\code{github_pat} is provided and \code{create_repo = FALSE} in \code{datom_init_repo()}.}

\item{gov_repo_url}{GitHub remote URL for the shared governance repository.
The governance repo is created once per org via \code{datom_init_gov()} and
referenced here by every project that uses it.}

\item{gov_local_path}{Local directory path for the governance clone. If NULL
(default), the clone is placed as a sibling of the data repo, named after
the basename of \code{gov_repo_url} (e.g., \code{"acme-gov"}).}

\item{github_org}{GitHub organization for repo creation. NULL for personal repos.}

\item{validate}{If \code{TRUE} (default), validate GitHub PAT via API.
Set to \code{FALSE} for tests or offline use.}
}
\value{
A \code{datom_store} object.
}
\description{
Bundles a governance store component, a data store component, and git config
into a single store object. Role (developer vs reader) is derived from
\code{github_pat} presence.
}
