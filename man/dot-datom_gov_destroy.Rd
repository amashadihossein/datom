% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-gov.R
\name{.datom_gov_destroy}
\alias{.datom_gov_destroy}
\title{Destroy the Gov Repo (Guard + Local Clone Removal)}
\usage{
.datom_gov_destroy(gov_local_path, force = FALSE)
}
\arguments{
\item{gov_local_path}{Absolute path to the local gov clone.}

\item{force}{Logical. If \code{TRUE}, destroy even when projects are registered.}
}
\value{
Named character vector of registered project names (invisible), so
the caller can clean up projects first if needed.
}
\description{
Refuses to proceed if any projects are still registered in the gov clone,
unless \code{force = TRUE}. When clear (or forced), removes the local gov clone
directory. GitHub repo and storage deletion are handled by the caller
(e.g., sandbox teardown via \code{gh} CLI and storage backend tools).
}
\details{
\strong{\verb{GOV_SEAM:}} The companion package will eventually own the full gov
lifecycle (init -> register -> destroy) and expose a user-facing
\code{gov_decommission()}. In Phase 15, only the dev sandbox calls this.
}
\keyword{internal}
