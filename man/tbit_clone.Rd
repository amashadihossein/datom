% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conn.R
\name{tbit_clone}
\alias{tbit_clone}
\title{Clone a tbit Repository}
\usage{
tbit_clone(remote_url, path, ...)
}
\arguments{
\item{remote_url}{Remote repository URL (HTTPS or SSH).}

\item{path}{Local path to clone into.}

\item{...}{Additional arguments passed to \code{\link[git2r:clone]{git2r::clone()}}.}
}
\value{
A \code{tbit_conn} object (developer role).
}
\description{
Clones a remote tbit repository and returns a connection. This is the
recommended way for teammates to join an existing tbit project — it wraps
\code{git2r::clone()} and immediately returns a ready-to-use \code{tbit_conn}.
}
\details{
Requires \code{GITHUB_PAT} for HTTPS remotes and the project's AWS credentials
(see \code{vignette("credentials")}).
}
\examples{
\dontrun{
conn <- tbit_clone(
  remote_url = "https://github.com/org/study-001-data.git",
  path = "study_001_data"
)
tbit_pull(conn)
}
}
