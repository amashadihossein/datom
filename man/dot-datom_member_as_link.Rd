% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set-members.R
\name{.datom_member_as_link}
\alias{.datom_member_as_link}
\title{Build the Resolvable Link for One Member Record}
\usage{
.datom_member_as_link(record, what = "member")
}
\arguments{
\item{record}{A member record, with or without a \code{fetch} element.}

\item{what}{Noun for messages about an unusable record.}
}
\value{
A \code{datom_link}.
}
\description{
The single route from a record to a callable link, used by
\code{\link[=datom_fetch_member]{datom_fetch_member()}} and by every leaf \code{\link[=datom_structure_members]{datom_structure_members()}} produces
-- and it is the same factory \code{\link[=datom_get_set]{datom_get_set()}} uses for \verb{$fetch}. So kind
dispatch, and the project hint that lives beside it, have one implementation
reached by every route.
}
\details{
Any \code{fetch} already on the record is dropped first, so a record that came from
a read produces a link over pure data rather than a link carrying a link.
}
\keyword{internal}
