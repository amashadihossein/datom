% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/storage.R
\name{datom_storage_read_json}
\alias{datom_storage_read_json}
\title{Read a JSON Document from a datom Storage Namespace}
\usage{
datom_storage_read_json(conn, key)
}
\arguments{
\item{conn}{A \code{datom_conn} object.}

\item{key}{Relative storage key (after \verb{\{prefix\}/datom/}).}
}
\value{
The parsed JSON document as an R list. Nested structures are kept as
lists (\code{simplifyVector = FALSE}), so JSON arrays of objects do not collapse
into data frames.
}
\description{
Reads and parses a JSON object from storage, dispatching on the connection's
backend. Intended for package developers building tools on top of datom
(e.g. datomanager) that need to inspect datom's own documents -- manifests,
metadata, version history -- without reaching into internals via \code{:::}.
}
\details{
End users should prefer the purpose-built readers: \code{\link[=datom_read]{datom_read()}} for table
data, \code{\link[=datom_list]{datom_list()}} and \code{\link[=datom_summary]{datom_summary()}} for the manifest, and
\code{\link[=datom_history]{datom_history()}} for version history. This export is a byte-level
primitive and does not interpret what it reads.

\code{key} is a \strong{relative} key -- the portion after \verb{\{prefix\}/datom/}, e.g.
\code{"dm/.metadata/metadata.json"}. The backend prepends the namespace itself,
so passing a full key (one containing a \verb{datom/} segment) is refused rather
than silently resolving under \verb{\{prefix\}/datom/\{prefix\}/datom/} and finding
nothing. Keys containing a \code{..} segment or a leading \code{/} are refused too:
reads are confined to this project's namespace.

There is no corresponding write export. Documents are written by
purpose-built verbs (\code{\link[=datom_write]{datom_write()}}, \code{\link[=datom_repo_attach_governance]{datom_repo_attach_governance()}}), not
by a generic byte channel, so that every mutation of a datom namespace
routes through a function that knows what it is writing.
}
\examples{
# Offline, self-contained: a bare git repo stands in for GitHub and a
# local directory for object storage.
if (requireNamespace("git2r", quietly = TRUE)) {
  tmp <- tempfile("datom-example-")
  remote <- file.path(tmp, "remote.git")
  dir.create(remote, recursive = TRUE)
  git2r::init(remote, bare = TRUE)

  store <- datom_store(
    data = datom_store_local(file.path(tmp, "storage")),
    github_pat = "example-token", # role selector; a local remote needs none
    data_repo_url = remote,
    validate = FALSE
  )
  datom_init_repo(file.path(tmp, "repo"), "example_project", store)
  conn <- datom_get_conn(file.path(tmp, "repo"), store)
  datom_write(conn, data = datom_example_data("dm"), name = "dm")

  # Relative key: the part after `{prefix}/datom/`
  meta <- datom_storage_read_json(conn, "dm/.metadata/metadata.json")
  print(meta$hash_algo)

  unlink(tmp, recursive = TRUE)
}
}
\seealso{
\code{\link[=datom_storage_list]{datom_storage_list()}} to discover keys; \code{\link[=datom_read]{datom_read()}},
\code{\link[=datom_list]{datom_list()}}, \code{\link[=datom_history]{datom_history()}} for the interpreted equivalents.
}
