% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sync.R
\name{.datom_notify_manifest_upgraded}
\alias{.datom_notify_manifest_upgraded}
\title{Say That a Manifest's Format Was Moved Forward}
\usage{
.datom_notify_manifest_upgraded(declared, where)
}
\arguments{
\item{declared}{The version the document declared before conversion, as
returned by \code{\link[=.datom_check_schema_version]{.datom_check_schema_version()}}.}

\item{where}{Human-readable name of the copy being written.}
}
\value{
Invisibly \code{NULL}.
}
\description{
Called from the two places that \strong{persist} a converted manifest: the entry
updater, which rewrites the git-tracked file, and the data-side metadata sync,
which mirrors the converted document to storage. Reads convert too and stay
silent, deliberately -- a read changes nothing, and a line on every
\code{\link[=datom_list]{datom_list()}} call would be noise nobody can act on.
}
\details{
Why say anything: conversion is one-way for everybody else. Once this repo's
manifest declares the newer format, a collaborator on an older datom no longer
finds the artifact list where their build looks for it, and their
\code{datom_list()} reports an empty repo \strong{without erroring}. Their
\code{\link[=datom_read]{datom_read()}} keeps working, because the data path never touches the
manifest. That is a real consequence of a command whose stated job was
something else -- \code{datom_validate(fix = TRUE)} in particular reads as a
repair -- and an unannounced one is the silent degradation the whole schema
contract exists to remove.

No-op when the document was already current, which is every ordinary write.
}
\keyword{internal}
