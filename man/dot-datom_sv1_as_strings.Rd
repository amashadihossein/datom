% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hashable-set.R
\name{.datom_sv1_as_strings}
\alias{.datom_sv1_as_strings}
\title{Coerce a Parsed-JSON String Set to a Character Vector}
\usage{
.datom_sv1_as_strings(v, what)
}
\arguments{
\item{v}{The value to normalise.}

\item{what}{Key path used in error messages (e.g. \code{"tags$domain"}).}
}
\value{
A character vector, possibly of length zero.
}
\description{
A tag value arrives in one of three spellings, all meaning the same thing:
a length-1 character vector (\code{"output"}), a longer character vector
(\code{c("safety", "efficacy")}), or -- after a JSON round trip with
\code{simplifyVector = FALSE} -- a list of length-1 strings. All three normalise
to a character vector here, which is what makes a single string and a
one-element array hash identically.
}
\details{
\code{character(0)} and \code{list()} (the parsed form of \verb{[]}) both normalise to the
empty set. Validation refuses an empty tag value upstream and
canonicalization cannot produce one, but the encoder must not depend on that:
an encoder whose correctness rests on an upstream refusal breaks silently the
day the refusal is relaxed.
}
\keyword{internal}
