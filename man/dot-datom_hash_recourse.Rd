% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hashable.R
\name{.datom_hash_recourse}
\alias{.datom_hash_recourse}
\title{Canonical Recourse String for an Unhashable Column}
\usage{
.datom_hash_recourse(x)
}
\arguments{
\item{x}{A single column (vector) from a data frame.}
}
\value{
\code{NULL} when \code{x} is hashable, otherwise a canonical recourse string.
}
\description{
The single source of truth for the remediation advice attached to an
unsupported column. Returns \code{NULL} when \code{.datom_column_kind(x)} classifies
the column as hashable, otherwise the canonical recourse string for the
first matching offender category. Both \code{datom_check_hashable()} and the
\code{.datom_canonical_hash()} all-offenders abort call this one function, so
the checker's advice and the abort's advice can never diverge.
}
\details{
The column name and class are added by the caller (a checker row or an
abort bullet); the strings here are type-scoped only. Detection order
matters: \code{POSIXlt} (a list under the hood) is matched before the generic
list rows; the nested-data-frame list row before the generic list row; and
the class-specific rows (\code{units}, \code{sfc}, \code{yearmon}/\code{yearqtr}/\code{chron})
before the "other classed" fallback.
}
\keyword{internal}
