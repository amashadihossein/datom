% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hashable-set.R
\name{.datom_canonical_set_hash}
\alias{.datom_canonical_set_hash}
\title{Compute the datom-sv1 Canonical Set-Content Hash}
\usage{
.datom_canonical_set_hash(payload)
}
\arguments{
\item{payload}{The set payload: a list with \code{members} (an unnamed list of
member records, each an \code{id} map plus optional \code{tags} map) and optional
set-level \code{tags}.}
}
\value{
A 64-character SHA-256 hex string.
}
\description{
The identity engine for a set artifact: \code{data_sha} for the payload's
semantic content. \code{data_sha = h(0x06 || utf8("datom-sv1") || set(payload))}.
}
\details{
The hash domain is the \strong{parsed-JSON data model}, not the in-memory R
object, and the write path -- which necessarily starts from an in-memory
object -- agrees with it \emph{by construction} rather than through a
serialize-and-reparse pass. Each way a JSON round trip could mutate a value
is unrepresentable instead of handled: there are no numbers (so
integer-versus-double cannot arise), \code{NA} aborts and absence is omission (so
neither the string \code{"NA"} nor \code{null} can appear), and a single string hashes
equal to a one-element array (so scalar-versus-length-1 is not a question).
One structural condition remains: the payload must be parsed with
\code{simplifyVector = FALSE}, so \code{members[]} stays a list of records instead of
collapsing into a data frame. Both storage backends already do that.

No I/O, no serializer, and no dependency that carries versioned data --
which is what makes the hash stable across R versions, \code{jsonlite} versions,
platforms, and architectures.
}
\keyword{internal}
