% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forward-compat.R
\name{.datom_check_document_vocabulary}
\alias{.datom_check_document_vocabulary}
\title{Refuse a Document Carrying a Field This Build Cannot Place}
\usage{
.datom_check_document_vocabulary(doc, known, source)
}
\arguments{
\item{doc}{Parsed document. A non-list, or a list with no names, has no
top-level keys to classify and passes through: it is not this check's job to
report a malformed document, and the write fails on it moments later on its
own terms.}

\item{known}{Character vector of field names this build can place.}

\item{source}{Path or key of the document, for the message.}
}
\value{
Invisibly \code{NULL}. Aborts on an unclassifiable key.
}
\description{
Compares one document's \strong{top-level} key names against the names this build
can classify, and aborts naming any it cannot. The evidence is in the file:
no version comparison, no configuration, no network.
}
\details{
Chosen over a declared version floor as the \emph{primary} mechanism for one
reason -- \strong{it cannot be forgotten.} A floor protects a repo only if somebody
remembers to raise it; this fires on the evidence whether or not anyone did
anything.

\strong{Top-level keys only, and that is a scope rather than a shortcut.} It means
do not descend into a value -- \code{custom} holds arbitrary user keys by design
and is classified as one recognised field, and the manifest's \code{summary} block
is a derived aggregate that is rebuilt on every write. It does \strong{not} mean
skip the manifest's artifact entries: an entry is its own document for this
purpose and gets checked against its own vocabulary.

\strong{The check cannot fire on the upgrade path}, and no code guards against
that: a newer build's vocabulary is a superset of every older build's, so it
can never meet a name it does not know. A directional special case would be
dead code protecting an unreachable state.

The accepted cost is that any release adding a field to a datom-owned document
forces a fleet-wide \strong{writer} upgrade, cosmetic additions included. Writes
are infrequent, done by few people, and they change content -- a false refusal
costs one person an install, a miss costs corrupted data.
}
\keyword{internal}
