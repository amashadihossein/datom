% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sync.R
\name{.datom_artifacts_of_kind}
\alias{.datom_artifacts_of_kind}
\title{Select the Artifacts of One Kind}
\usage{
.datom_artifacts_of_kind(artifacts, kind)
}
\arguments{
\item{artifacts}{The manifest's \code{artifacts} list, or \code{NULL}.}

\item{kind}{\code{"table"} or \code{"set"}.}
}
\value{
The entries of that kind, names preserved.
}
\description{
The one place the artifact list is filtered by \code{kind}. Four counters need it
-- two in the manifest's stored \code{summary} block, plus the numbers
\code{\link[=datom_summary]{datom_summary()}} and \code{\link[=datom_status]{datom_status()}} count for themselves -- and a
predicate written out at each of them is a predicate that can differ at one
of them.
}
\details{
\strong{An entry that is not a named list is skipped rather than dereferenced.}
The upgrade step deliberately passes such an entry through untouched, because
it has no shape to convert; without the check here, that preserved entry
reaches \code{entry$kind} and aborts with "$ operator is invalid for atomic
vectors". \code{\link[=datom_status]{datom_status()}} is the one that must not do that: it exists to
describe a connection when the manifest cannot be trusted, and this count
sits outside the error handling that gives it that tolerance. A hand-edited
manifest is exactly the document most likely to reach it.

Skipping is not the same as tolerating a \strong{missing} \code{kind}, which stays
deliberately uncounted: a typed entry with no type means the conversion was
skipped, and a visibly wrong count is the intended signal for that.
}
\keyword{internal}
