% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/repo.R
\name{datom_repo_delete}
\alias{datom_repo_delete}
\title{Delete the Data GitHub Repository and Local Clone}
\usage{
datom_repo_delete(conn, confirm, force_gov_attached = FALSE)
}
\arguments{
\item{conn}{A \code{datom_conn} object (developer role required).}

\item{confirm}{Character string. Must equal \code{conn$project_name} exactly.
No interactive prompts -- this must be supplied explicitly.}

\item{force_gov_attached}{Logical. \code{FALSE} (default) refuses to run when
governance is attached (\code{!is.null(conn$gov_root)}). Pass \code{TRUE} only
when called programmatically from \code{datomanager::gov_decommission()}.}
}
\value{
Invisible \code{TRUE} on success.
}
\description{
Deletes the data-side GitHub repository via the GitHub REST API and removes
the local clone directory. This is the data-side teardown step for a datom
project.
}
\details{
\strong{Solo projects} (no governance attached): call this together with
\code{\link[=datom_storage_delete_prefix]{datom_storage_delete_prefix()}} for a complete teardown.

\strong{Governed projects}: use \code{datomanager::gov_decommission()} instead.
That function calls \code{datom_repo_delete()} internally (with
\code{force_gov_attached = TRUE}). Calling \code{datom_repo_delete()} directly on
a governed project without that flag is refused to prevent accidentally
orphaning the governance registration.

Steps:
\enumerate{
\item Delete the data GitHub repo via the GitHub REST API (requires
\code{conn$github_pat} with \code{delete_repo} scope; skipped with a warning
when \code{conn$github_pat} is NULL or when the remote is not GitHub).
Aborts if \code{conn$data_repo_url} is not set.
\item Remove the local clone directory (\code{conn$path}).
}

Each step is warn-and-continue on failure so the other still runs.
}
\examples{
\dontrun{
# Solo project teardown (no governance)
datom_storage_delete_prefix(conn)
datom_repo_delete(conn, confirm = conn$project_name)
}
}
\seealso{
\code{\link[=datom_storage_delete_prefix]{datom_storage_delete_prefix()}}, \code{\link[=datom_repo_set_data_store]{datom_repo_set_data_store()}}
}
