% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lineage.R
\name{datom_validate_lineage}
\alias{datom_validate_lineage}
\title{Validate Source Lineage Consistency}
\usage{
datom_validate_lineage(conn, name, version = NULL)
}
\arguments{
\item{conn}{A \code{datom_conn} object from \code{\link[=datom_get_conn]{datom_get_conn()}}.}

\item{name}{Table name.}

\item{version}{Optional metadata_sha (datom version). If NULL, checks
the current version.}
}
\value{
A list with:
\describe{
\item{status}{One of \code{"ok"}, \code{"mismatch"}, \code{"unchecked"}, \code{"error"}.}
\item{missing}{List of entries in the computed union but absent from declared.}
\item{extra}{List of entries declared but absent from the computed union.}
\item{wrong_version}{List of entries where project+table match but
\code{version_sha} differs. Each element has \code{declared} and \code{computed} sub-lists.}
\item{message}{Human-readable summary string.}
}
}
\description{
Checks that a table's declared \code{source_lineage} matches the union of its
parents' \code{source_lineage} fields. Detects missing entries (in the computed
union but absent from declared), extra entries (declared but not in the
union), and wrong-version entries (matching project+table but different
\code{version_sha}).
}
\details{
datom validates schema shape only at write time. This function provides
on-demand semantic checking, suitable for CI runs, audits, or pre-publication
gates.

Tables without \code{parents} return \code{status = "unchecked"} -- there is no union
to compare against. Tables where any parent's metadata is unreachable return
\code{status = "error"}.
}
