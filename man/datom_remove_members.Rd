% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set-edit.R
\name{datom_remove_members}
\alias{datom_remove_members}
\title{Drop members from a set}
\usage{
datom_remove_members(x, member = NULL, tags = NULL, version = NULL)
}
\arguments{
\item{x}{A \code{datom_set} from \code{\link[=datom_get_set]{datom_get_set()}}, or a \code{datom_set_draft} from
\code{\link[=datom_assemble_set]{datom_assemble_set()}}.}

\item{member}{The member to drop, as its name, a member record, or a link.
Optional only when \code{tags} or \code{version} selects on its own.}

\item{tags}{Optional named list of labels selecting members, e.g.
\code{list(status = "draft")}. A member matches when it carries every label
listed.}

\item{version}{Optional version, or a prefix of one, selecting the members
pinned at it.}
}
\value{
\code{x} without the selected members, its \code{version} and \code{data_sha} emptied
because they described a payload it no longer holds, and what was dropped
appended to its \code{datom_edits} attribute -- which \code{\link[=datom_write_set]{datom_write_set()}} turns
into the commit message.
}
\description{
Removes the members you select and returns the set without them. \strong{Nothing is
written}: the object comes back edited, and the set is stored only when you
pass the result to \code{\link[=datom_write_set]{datom_write_set()}}.
}
\details{
It exists because the hand-rolled version is silently wrong. Filtering a
member list by name drops \strong{every} version of that name, so a set holding a
live table beside a deliberately frozen baseline loses both; removing by
position removes a different member the day somebody adds one.
}
\section{Why there is no connection argument}{

Not an oversight. Removing a member only has to \strong{find} a pointer the set
already holds, while adding or repointing one has to \strong{resolve} it -- read the
artifact's metadata, confirm its kind, record the project that wrote it. So
this verb does no IO at all and needs no credentials, which is also why it is
the one edit verb that works on a set read through a storage-only connection
with no clone.
}

\section{Selecting what to drop}{

A selection is \strong{required}. \code{datom_remove_members(x)} would mean removing
every member, which the writer refuses anyway, so it aborts instead of building
a payload the write then rejects. The safe default for a destructive verb is
nothing -- the opposite of \code{\link[=datom_update_members]{datom_update_members()}}, where the safe default is
everything because a refresh is idempotent.

Select by name, by a member record, or by a link, and narrow with \code{tags} or
\code{version}; \code{tags} or \code{version} on their own select every member they match, so
\code{tags = list(status = "draft")} drops the labelled ones.

Three things it refuses rather than doing quietly:\tabular{ll}{
   What you asked for \tab Why it stops \cr
   a name matching more than one member \tab that is the silently-wrong spelling this verb replaces -- it would drop a frozen baseline along with the live table \cr
   a selection matching nothing \tab it is a typo, and \code{Filter()} reports success for it \cr
   every member \tab a set with no members cannot be written, and the refusal belongs on the line that emptied it \cr
}


\strong{An ambiguous name refuses here while \code{\link[=datom_update_members]{datom_update_members()}} skips it}, and
the asymmetry is the consequence rather than a taste: skipping a repoint leaves
a valid pinned version behind, while skipping a removal silently does nothing at
all.
}

\examples{
# Offline, self-contained: a bare git repo stands in for GitHub and a
# local directory for object storage.
if (requireNamespace("git2r", quietly = TRUE)) {
  tmp <- tempfile("datom-example-")
  remote <- file.path(tmp, "remote.git")
  dir.create(remote, recursive = TRUE)
  git2r::init(remote, bare = TRUE)

  store <- datom_store(
    data = datom_store_local(file.path(tmp, "storage")),
    github_pat = "example-token", # role selector; a local remote needs none
    data_repo_url = remote,
    validate = FALSE
  )
  # A product repo declares itself as one and names the single set it owns.
  datom_init_repo(file.path(tmp, "repo"), "example_project", store,
                  mode = "product", set = "example_product")

  conn <- datom_get_conn(file.path(tmp, "repo"), store)

  datom_write(conn, data = datom_example_data("dm"), name = "dm")
  datom_write(conn, data = datom_example_data("lb"), name = "lb")
  datom_write_set(conn, list(
    datom_member(conn, "dm", datom_history(conn, "dm")$version[1],
                 tags = list(type = "input")),
    datom_member(conn, "lb", datom_history(conn, "lb")$version[1],
                 tags = list(status = "draft"))
  ))

  x <- datom_get_set(conn, "example_product")

  # By label, which is the selection that does not depend on position.
  x <- datom_remove_members(x, tags = list(status = "draft"))
  print(datom_list_members(x))

  datom_write_set(conn, x)

  unlink(tmp, recursive = TRUE)
}
}
\seealso{
\code{\link[=datom_update_members]{datom_update_members()}} to repoint members instead,
\code{\link[=datom_list_members]{datom_list_members()}} to see what a set holds, \code{\link[=datom_write_set]{datom_write_set()}} to store
the result.
}
