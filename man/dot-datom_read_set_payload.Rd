% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set.R
\name{.datom_read_set_payload}
\alias{.datom_read_set_payload}
\title{Download, Verify and Parse a Set's Stored Payload}
\usage{
.datom_read_set_payload(conn, name, data_sha, document_sha)
}
\arguments{
\item{conn}{A \code{datom_conn} object.}

\item{name}{Set name.}

\item{data_sha}{The resolved content hash -- the payload's storage address.}

\item{document_sha}{The recorded SHA-256 of the stored payload bytes.}
}
\value{
The parsed payload, with \code{members} kept as a list of records.
}
\description{
Download, hash, \strong{then} parse. The order is the point: a set read must not
parse an unverified payload, which is the same gate position
\code{\link[=.datom_read_parquet]{.datom_read_parquet()}} uses for \code{parquet_sha}.
}
\details{
\strong{\code{.datom_storage_read_json()} cannot be used here, and it would work.} It
parses, so after calling it there is nothing left to hash but bytes
re-serialized locally -- a hash of bytes nobody stored, which is exactly the
defect the write path guards against, inverted. It returns a structure
identical to parsing the downloaded file, so nothing fails if you reach for
it; the integrity check simply stops meaning anything.

\strong{A missing \code{document_sha} is an error, not a skip.} \code{parquet_sha}'s
skip-on-absent branch exists purely as a grace for metadata written before
that field did. Sets have recorded \code{document_sha} since their first write, so
there is no legacy population to be lenient about, and reproducing the grace
would build a silent-degradation path on purpose.

\code{data_sha} is deliberately \strong{not} recomputed from the parsed payload. It is
the address the payload was fetched from, so it catches nothing
\code{document_sha} did not, and it would refuse a payload a newer datom wrote --
the sv1 encoder aborts on a top-level payload key it does not know. Same
reason the parsed payload is not re-validated. Reads limp.
}
\keyword{internal}
