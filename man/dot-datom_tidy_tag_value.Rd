% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set.R
\name{.datom_tidy_tag_value}
\alias{.datom_tidy_tag_value}
\title{Tidy One Tag Value}
\usage{
.datom_tidy_tag_value(v)
}
\arguments{
\item{v}{A tag value.}
}
\value{
A sorted, deduplicated character vector, or \code{v} unchanged.
}
\description{
Sorts and dedupes a tag value, and normalises the three spellings of a string
set into one character vector so that \code{auto_unbox = TRUE} writes a single label
as a bare string and only a genuine multi-label value as an array.
}
\details{
\strong{Anything this build does not recognise as text is returned untouched.} That
is what keeps tidying from aborting: \code{sort()} on a list or a function fails
with a base-R message that names nothing, whereas leaving the value alone hands
it to the validator, whose message names the key and the allowed types. Tidy
what you can, refuse the rest -- in that order, and never the reverse.

A missing value is left alone for the same reason: \code{NA} has no text meaning, so
it is a refusal rather than a tidy case.
}
\keyword{internal}
