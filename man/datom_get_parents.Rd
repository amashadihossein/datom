% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query.R
\name{datom_get_parents}
\alias{datom_get_parents}
\title{Get Parent Lineage for a Table}
\usage{
datom_get_parents(conn, name, version = NULL)
}
\arguments{
\item{conn}{A \code{datom_conn} object from \code{\link[=datom_get_conn]{datom_get_conn()}}.}

\item{name}{Table name.}

\item{version}{Optional metadata_sha (datom version). If NULL, reads
current metadata. If provided, fetches the versioned metadata snapshot
from S3.}
}
\value{
List of parent entries (each with \code{source}, \code{table}, \code{version},
\code{data_sha}), or \code{NULL} if no lineage is recorded. The \code{data_sha} field
is the parent's authoritative data SHA recorded via \code{\link[=datom_parent]{datom_parent()}},
and together with \code{source} and \code{version} is sufficient to select the
parent's project connection and its pinned version.
}
\description{
Reads the \code{parents} field from a table's metadata. Returns the lineage
entries recorded at write time by \code{\link[=datom_write]{datom_write()}}. For imported tables or
derived tables with no recorded lineage, returns \code{NULL}.
}
\examples{
# Offline, self-contained: a bare git repo stands in for GitHub and a
# local directory for object storage.
if (requireNamespace("git2r", quietly = TRUE)) {
  tmp <- tempfile("datom-example-")
  remote <- file.path(tmp, "remote.git")
  dir.create(remote, recursive = TRUE)
  git2r::init(remote, bare = TRUE)

  store <- datom_store(
    data = datom_store_local(file.path(tmp, "storage")),
    github_pat = "example-token", # role selector; a local remote needs none
    data_repo_url = remote,
    validate = FALSE
  )
  datom_init_repo(file.path(tmp, "repo"), "example_project", store)
  conn <- datom_get_conn(file.path(tmp, "repo"), store)

  dm <- datom_example_data("dm")
  datom_write(conn, data = dm, name = "dm")
  datom_write(
    conn,
    data    = dm[dm$SEX == "F", ],
    name    = "dm_female",
    parents = list(datom_parent(conn, "dm", datom_history(conn, "dm")$version[1]))
  )
  datom_get_parents(conn, "dm_female")

  unlink(tmp, recursive = TRUE)
}
}
\seealso{
\code{\link[=datom_get_lineage]{datom_get_lineage()}} for a unified interface that also exposes
the transitive \code{source_lineage} field via \code{depth = "source"}.
}
