% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set.R
\name{.datom_check_include_paths}
\alias{.datom_check_include_paths}
\title{Check the Caller's Extra Paths Before a Set Write Does Anything}
\usage{
.datom_check_include_paths(conn, name, include_paths)
}
\arguments{
\item{conn}{A \code{datom_conn} object with a local path.}

\item{name}{The set being written, as resolved by
\code{\link[=.datom_check_set_write_gates]{.datom_check_set_write_gates()}}. Named rather than discovered because a
first write has no directory to discover.}

\item{include_paths}{The caller's character vector, or \code{NULL}.}
}
\value{
Absolute paths in the clone, or \code{NULL}. \strong{Absolute}, because
\code{\link[=.datom_commit_and_mirror]{.datom_commit_and_mirror()}} relativises what it is given against \code{conn$path},
and \code{fs::path_rel()} on an already-relative path resolves it against the
working directory instead -- which aborts with "files do not exist" pointing
somewhere the caller never named.
}
\description{
\code{include_paths} is the \strong{only} way a commit datom makes on its own initiative
may carry a path datom does not own, and it is allowed only because the caller
enumerated it (R14.3). What it buys is that checking out a set version's commit
yields the data pointers \strong{and} the code and environment that produced them.
So every refusal here is a refusal to produce a commit that would claim more
than it holds.
}
\details{
Four refusals, in this order, each with its own condition class:
\enumerate{
\item \strong{Not a path inside the clone.} An absolute path, or one climbing out
through \code{..}, refused lexically before the filesystem is touched.
\code{fs::path()} joins an absolute second argument \emph{onto} the clone path rather
than replacing it, so \verb{/etc/passwd} would otherwise be reported as a missing
path inside the repo -- a correct refusal whose message names the wrong
thing.
\item \strong{A datom-owned path.} \verb{.datom/}, the set being written, and any artifact
directory already in the clone. The write stages those itself, so listing one
is either a misunderstanding or an attempt to hand-place a datom document
into a commit through a caller's argument.
\item \strong{A path that does not exist.} An error, never a skip: a joint commit is
deterministic or it is refused.
\item \strong{A path git is ignoring.} \code{git2r::add()} on a gitignored path raises
nothing and stages nothing, and \code{\link[=.datom_git_commit]{.datom_git_commit()}} cannot notice, because
it objects only when the staging area ends up empty and datom's own files are
always in it. The commit would therefore succeed while omitting exactly the
file the caller named, and the set version would claim a joint commit it does
not have. Refused rather than dropped in silence (decided 2026-09-18).
}

\strong{This runs before the first hash and the first local write}, the same
placement as the two gates above, so a refused joint commit leaves nothing
behind. One consequence, stated so nobody later softens it: change detection
needs the hashes, so a bad path is an error \strong{even when the set is unchanged}.
The refusal wins over the no-op.
}
\keyword{internal}
