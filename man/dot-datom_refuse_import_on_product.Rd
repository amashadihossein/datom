% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sync.R
\name{.datom_refuse_import_on_product}
\alias{.datom_refuse_import_on_product}
\title{Refuse the File-Import Path on a Product Repo}
\usage{
.datom_refuse_import_on_product(conn, verb)
}
\arguments{
\item{conn}{A \code{datom_conn} object with a local path.}

\item{verb}{Name of the import verb being refused, for the message.}
}
\value{
Invisibly \code{NULL}. Aborts with class \code{datom_import_on_product} when the
repo declares \code{mode: product}.
}
\description{
A \code{mode: product} repo \strong{builds} its artifacts: derived tables written from
data frames, and one set collecting them. It never onboards source files, so
the two import verbs refuse instead of answering. Before this they answered
unhelpfully -- \verb{input_files/} exists and is empty on such a repo, so the scan
reported "no files found" and handed back a zero-row frame, which describes a
repo with nothing to import rather than a repo that does not import.
}
\details{
\strong{Read from the file, not from the connection}, and the rule behind that is
worth carrying: a check that \strong{authorises a write} must see the config as it
is \emph{now}, because a hand edit or a pull can replace it after the connection was
built. Only \code{\link[=datom_status]{datom_status()}}, which reports rather than decides, reads the mode
off the connection. A future site applies the same test: does it authorise a
write? Then it reads the file.

\strong{Three steps in one place, and the middle one is easy to leave out.} Parsing
this file makes this a new \emph{gated} parse: every site that reads
\code{.datom/project.yaml} checks its declared format first, or a build that cannot
interpret the file acts on fields it has misread. Skipping that step here would
reopen exactly that hole, on a path that writes.

\strong{Called from both import verbs, not only the first.} \code{\link[=datom_sync]{datom_sync()}} takes a
manifest data frame, so a caller can hand it rows that a refusing
\code{\link[=datom_sync_manifest]{datom_sync_manifest()}} would never have produced.

\strong{Above the input-file scan, never in its empty branch.} A product repo with a
file dropped into \verb{input_files/} by accident would otherwise be imported, which
is the thing this exists to prevent; the unhelpful no-op only happened when the
directory was empty.
}
\keyword{internal}
