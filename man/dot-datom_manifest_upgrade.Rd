% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest-upgrade.R
\name{.datom_manifest_upgrade}
\alias{.datom_manifest_upgrade}
\title{Apply Every Upgrade Step from a Declared Version to Current}
\usage{
.datom_manifest_upgrade(manifest, declared)
}
\arguments{
\item{manifest}{Parsed manifest document (a named list).}

\item{declared}{Declared schema version, as returned by
\code{\link[=.datom_check_schema_version]{.datom_check_schema_version()}}.}
}
\value{
The document in current shape, declaring the version it reached.
}
\description{
The dispatcher. Runs each step from \code{declared} up to
\code{.datom_supported_schema} in order, then records the version it reached.
}
\details{
\code{declared} is a parameter rather than something read off the document,
because the only correct source for it is
\code{\link[=.datom_check_schema_version]{.datom_check_schema_version()}}, which returns it after refusing a document
this build cannot convert. Taking it as an argument is what makes
"check first, then upgrade" structural: there is no way to call this without
having obtained the number from the check.

Identity on a document already at the current version -- zero steps run. A
document declaring a version \emph{above} current is returned untouched too, since
no step exists for it; that state is unreachable through the check, which
aborts first.
}
\keyword{internal}
