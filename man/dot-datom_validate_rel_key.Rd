% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-validate.R
\name{.datom_validate_rel_key}
\alias{.datom_validate_rel_key}
\title{Validate a Caller-Supplied Relative Storage Key}
\usage{
.datom_validate_rel_key(key, arg = "key")
}
\arguments{
\item{key}{Value to validate as a relative storage key.}

\item{arg}{Name of the calling argument, used in the error message.}
}
\value{
Invisible \code{key} on success. Aborts otherwise.
}
\description{
Guards a whole key string that a caller composed, as opposed to one datom
built itself from validated parts. The internal key builders in
\code{R/utils-path.R} need no such check: \code{.datom_validate_name()} admits only
\verb{[a-zA-Z0-9_ ()-]} and \code{.datom_validate_sha()} only hex, so their output
cannot contain a \code{..} segment or a \verb{datom/} segment. A key arriving through
a public export has had no such filtering.
}
\details{
Two distinct failures are caught:
\itemize{
\item \strong{Traversal / shape.} A \code{..} segment or a leading \code{/} escapes the datom
namespace on the local backend, where the key is pasted into a path and
resolved by the filesystem (\code{.datom_local_path()}). This applies to reads
as much as to writes -- reading \code{../../secrets.json} is exactly the sort
of probe the guard sweep in #74 existed to close.
\item \strong{A full key passed where a relative one belongs.} The two key shapes are
documented at the top of \code{R/utils-path.R}; mixing them does not error
today, it resolves under \code{{prefix}/datom/{prefix}/datom/...} and finds
nothing, which reads to the caller as a missing object rather than a
malformed key. A \code{datom} path segment is the detectable form, and it can
never occur in a legitimate relative key because \code{datom} is a reserved
artifact name (\code{.datom_reserved_names}).
}
}
\keyword{internal}
