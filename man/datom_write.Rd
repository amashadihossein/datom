% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_write.R
\name{datom_write}
\alias{datom_write}
\title{Write a datom Table}
\usage{
datom_write(
  conn,
  data = NULL,
  name = NULL,
  metadata = NULL,
  message = NULL,
  parents = NULL,
  .source_lineage = NULL,
  .table_type = "derived",
  .original_file_sha = NULL,
  .original_format = NULL
)
}
\arguments{
\item{conn}{A \code{datom_conn} object from \code{\link[=datom_get_conn]{datom_get_conn()}}.}

\item{data}{Data frame to write. If NULL with name, does metadata-only sync.}

\item{name}{Table name. If NULL with NULL data, does a data-only metadata
sync to storage (manifest + per-table metadata).}

\item{metadata}{Optional list of custom metadata.}

\item{message}{Optional commit message.}

\item{parents}{Optional list of parent records produced by
\code{\link[=datom_parent]{datom_parent()}}, each carrying \code{source}, \code{table}, \code{version},
\code{data_sha}, and \code{source_lineage}. When supplied, the table's
\code{source_lineage} is derived as the deduplicated union of the parents'
\code{source_lineage} and each parent is recorded lean (\code{source}, \code{table},
\code{version}, \code{data_sha}). NULL if no lineage is recorded. There is no
public \code{source_lineage} parameter; it is always derived from \code{parents}.}

\item{.source_lineage}{Internal. Flat list of transitive non-derived
source descriptors (each with \code{project}, \code{table}, \code{version_sha}) for the
imported self-entry path, set by \code{\link[=datom_sync]{datom_sync()}}. Unused on the derived
(parents) path.}

\item{.table_type}{Internal. \code{"derived"} (default) or \code{"imported"}
(set by \code{datom_sync()}).}

\item{.original_file_sha}{Internal. SHA of source file
(set by \code{datom_sync()}); NULL for derived.}

\item{.original_format}{Internal. Original file format
(set by \code{datom_sync()}); NULL for derived.}
}
\value{
List with deployment details.
}
\description{
Writes data to a datom repository. Commits to git, pushes, and syncs to S3.
}
\examples{
\dontrun{
tmp <- tempfile("datom_write_")
store <- datom_store(
  data = datom_store_local(path = file.path(tmp, "storage")),
  github_pat = "ghp_examplePATforDemoPurposesOnly1234",
  data_repo_url = "https://github.com/example/my-project",
  validate = FALSE
)
datom_init_repo(
  path = file.path(tmp, "repo"),
  project_name = "example_project",
  store = store
)
conn <- datom_get_conn(path = file.path(tmp, "repo"), store = store)

# --- Basic write (no lineage) ---
dm <- datom_example_data("dm")
datom_write(conn, data = dm, name = "dm")

# --- Write with a single parent ---
# Use datom_parent() to resolve each parent's data_sha and lineage.
lb <- datom_example_data("lb")
datom_write(conn, data = lb, name = "lb")
lb_summary <- aggregate(
  list(n = lb$LBTESTCD), by = list(LBTESTCD = lb$LBTESTCD), FUN = length
)
datom_write(
  conn,
  data    = lb_summary,
  name    = "lb_summary",
  message = "Lab test counts",
  parents = list(
    datom_parent(conn, "lb", datom_history(conn, "lb")$version[1])
  )
)

# --- Write with multiple parents ---
# Each parent is declared with datom_parent(); datom_write() derives
# the table's source_lineage as the union of the parents' lineages.
dm_lb_merged <- merge(dm, lb, by = "USUBJID")
datom_write(
  conn,
  data    = dm_lb_merged,
  name    = "dm_lb_merged",
  message = "Demographics joined with lab results",
  parents = list(
    datom_parent(conn, "dm", datom_history(conn, "dm")$version[1]),
    datom_parent(conn, "lb", datom_history(conn, "lb")$version[1])
  )
)

unlink(tmp, recursive = TRUE)
}
}
