% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-sha.R
\name{.datom_encode_numeric}
\alias{.datom_encode_numeric}
\title{Encode a Numeric Payload for Canonical Hashing}
\usage{
.datom_encode_numeric(x)
}
\arguments{
\item{x}{A vector coercible to double (logical, integer, double, or the
numeric payload of a Date/POSIXct/difftime column).}
}
\value{
A raw vector of \code{8 * length(x)} bytes.
}
\description{
The single shared numeric encoder used by the \code{num}, \code{date}, \code{time}, and
\code{drtn} column kinds of \code{datom-cv1}. Produces a fixed, platform-independent
byte sequence: IEEE-754 doubles written little-endian regardless of host
endianness, with three canonicalizations so that logically-equal values
encode identically:
}
\details{
\itemize{
\item Every \code{NaN} payload (e.g. \code{0/0}, a signalling NaN, a negative NaN) is
folded to R's single canonical quiet \code{NaN} bit pattern.
\item \code{-0.0} is converted to \code{+0.0}.
\item \code{NA_real_} is preserved as its own distinct bit pattern -- it is a
specific \code{NaN} payload in R and is deliberately \emph{not} folded into the
canonical \code{NaN}, so \code{NA_real_} and \code{NaN} encode differently.
}

No rounding is applied: doubles are encoded bit-exact.
}
\keyword{internal}
