% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_write.R
\name{.datom_check_artifact_kind}
\alias{.datom_check_artifact_kind}
\title{Refuse to Write One Kind of Artifact Over Another}
\usage{
.datom_check_artifact_kind(current, name, expected)
}
\arguments{
\item{current}{The artifact's current metadata document, or \code{NULL} when the
name is free.}

\item{name}{Artifact name.}

\item{expected}{\code{"table"} or \code{"set"} -- the kind the caller is about to write.}
}
\value{
Invisibly \code{NULL}. Aborts on a kind mismatch.
}
\description{
One name means one artifact, whatever its kind, because both kinds store
everything under \verb{\{name\}/} -- a set named \code{dm} beside a table named \code{dm} would
write the same \code{dm/.metadata/metadata.json} and each would clobber the other.
}
\details{
\strong{Checked against the metadata document in storage, not against the
manifest.} The manifest is a projection and can lag behind a write that got
partway through, so it can say a name is free when it is not. The document is
also the copy \code{\link[=.datom_has_changes]{.datom_has_changes()}} has just read, so the comparison costs no
extra round trip -- which is why the current document is passed in rather than
fetched here.

An absent \code{kind} reads as \code{"table"}: every document written before the field
existed describes a table, because sets did not exist. The pairing with a
format check is not needed here the way it is in \code{datom_member()} -- a document
from a future datom has already been refused at the write entry.
}
\keyword{internal}
