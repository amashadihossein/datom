% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/projects.R
\name{datom_projects}
\alias{datom_projects}
\title{List Projects Registered in the Governance Repo}
\usage{
datom_projects(x)
}
\arguments{
\item{x}{A \code{datom_conn} or a \code{datom_store} with a governance component.}
}
\value{
A data frame, sorted by \code{name}, with columns:
\code{name} (character), \code{data_backend} (character),
\code{data_root} (character), \code{data_prefix} (character; NA when absent),
\code{registered_at} (character ISO8601 from clone mtime; NA on storage path).
}
\description{
Returns a data frame with one row per project registered in the shared
governance repo. Useful for managers and auditors who need to see the
portfolio without having to clone every data repo.
}
\details{
Accepts either a \code{datom_conn} (typically the developer's existing
connection -- reads the local gov clone) or a \code{datom_store} (lets a caller
enumerate the portfolio before connecting to any specific project).

Read path:
\itemize{
\item If a local gov clone is available (developer or any caller whose
\code{gov_local_path} exists on disk), \verb{projects/} is listed from disk and
each \code{ref.json} is read locally. No network calls.
\item Otherwise the gov storage client is used: \verb{projects/} is listed and
each \code{projects/{name}/ref.json} is fetched.
}

Corrupt registry entries (missing \code{ref.json}, unreadable JSON) emit a
warning and are skipped -- one bad project does not take down the listing.
}
