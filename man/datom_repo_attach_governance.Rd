% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/repo.R
\name{datom_repo_attach_governance}
\alias{datom_repo_attach_governance}
\title{Write the Data-Side Governance Attachment Record}
\usage{
datom_repo_attach_governance(conn, gov_repo_url, gov_store, message = NULL)
}
\arguments{
\item{conn}{A \code{datom_conn} object with \code{role = "developer"} and a local
data clone (\code{conn$path}).}

\item{gov_repo_url}{HTTPS clone URL of the governance git repository to
record.}

\item{gov_store}{A \code{datom_store_s3} or \code{datom_store_local} component for the
governance storage. Only its location fields are persisted; credentials
are discarded.}

\item{message}{Optional commit message. Defaults to
\code{"Attach governance: {project_name}"}.}
}
\value{
Invisibly, the SHA of the resulting data-repo commit.
}
\description{
Writes \code{governance.json} -- the data-side pointer recording which governance
repository a project is attached to. This is the data-repo / data-storage
half of attaching governance; the gov-repo registration (writing \code{ref.json}
and \code{dispatch.json}, committing to the gov repo) is performed separately by
the governance layer (\code{datomanager::gov_attach()}).
}
\details{
\code{governance.json} is the canonical data->gov pointer in the bidirectional
governance link: the gov repo's \code{ref.json} points gov->data, and this file
points data->gov, so either repo can find the other. It is written to two
locations, mirroring the manifest pattern (git canonical, storage derived):
\itemize{
\item \code{.datom/governance.json} in the local data clone (git canonical), committed
and pushed to the data repo.
\item \code{{prefix}/datom/.metadata/governance.json} in data storage (derived mirror;
a failed mirror write warns but does not abort -- the git copy is canonical
and readers with gov access resolve location from the gov repo).
}

Routing this write through datom upholds the two-repos invariant: the
governance layer never mutates the data repo directly.
}
\examples{
\dontrun{
# Data-side half of governance attachment (gov-repo registration is
# performed by datomanager::gov_attach()).
datom_repo_attach_governance(conn, gov_repo_url, gov_store)
}
}
\seealso{
\code{\link[=datom_repo_delete]{datom_repo_delete()}}, \code{\link[=datom_repo_set_data_store]{datom_repo_set_data_store()}}
}
