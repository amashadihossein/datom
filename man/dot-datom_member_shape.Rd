% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/member.R
\name{.datom_member_shape}
\alias{.datom_member_shape}
\title{Which of the Three Shapes a Member Argument Arrived In}
\usage{
.datom_member_shape(member, arg = "member")
}
\arguments{
\item{member}{The value the caller passed.}

\item{arg}{Argument name for the message.}
}
\value{
A list of \code{shape} (a phrase naming what arrived) and \code{record} (the
member record, or \code{NULL} when a name arrived).
}
\description{
A caller naming one member holds one of three things, and every verb that
takes a member accepts all three: a \strong{name}, a \strong{member record} (what
\code{\link[=datom_member]{datom_member()}} returns, and what stripping a read set's links produces), or
a \strong{link} (a member's \code{fetch} element, or a leaf of
\code{\link[=datom_structure_members]{datom_structure_members()}}).
}
\details{
This is the shape dispatch alone, deliberately without the lookup. What a
\strong{name} means differs by verb -- to \code{\link[=datom_fetch_member]{datom_fetch_member()}} it is a member of
the set already in hand, to \code{\link[=datom_add_member]{datom_add_member()}} it is an artifact to look up
in the project's storage -- so handing the name back to the caller is what lets
one dispatch serve both without either searching the wrong thing.

The refusal of \code{tags} / \code{version} beside a record or a link is left to each
caller too: both refuse, and the reason differs enough to word differently
(narrowing a search versus declaring a member twice). \code{shape} is the phrase to
name it by, so the two messages at least agree on what the caller passed.
}
\keyword{internal}
