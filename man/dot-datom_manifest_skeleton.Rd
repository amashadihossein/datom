% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sync.R
\name{.datom_manifest_skeleton}
\alias{.datom_manifest_skeleton}
\title{Empty Manifest Skeleton}
\usage{
.datom_manifest_skeleton(project_name = NULL)
}
\arguments{
\item{project_name}{Project name, or \code{NULL} to omit the field (callers that
only need somewhere to look up entries have no project name to hand).}
}
\value{
A list with \code{schema_version}, \code{project_name} (when supplied),
\code{artifacts} and \code{summary}.
}
\description{
The one shape of an empty manifest. Callers that need a manifest when none
exists yet build it here rather than inline, so a later change to the
manifest's shape has a single place to land.
}
\details{
\code{artifacts} is a \strong{named} empty list on purpose: \code{jsonlite} serializes an
empty bare list as a JSON array (\verb{[]}) and an empty named list as an object
(\code{{}}), and a manifest's artifact block must be an object. Inert today, since
nothing writes a manifest that still has zero entries, and correct for the one
case where it would.

The skeleton declares \code{schema_version} itself, so no repo ever exists in a
state that declares no format at all -- not even between being created and
receiving its first artifact. This covers only the built-from-nothing path:
a document read from disk in an older shape gets its version from
\code{\link[=.datom_manifest_upgrade]{.datom_manifest_upgrade()}} instead, because the skeleton is unreachable
whenever a manifest file exists.
}
\keyword{internal}
