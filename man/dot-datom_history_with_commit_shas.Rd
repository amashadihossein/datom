% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/version-commit.R
\name{.datom_history_with_commit_shas}
\alias{.datom_history_with_commit_shas}
\title{Add \code{commit_sha} to a History on Its Way to Storage}
\usage{
.datom_history_with_commit_shas(
  conn,
  name,
  history,
  version = NULL,
  commit_sha = NULL
)
}
\arguments{
\item{conn}{A \code{datom_conn} object with a local path.}

\item{name}{Artifact name, of either kind -- \code{version_history.json} is shared.}

\item{history}{The clone's parsed history, newest-first, as a list of entries.}

\item{version}{The version this write produced, or \code{NULL}.}

\item{commit_sha}{The commit that produced \code{version}, or \code{NULL}. A caller that
made no commit passes \code{NULL} and every entry is derived.}
}
\value{
\code{history} with \code{commit_sha} filled in where it is known.
}
\description{
Returns \code{history} with a \code{commit_sha} on every entry whose producing commit is
known, and unchanged entries where it is not. Called by each of the three
functions that upload \code{version_history.json}.
}
\details{
Two sources, in this order:
\enumerate{
\item \strong{What storage already holds.} Cheap, and it is the only source for a
value git can no longer produce.
\item \strong{Derived from git}, for the entries still missing after step 1 -- and
only then, so a repo whose history is complete pays no git walk.
}

\code{version} / \code{commit_sha} are the write path's shortcut: the caller has just
made the commit that produced that version, so the walk is not needed for it.
They are ignored when storage already records a commit for that version, since
the recorded value is the \strong{first} commit that introduced it and a later
re-upload must not repoint it.

\strong{When step 1 failed rather than found nothing, and something was lost by it,
this says so.} The two states are not interchangeable: nothing to merge is the
ordinary first write, whereas a stored copy that would not read means the
values only storage had are now unknown, and the upload below replaces the file
wholesale. The warning is raised only when a version actually ends up with no
commit -- if git could attribute every one of them, the same values were
reconstructed and nothing is degraded.
}
\keyword{internal}
