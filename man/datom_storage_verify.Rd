% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/storage.R
\name{datom_storage_verify}
\alias{datom_storage_verify}
\title{Verify a Copy Between Two datom Storage Namespaces}
\usage{
datom_storage_verify(
  from_conn,
  to_conn,
  keys = NULL,
  mode = c("structural", "content")
)
}
\arguments{
\item{from_conn}{A \code{datom_conn} object (source / reference).}

\item{to_conn}{A \code{datom_conn} object (destination to verify).}

\item{keys}{Character vector of relative keys (after \verb{\{prefix\}/datom/}) to
verify. \code{NULL} (default) verifies every key returned by
\code{datom_storage_list(from_conn)}. Pass a subset to verify a sample.}

\item{mode}{\code{"structural"} (default) or \code{"content"}. See above.}
}
\value{
A data frame with columns:
\itemize{
\item \code{key} (character): relative storage key.
\item \code{ok} (logical): \code{TRUE} if the object passed verification.
\item \code{issue} (character): description of the mismatch, or \code{NA} if \code{ok}.
Returns a zero-row data frame if \code{keys} is empty.
}
}
\description{
Checks that objects in \code{to_conn}'s datom namespace match their counterparts
in \code{from_conn}. Two verification modes are available:
}
\details{
\itemize{
\item \strong{\code{"structural"} (default)}: Confirms each destination object exists and
its byte size matches the source. Fast -- one \code{HEAD}/stat per object, no
byte transfer. Catches truncated or missing objects, which is the dominant
copy failure mode.
\item \strong{\code{"content"}}: Re-reads destination bytes, recomputes the SHA-256 hash,
and compares against the source hash. Expensive (full re-download for
remote backends) but gives true bit-level integrity. Use for regulated or
paranoid runs.
}
}
\examples{
# Offline, self-contained: a bare git repo stands in for GitHub and two
# local directories for the source and destination object stores.
if (requireNamespace("git2r", quietly = TRUE)) {
  tmp <- tempfile("datom-example-")
  remote <- file.path(tmp, "remote.git")
  dir.create(remote, recursive = TRUE)
  git2r::init(remote, bare = TRUE)

  store <- datom_store(
    data = datom_store_local(file.path(tmp, "storage")),
    github_pat = "example-token", # role selector; a local remote needs none
    data_repo_url = remote,
    validate = FALSE
  )
  datom_init_repo(file.path(tmp, "repo"), "example_project", store)
  from_conn <- datom_get_conn(file.path(tmp, "repo"), store)
  datom_write(from_conn, data = datom_example_data("dm"), name = "dm")

  to_store <- datom_store(data = datom_store_local(file.path(tmp, "storage2")))
  to_conn <- datom_get_conn(store = to_store, project_name = "example_project")
  copied <- datom_storage_copy(from_conn, to_conn)

  # Verify all copied objects structurally (default, fast)
  results <- datom_storage_verify(from_conn, to_conn)
  all(results$ok)

  # Verify a subset with full content hash
  datom_storage_verify(from_conn, to_conn,
                       keys = copied$key[1],
                       mode = "content")

  unlink(tmp, recursive = TRUE)
}
}
\seealso{
\code{\link[=datom_storage_copy]{datom_storage_copy()}}, \code{\link[=datom_storage_list]{datom_storage_list()}}
}
