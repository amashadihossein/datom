% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set.R
\name{.datom_check_set_payload}
\alias{.datom_check_set_payload}
\title{Refuse a Payload Only a Whole-Payload View Can Judge}
\usage{
.datom_check_set_payload(payload, name, project)
}
\arguments{
\item{payload}{A tidied, validated, ordered payload.}

\item{name}{The set's own name.}

\item{project}{The set's own project.}
}
\value{
Invisibly \code{TRUE}.
}
\description{
Three refusals that \code{.datom_validate_members()} deliberately cannot make,
because each needs the whole payload rather than one member:
}
\details{
\itemize{
\item \strong{Zero members.} An empty citable product has no content to identify, and
the recourse is simply to write the set once its first output exists.
\item \strong{The same \code{id} listed twice with different \code{tags}.} Deduplication does not
catch this -- the digest covers tags, so both entries survive -- and the
payload then holds one member twice with conflicting labels, which a consumer
projecting tags into a folder view sees as one artifact in two places.
Refused rather than tidied because both ways to tidy it guess: merging the
tags is right if the caller meant both categories and nonsense if two code
paths disagreed, and picking one entry is arbitrary.
\item \strong{Self-reference.} A set listing itself, at any version, is refused.
}

\strong{The same \code{project} and \code{name} at two different \code{version}s is legal and must
stay legal}: a product carrying a current table beside a locked baseline is
atypical and entirely sensible. So the duplicate check keys on the \strong{full}
\code{id}, never on \code{project} + \code{name} -- which is the tightening that looks natural
and would break that use silently.

\strong{Self-reference is a nonsense check, not cycle detection.} Cycles are
structurally impossible: a member pins a version that already exists, so a set
cannot reference anything containing it. Nothing here may grow into a visited
set or a depth limit.
}
\keyword{internal}
