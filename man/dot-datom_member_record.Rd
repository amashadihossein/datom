% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set-members.R
\name{.datom_member_record}
\alias{.datom_member_record}
\title{Resolve the Third Argument of \code{\link[=datom_fetch_member]{datom_fetch_member()}} to a Member Record}
\usage{
.datom_member_record(members, member, tags = NULL, version = NULL)
}
\arguments{
\item{members}{The set's member list.}

\item{member}{A name, a member record, or a \code{datom_link}.}

\item{tags}{Optional label filter.}

\item{version}{Optional version, or a prefix of one.}
}
\value{
One member record.
}
\description{
One accessor for the three shapes a caller holds, so a console call and a loop
use the same verb: a \strong{name}, a \strong{member record}, or a \strong{link}.
}
\details{
The shape dispatch itself is \code{\link[=.datom_member_shape]{.datom_member_shape()}}, shared with
\code{\link[=datom_add_member]{datom_add_member()}}. Only the \strong{name} half is here, and it genuinely differs
between the two verbs: a name means "a member of this set" here and "an
artifact in this project's storage" there, so a shared lookup would search the
wrong thing on one of the two routes.

A record with no \code{fetch} on it is accepted, and that matters: it is the payload
shape -- what a caller who built a member with \code{\link[=datom_member]{datom_member()}} holds, and what
stripping a read set's links produces. The accessor keys on \code{id} and nothing
else, which is what keeps this verb and \code{\link[=datom_write_set]{datom_write_set()}} agreeing about what
a member is.

\code{tags} and \code{version} narrow a \strong{name}. Supplied beside a record or a link they
are refused rather than ignored, because ignoring them would resolve a
different version than the one asked for and report success.
}
\keyword{internal}
