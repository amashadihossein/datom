% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/repo.R
\name{.datom_check_git_remote}
\alias{.datom_check_git_remote}
\title{Refuse a Repo With No Remote, Before git2r Does It Unhelpfully}
\usage{
.datom_check_git_remote(path, verb)
}
\arguments{
\item{path}{Repository path.}

\item{verb}{Name of the calling verb, for the message.}
}
\value{
The remote name.
}
\description{
\code{.datom_git_push()} reads \code{git2r::remotes(repo)[[1L]]}, which subscripts an
empty list on a repo with no remote and fails with R's own out-of-bounds
error -- a message naming nothing the caller can act on. A data repo is
required to have a remote, so this is an edge rather than a scenario, but
\code{datom_repo_push()} is the first thing somebody points at a half-configured
repo.
}
\details{
Deliberately \strong{not} inside \code{.datom_git_push()}: putting it there would change
what four existing callers do on a repo they have never met in that state.
Both new verbs call it, so there is no second copy.
}
\keyword{internal}
