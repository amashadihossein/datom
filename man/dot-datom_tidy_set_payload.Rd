% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set.R
\name{.datom_tidy_set_payload}
\alias{.datom_tidy_set_payload}
\title{Tidy a Set Payload}
\usage{
.datom_tidy_set_payload(payload)
}
\arguments{
\item{payload}{A list with \code{members} and optional set-level \code{tags}.}
}
\value{
The tidied payload.
}
\description{
The silent half of canonicalization: every spelling that states the same fact
is reduced to one, and nothing here is an error. Covers the set-level tag map,
each member's \code{id} key order, and each member's tag map.
}
\details{
Member order and member deduplication are \strong{not} here, because they need the
identity encoder and so can only run once validation has established that every
value is encodable -- see \code{\link[=.datom_order_set_members]{.datom_order_set_members()}}.

An empty tag map has its key \strong{removed} rather than set to \code{NULL}, at both
levels. \code{jsonlite} writes a NULL element as \code{{}}, and \code{"tags": {}} is the one
spelling a writer must never emit: the hash cannot tell it from an absent map,
so nothing would fail, and the stored file would carry an empty object in every
untagged member forever.
}
\keyword{internal}
