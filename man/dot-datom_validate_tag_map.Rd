% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/member.R
\name{.datom_validate_tag_map}
\alias{.datom_validate_tag_map}
\title{Validate a Tag Map}
\usage{
.datom_validate_tag_map(tags, what = "tags", remedy = NULL)
}
\arguments{
\item{tags}{A named list, or \code{NULL} (no tags).}

\item{what}{Label used in error messages, e.g. \code{"tags"} or
\code{"members[[2]]$tags"}.}

\item{remedy}{Optional \code{cli} bullet appended to every abort.}
}
\value{
Invisibly \code{TRUE}.
}
\description{
The tag grammar, in one place, shared by \code{\link[=datom_member]{datom_member()}} (its \code{tags}
argument), the member validator (each member's \code{tags}), and the set write
(set-level \code{tags}). A tag map is a named list whose values are UTF-8 strings
or arrays of them -- no numbers, booleans, \code{null}, or nesting.
}
\details{
Per-value type checking delegates to \code{.datom_sv1_as_strings()}, the same
coercion the hash encoder uses, rather than restating its rules. That is
deliberate: two copies of "what counts as text here" would eventually
disagree, and the encoder's messages already name the offending key and the
allowed types. What this function adds on top is the \strong{empty-label} refusal,
which the encoder does not make -- there, \code{""} hashes as an ordinary label.

An empty value is \strong{not} refused, because it is a tidy case rather than an
error: call \code{\link[=.datom_drop_empty_tags]{.datom_drop_empty_tags()}} first, which every caller does.
}
\keyword{internal}
