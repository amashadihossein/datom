% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decommission.R
\name{datom_decommission}
\alias{datom_decommission}
\title{Decommission a datom Project}
\usage{
datom_decommission(conn, confirm = NULL)
}
\arguments{
\item{conn}{A \code{datom_conn} object (developer role required).}

\item{confirm}{Character string.  Must equal \code{conn$project_name} exactly.
No interactive prompts -- this must be supplied explicitly to prevent
accidental decommissioning in scripts.}
}
\value{
Invisible \code{TRUE} on success.
}
\description{
Permanently removes all storage, git, and governance artefacts for a
project.  This is irreversible -- data is deleted from storage and the
project is unregistered from the shared governance repo.
}
\details{
Teardown order (each step is warn-and-continue on failure so the remaining
steps still run):
\enumerate{
\item Delete all objects under the data storage namespace.
\item Delete the data GitHub repo (requires \code{gh} CLI; skipped with a warning
if unavailable or if the local clone has no GitHub remote).
\item Remove the local data clone directory (\code{conn$path}).
\item Unregister the project from the governance repo (git commit + push).
Skipped with a warning when \code{conn$gov_local_path} is \code{NULL}.
\item Delete the project folder from governance storage
(\verb{projects/\{project_name\}/}). Skipped when there is no governance client.
}
}
