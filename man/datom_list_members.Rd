% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set-members.R
\name{datom_list_members}
\alias{datom_list_members}
\title{List a set's members and their labels}
\usage{
datom_list_members(x)
}
\arguments{
\item{x}{A \code{datom_set} from \code{\link[=datom_get_set]{datom_get_set()}}.}
}
\value{
A data frame of \code{name}, \code{project}, \code{version}, \code{kind}, \code{key}, \code{value}.
Zero rows, with those columns, for a set with no members.
}
\description{
A data frame with one row per member \strong{per label value} -- long format, not
wide. Tags are open-keyed and multi-valued, so a wide frame would need a
list-column and a column set that changes from one set to the next; long is a
plain frame with fixed columns whatever the set holds.
}
\details{
Filtering is therefore ordinary R -- \code{subset()}, \code{dplyr::filter()} -- and datom
grows no query vocabulary of its own.
}
\section{The columns}{

\code{name}, \code{project}, \code{version} and \code{kind} identify the member; \code{key} and \code{value}
are one label. \strong{An untagged member still gets a row}, with \code{NA} for both, so
\code{unique(m$name)} is the complete member list rather than the tagged part of it.

\code{value} is a plain character column, never a list-column, because the tag
grammar is text only.
}

\examples{
# These two facts about a set -- its members and their labels -- are all this
# verb reads, so a set built by hand shows the shape. In practice `x` comes
# from datom_get_set().
x <- structure(
  list(
    name = "study001-adam", project = "study001", version = NULL,
    data_sha = NULL, tags = list(description = "ADaM datasets"),
    members = list(
      list(
        id = list(project = "study001", name = "adsl", kind = "table",
                  version = strrep("a", 64)),
        tags = list(type = "output", domain = c("safety", "efficacy"))
      ),
      list(
        id = list(project = "study001", name = "dm", kind = "table",
                  version = strrep("b", 64))
      )
    )
  ),
  class = "datom_set"
)

# adsl appears three times, once per label; the untagged dm appears once.
datom_list_members(x)

# Filtering is plain R.
subset(datom_list_members(x), key == "domain" & value == "safety")
}
\seealso{
\code{\link[=datom_structure_members]{datom_structure_members()}} for a navigable view of the same labels,
\code{\link[=datom_fetch_member]{datom_fetch_member()}} to resolve one member.
}
