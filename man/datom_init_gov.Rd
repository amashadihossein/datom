% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conn.R
\name{datom_init_gov}
\alias{datom_init_gov}
\title{Initialize a Governance Repository}
\usage{
datom_init_gov(
  gov_store,
  gov_repo_url = NULL,
  gov_local_path = NULL,
  create_repo = FALSE,
  repo_name = NULL,
  github_pat = NULL,
  github_org = NULL,
  private = TRUE
)
}
\arguments{
\item{gov_store}{A governance store component (\code{datom_store_s3} or
\code{datom_store_local}).  This is the storage backend that individual data
projects will use to register their dispatch and ref files.}

\item{gov_repo_url}{GitHub URL of the governance repo.  Mutually exclusive
with \code{create_repo = TRUE}.}

\item{gov_local_path}{Local path for the governance clone.  When \code{NULL},
derived from the basename of \code{gov_repo_url} (\code{.git} suffix stripped) in
the current directory.  When \code{create_repo = TRUE} and no URL is known
yet, set this explicitly or let it default to \code{repo_name}.}

\item{create_repo}{If \code{TRUE}, create a GitHub repo via the API and use the
returned URL.  Mutually exclusive with providing \code{gov_repo_url}.}

\item{repo_name}{GitHub repo name when \code{create_repo = TRUE}.  Required
when \code{create_repo = TRUE}.}

\item{github_pat}{GitHub personal access token.  Required when
\code{create_repo = TRUE}.}

\item{github_org}{GitHub organisation slug.  \code{NULL} creates the repo under
the authenticated user account.}

\item{private}{Whether the created repo should be private.  Default \code{TRUE}.
Ignored when \code{create_repo = FALSE}.}
}
\value{
Invisible \code{gov_repo_url} on success.
}
\description{
One-time setup to create a shared governance repository that serves many
data projects in an organisation. Creates the GitHub repo (optionally),
seeds the skeleton (\code{README.md} + \code{projects/.gitkeep}), commits, and
pushes.
}
\details{
Idempotent: if \code{gov_local_path} already contains an initialised governance
clone (i.e. \code{projects/.gitkeep} exists), the function returns silently
without making any changes.
}
