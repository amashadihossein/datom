% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set-draft.R
\name{print.datom_set_draft}
\alias{print.datom_set_draft}
\title{Print a draft set}
\usage{
\method{print}{datom_set_draft}(x, ..., n = 20L)
}
\arguments{
\item{x}{A \code{datom_set_draft} from \code{\link[=datom_assemble_set]{datom_assemble_set()}}.}

\item{...}{Ignored.}

\item{n}{Maximum number of members to list.}
}
\value{
Invisible \code{x}.
}
\description{
What is assembled so far, and that it is not written yet. One line per member
-- name, kind, and its labels as compact \code{key=value} pairs -- which is what
makes a pipe inspectable mid-build.
}
\details{
\strong{It names every field it shows and never prints the connection.} A
connection may carry a credential, and a \code{datom_conn} prints an allowlist of
named fields for exactly that reason: a secret added to a connection later
cannot leak through a method that never iterates it.
}
\examples{
# See datom_assemble_set() for a runnable example that prints a draft.
print(names(formals(datom_assemble_set)))
}
