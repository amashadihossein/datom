% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set-edit.R
\name{.datom_select_members}
\alias{.datom_select_members}
\title{Which Members Does This Call Refer To?}
\usage{
.datom_select_members(
  members,
  member = NULL,
  tags = NULL,
  version = NULL,
  version_arg = "version"
)
}
\arguments{
\item{members}{The set's member list.}

\item{member}{A name, a member record, a \code{datom_link}, or \code{NULL} for all.}

\item{tags}{Optional label filter.}

\item{version}{Optional version, or a prefix of one.}

\item{version_arg}{The calling verb's name for \code{version}, for messages.}
}
\value{
An integer vector of positions in \code{members}, never empty.
}
\description{
The plural selector both edit verbs share. \code{\link[=.datom_find_member]{.datom_find_member()}} resolves
exactly \strong{one} member and aborts on an ambiguous name, which is right for a
fetch and only half of what an edit needs: an edit legitimately acts on many.
}
\details{
Three routes, and the difference between them is how many members they can
return:\tabular{ll}{
   What arrives \tab What comes back \cr
   no \code{member} \tab every member, narrowed by \code{tags} and \code{version} \cr
   a name \tab exactly one, aborting when the name is ambiguous \cr
   a record or a link \tab exactly the member carrying that \code{id} \cr
}


\strong{An explicitly named member that is ambiguous aborts}, and that is not in
tension with the caller who sweeps: a sweep can honour "refresh everything"
while skipping a name it cannot choose between, whereas a caller who named one
member asked for something that cannot be done, so it is a user error and the
narrowing arguments are what resolve it. The abort comes from
\code{\link[=.datom_find_member]{.datom_find_member()}} rather than from a second copy of that message.

\code{tags} and \code{version} narrow a \strong{name} or a sweep. Supplied beside a record or
a link they are refused rather than ignored, because ignoring them would act
on a different member than the one asked for and report success.
}
\keyword{internal}
