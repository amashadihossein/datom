% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/storage.R
\name{datom_storage_delete_prefix}
\alias{datom_storage_delete_prefix}
\title{Delete All Objects Under a datom Storage Prefix}
\usage{
datom_storage_delete_prefix(conn, prefix_key = NULL)
}
\arguments{
\item{conn}{A \code{datom_conn} object.}

\item{prefix_key}{Relative prefix to delete under (after
\verb{\{prefix\}/datom/}). \code{NULL} (default) deletes the entire datom namespace
root for this connection.}
}
\value{
Invisibly, a backend-specific value. For S3: the count of deleted
objects (0L if nothing found). For the local backend: \code{1L} if the prefix
directory existed and was removed, \code{0L} otherwise.
}
\description{
Removes every file under \code{{prefix}/datom/{prefix_key}} from storage.
Pass \code{prefix_key = NULL} (the default) to delete the entire datom
namespace for this connection. A missing or empty prefix is a no-op.
}
\details{
\strong{Irreversible.} Intended for package developers building tools on top of
datom (e.g. datomanager for rollback or source deletion after migration).
End users performing a full project teardown should use
\code{\link[=datom_repo_delete]{datom_repo_delete()}} instead.
}
\examples{
\dontrun{
# Delete a single table's objects
datom_storage_delete_prefix(conn, prefix_key = "demographics")

# Delete the entire datom namespace (use with care)
datom_storage_delete_prefix(conn)
}
}
