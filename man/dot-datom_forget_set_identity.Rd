% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set-edit.R
\name{.datom_forget_set_identity}
\alias{.datom_forget_set_identity}
\title{Forget the Version an Edited Set Was Read As}
\usage{
.datom_forget_set_identity(x)
}
\arguments{
\item{x}{The edited \code{datom_set} or \code{datom_set_draft}.}
}
\value{
\code{x}, with \code{version} and \code{data_sha} emptied when it had them.
}
\description{
\code{datom_get_set()} fills \code{version} and \code{data_sha} from the payload it read.
Once a member moves, those two describe a payload that no longer exists -- and
a set exists to be cited, so a stale version is a wrong statement rather than
a missing one. Left in place when nothing moved: there the object still
describes exactly the stored version, and dropping a true fact would cost the
common "refresh found nothing" case its citability for no reason.
}
\details{
\strong{Spelled \code{x["f"] <- list(NULL)}, never \code{x$f <- NULL}}, which would REMOVE the
element and change \code{names(x)}. A read set may legitimately report a \code{NULL}
version, so the field exists and is empty rather than being absent.

A draft has neither field, so this is a no-op on one.
}
\keyword{internal}
