% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest-rebuild.R
\name{.datom_recorded_current_version}
\alias{.datom_recorded_current_version}
\title{The Version Storage Recorded for an Artifact's Current State}
\usage{
.datom_recorded_current_version(meta, history)
}
\arguments{
\item{meta}{The artifact's parsed \code{metadata.json}.}

\item{history}{The artifact's parsed \code{version_history.json}, a list of
entries, or \code{NULL}.}
}
\value{
The recorded version string, or \code{NULL} when the history records
nothing usable -- in which case the rebuilt row simply carries no version,
rather than a manufactured one.
}
\description{
Picks the \code{version_history.json} entry that describes \code{metadata.json}, and
returns the \code{version} \strong{recorded} on it.
}
\details{
\strong{Never recomputed, and that is the point of this function existing at all.}
Hashing \code{metadata.json} here would reach for the identity code in precisely
the scenario a rebuild is for -- a repo touched by a build whose field
classification differs from this one's -- and publish a \code{current_version}
matching no version in the recorded history. An index pointing at a version
that does not exist is worse than the empty list it replaced.

Which entry describes the current state is not simply the newest one. History
is prepended newest-first, but a write that reverts to content already in the
history appends nothing, so the current state can be an older entry. The
selection therefore narrows by recorded fields only:
\enumerate{
\item Entries whose \code{data_sha} equals the current document's. One match settles
it -- this is the revert case, and it is why the newest entry alone is
wrong.
\item Several matches means metadata-only versions of the same content; the one
whose \code{timestamp} equals the document's \code{created_at} is the current one,
since a version's history entry copies that field verbatim.
\item Anything still ambiguous falls back to the newest candidate, and a document
matching no entry at all to the newest entry overall.
}
}
\keyword{internal}
