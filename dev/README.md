# datom Development Hub

## Workflow model (read first): spec = phase

A unit of multi-step work is a **Kiro spec** under `.kiro/specs/{feature}/`:
`requirements.md` (goal + acceptance), `design.md` (context, invariants, correctness
properties), `tasks.md` (the chunk breakdown + status). **The spec replaces the legacy
`dev/phase_{n}_{name}.md` phase doc.**

Everything in this document still applies — just remap the terms:

| Legacy term (used below) | Now |
|---|---|
| phase doc `dev/phase_*.md` | the spec `.kiro/specs/{feature}/` |
| Chunks table / Progress Log | `tasks.md` (checkboxes) + commit history |
| Active Phases | Active Specs |
| "delete the phase doc" on completion | **specs persist — never deleted** |

A "chunk" = one task (or a small related group) in `tasks.md` = one commit. All branch,
test-before-commit, chunk-checkpoint, and review discipline below is unchanged. Works the
same in Kiro (native specs) and Copilot (read/maintain the same `.kiro/specs/` files).

## Documentation Hierarchy

This folder contains all development documentation following a hierarchical chain:

```
.github/copilot-instructions.md     ← Entry point for AI/developers
         ↓
dev/README.md                       ← This file: navigation hub
         ↓
dev/datom_specification.md           ← Design spec (authoritative, evolves slowly)
dev/datom_pathways.md               ← Canonical route map across metadata/gov/storage/access
dev/daapr_architecture.md           ← Ecosystem context
dev/datomanager_scope.md             ← datomanager companion package scope (gov lifecycle + migration)
dev/datomanager_overview.md          ← datomanager access enforcement design (roles, grants, IAM; forward-looking)
         ↓
.kiro/specs/{feature}/              ← Active work: requirements.md, design.md, tasks.md
```

## Documentation Lifecycle

### Units of Work (Specs)

A unit of work is a **Kiro spec** under `.kiro/specs/{feature}/` (see Workflow model at top):

1. **Created** when starting a feature (requirements → design → tasks)
2. **Updated continuously** as development proceeds (task status in `tasks.md`)
3. **Completed** when all tasks are checked off and acceptance criteria met
4. **Persists**: the spec stays as durable documentation; additional durable learnings → spec/architecture docs and `dev/engineering-notes.md`

### What Goes Where

| Content Type | Location | Lifecycle |
|--------------|----------|-----------|
| Coding style, conventions | `.github/copilot-instructions.md` | Permanent |
| Architecture, API design | `dev/datom_specification.md` | Permanent, evolves |
| Canonical lookup/traversal routes | `dev/datom_pathways.md` | Permanent, evolves with schema/routing changes |
| Ecosystem context | `dev/daapr_architecture.md` | Permanent |
| Current work, tasks, decisions | `.kiro/specs/{feature}/` | Persists |
| Implementation gotchas discovered | `dev/engineering-notes.md` | Persists |

## Branching During CRAN Submission

> **STATUS 2026-09-08 -- 0.1.1 IS RELEASED; 0.1.2 IS SUBMITTED AND AWAITING CRAN.** So the freeze
> still holds, for the same reason it always did: if CRAN asks for another fix, `main` must reflect
> exactly what they received. Verified state, so nobody has to re-derive it: `main` is at `cb60007`,
> which is the commit submitted as 0.1.2; `dev` (`17a84ce`, in the `../datom-cran-depends` worktree) is
> level with it; **`spec/datom-sets` is now AHEAD of both** -- it took the 0.1.2 fix via
> `main -> dev -> spec/datom-sets` and has since accumulated Task 6's work, so "level with `main`" is
> no longer true of it and a cold reader should take its head from `git log` rather than from this
> line, which only claims to track `main` and `dev`;
> tag `v0.1.1` exists on the remote at `1eaee26` with its GitHub release published; **no `v0.1.2` tag
> or release yet, and there must not be one until CRAN accepts** -- the tag is created by the release
> step, not by hand. `CRAN-SUBMISSION` for 0.1.2 sits untracked in the **`../datom-cran-fix`**
> worktree, which is therefore the checkout that must publish the release and **must not be removed
> before then**. The 0.1.1 acceptance ran in the documented order (publish first, then merge), and
> the ordering hazards in "`CRAN-SUBMISSION`" below are what made that necessary -- they apply
> unchanged to 0.1.2.
>
> **What 0.1.2 was.** CRAN's check farm reported `tests` ERROR on four Linux flavors after accepting
> 0.1.1: test fixtures hardcoded `refs/heads/master` when pushing to a throwaway remote, and
> `git2r::init()` honours git's `init.defaultBranch`, so on a machine configured for any other name
> the fixture pushed a branch that had never been created. 26 failures, all the same error, all
> during setup. Test-only -- no `R/` file changed. Issues
> [#106](https://github.com/amashadihossein/datom/issues/106) and
> [#108](https://github.com/amashadihossein/datom/issues/108), PRs #107, #109, #110. **The step 3
> route below (fix on `main`, rebuild, resubmit the same version) did NOT apply**, because a released
> version cannot be resubmitted -- it needed a patch bump, which is now the precedent for any
> post-acceptance defect.
>
> **Three tools silently misbehave in a git worktree, where `.git` is a FILE rather than a
> directory.** All three bit during the 0.1.2 fix and all three fail without saying anything, so
> check for them rather than trusting a green run: `R CMD build` sweeps `.git` into the tarball
> (fixed -- `^\.git$` is now in `.Rbuildignore`); `devtools::submit_cran()` skips writing
> `CRAN-SUBMISSION` entirely, because `devtools:::uses_git()` is a directory test, and it also
> swallows its own "don't forget to tag this release" reminder (the 0.1.2 record in
> `../datom-cran-fix` was reconstructed by hand from the built tarball's timestamp and `main`'s head);
> and the `.gitignore` rule for `CRAN-SUBMISSION` exists **only on `spec/datom-sets`**, so on a
> `main` checkout the artifact is untracked and unignored -- never `git add .` there. See
> `dev/engineering-notes.md`.
>
> **OWNER INTENT, stated 2026-09-01: `dev` becomes PERMANENT.** The target pattern is
> `feature -> dev -> main`, with `dev` as the testing ground / alpha source rather than a
> submission-freeze device. So **"delete the `dev` branch" in Acceptance step 4 is ON HOLD** -- do not
> run it. The rest of this section still describes how things work today and stays until the new
> pattern is designed and written down (release cadence, what `main` means when it is no longer the
> only long-lived branch, and where pkgdown deploys from are the parts that need deciding, not just
> the branch names). **Currently on `dev` and not on `main`**: the same documentation-only commits as
> before -- a derived-columns section in `vignettes/design-version-shas.Rmd` (the only one that ships
> in the package), this branching section itself, a 3-line specification correction, and a 1-line
> conventions edit -- plus the 0.1.2 bookkeeping in this block. All are contained in
> `spec/datom-sets` as well, so nothing is at risk of being lost while the decision waits.
>
> **`main` is protected and takes only PRs**, enforced for admins, with `ubuntu-latest (release)` and
> `pkgdown` as required status checks (strict). This was verified rather than assumed during the
> 0.1.2 fix, so a "commit the fix directly on `main`" reading of step 3 below is not available in
> practice -- use a short-lived branch and a PR. A related permission limit worth knowing before
> planning any CI change: pushing anything under `.github/workflows/` needs a token scope that also
> grants the ability to rewrite CI, and CI runs with repository secrets, so that scope was
> deliberately **not** acquired. Workflow edits go through GitHub's web editor, which authenticates
> with the browser session instead (that is how #109 landed).

When a version has been submitted to CRAN and is awaiting acceptance, we freeze
`main` and develop on a long-lived `dev` branch.

### Why

If CRAN requests a surgical fix, we need `main` to reflect exactly what was
submitted so the fix can be applied cleanly and resubmitted without dragging in
unrelated work. Meanwhile, feature development continues on `dev` as the
single aggregation point.

### Branch roles

| Branch | Purpose | Who merges into it |
|--------|---------|-------------------|
| `main` | Frozen at the submitted state. pkgdown deploys from here. `install_github()` installs from here. | Only: (a) CRAN-requested fixes, or (b) `dev` merge after acceptance. |
| `dev` | Aggregator for ongoing work while submission is in flight. | Feature branches PR into `dev`. |
| `issue-{N}-*` / `spec/*` | Feature branches, same as before. | PR into **`dev`** (not `main`) during submission freeze. |

### Workflow

1. **Normal development (no pending submission):** feature branches off `main`,
   PR into `main`. The `dev` branch does not exist or is deleted.

2. **Submission pending:**
   - `dev` is created off `main` at the submitted commit.
   - Feature branches branch off `dev` and PR into `dev`.
   - `main` is not touched unless CRAN requests a fix.

3. **CRAN requests a fix:**
   - Fix is committed directly on `main` (or via a short-lived branch off
     `main`).
   - Rebuild tarball from `main`, resubmit.
   - Merge the fix into `dev` so they don't diverge:
     `git checkout dev && git merge main`.

4. **Acceptance:**
   - **Publish the release FIRST, before merging** -- `usethis::use_github_release()` from a
     `main` checkout that still has the `CRAN-SUBMISSION` artifact beside it. Order matters:
     see "CRAN-SUBMISSION" below for why doing it after the merge can silently tag the wrong
     commit.
   - Merge `dev` into `main`: `git checkout main && git merge dev`.
   - ~~Delete the `dev` branch (local + remote).~~ **ON HOLD as of 2026-09-01 -- do not run this.**
     See the STATUS block at the top of this section: `dev` is becoming permanent.
   - ~~Resume normal workflow (feature branches off `main`).~~ Same hold: the target is
     `feature -> dev -> main`.

### Constraints

- **pkgdown** always deploys from `main`. Vignette previews during development
  require a local build (`pkgdown::build_site()`).
- **GitHub default branch** stays `main` — this is what `install_github()`
  resolves and what new clones check out.
- **`CRAN-SUBMISSION`** is an untracked, transient artifact -- see the next section. (An earlier
  version of this line said it "on `main` records the submitted SHA", which was never true: the
  file has never been committed on any branch.)

### `CRAN-SUBMISSION`

**What it is.** `devtools::submit_cran()` writes it next to `DESCRIPTION`, recording the version,
the submission timestamp, and the SHA of the commit that was submitted. It is a **handoff
artifact**, not a record meant to live in the repo: `usethis::use_github_release()` reads it to
populate the release notes and **deletes it on success**. The durable record of a submission is
therefore the **GitHub release and its tag**, which point at the submitted commit.

**Rules.**

- **Never committed.** It is in `.gitignore` (and `.Rbuildignore`), so no branch can sweep it into
  a commit via `git add .`. Nothing is lost by that, because it was never tracked in the first
  place and the release is the real record. Deliberate exception if one is ever wanted:
  `git add -f`.
- **Written only by `devtools::submit_cran()` run from `main`**, which is the only branch whose
  tree matches what CRAN received. Never hand-edited, never regenerated from a feature branch.
- **Never deleted by hand** while a submission is pending -- `use_github_release()` removes it as
  part of publishing.

**Two hazards, both silent.**

1. **Absent file means "HEAD is the submitted state".** usethis says so explicitly: with no
   `CRAN-SUBMISSION` present it assumes the current SHA, version, and NEWS *are* the submitted
   ones. Run `use_github_release()` after merging `dev` into `main` and the release names the
   **merge commit** rather than the commit CRAN actually received. Hence the ordering in
   Acceptance step 4: publish, then merge.
2. **The artifact lives in whichever working directory `submit_cran()` ran in**, and that is not
   necessarily where `main` is checked out later. If it is missing at release time, check other
   worktrees (`git worktree list`) before assuming it was never created -- and cross-check the SHA
   against the record below.

**Record for the pending 0.1.1 submission** (belt to the artifact's braces, since the artifact
is untracked by design and this file is not):

| Version | Submitted (UTC) | SHA | Artifact location |
|---|---|---|---|
| 0.1.1 | 2026-08-21 23:39:19 | `1eaee2660f9d4d19d0d5fec979bba4617a1bc776` (`main` head, "Declare Depends: R (>= 4.1.0) explicitly (#99)") | consumed 2026-09-07 by `use_github_release()`; durable record is tag `v0.1.1` |
| 0.1.2 | 2026-09-08 04:11:41 | `cb60007f59c5274b8f816c6422ee5ca86e0ac5f3` (`main` head, "Correct the check-results section of cran-comments for 0.1.2 (#110)") | the `../datom-cran-fix` worktree, uncommitted -- **reconstructed by hand**, see below |

Add a row here at each submission. It costs one line and it is the thing that makes the release
verifiable if the artifact goes missing.

**It went missing for 0.1.2, which is why this table now earns its keep.** `submit_cran()` ran from a
git **worktree**, and the step that writes the artifact (`devtools:::flag_release()`) begins
`if (!uses_git(pkg$path)) return(invisible())` where `uses_git()` tests for a `.git` **directory**.
In a worktree `.git` is a file, so the step exited silently -- no artifact, and no "don't forget to
tag this release" reminder either, since that message sits one line above the write in the same
function. The submission itself was unaffected; only the local bookkeeping was skipped. The 0.1.2
record was rebuilt from `main`'s head (verified unmoved and level with `origin/main`) and the built
tarball's mtime in the R temp directory, then confirmed parseable by `usethis:::get_release_data()`.
**Check for the artifact after every `submit_cran()` run from a worktree**, or run the submission
from a normal clone instead.

---

## Current Development State

### Active Specs

Units of work are **Kiro specs** under `.kiro/specs/{feature}/` (see Workflow model at top).

| Spec | Started | Status | Location |
|------|---------|--------|----------|
| datom-sets | 2026-08-09 | **TASK 16 DONE (2026-09-19) -- the acceptance sweep is finished and every criterion this release defines is now covered by a test that was watched failing. NEXT IS TASK 17**, docs plus spec completion, and it is the last task. The sweep was not a grep: of the 41 criteria, 26 named themselves nowhere in the test suite, so matching each one to a test had to be done by meaning. **The standard was evidence rather than judgement** -- a criterion counts as covered only when a named test goes red on a deliberate break of the behaviour it claims, and the record is a table of what was broken and what reddened, so the verdict can be inspected instead of taken on trust. **81 deliberate breakages across five batches.** **One real hole was found, and it is the third instance of one pattern.** The test for 're-writing an identical set does nothing' could not fail: with the guard deleted, every assertion in it stayed green, and across the whole suite exactly two other tests noticed. Four separate reasons, each enough on its own -- what the write reports is computed the same way either way, both hashes come out the same, adding a version that already exists does nothing, and an identical payload leaves git nothing to commit, **so even reading the commit id before and after cannot tell the two apart**. That last one killed the obvious fix, which I tried first. What does survive is what the caller is **told**: a skipped write says so and a completed one announces itself. The lesson, now written down twice before and confirmed a third time: a test that asks a write what it reported cannot tell a no-op from a finished write. **An instruction inside the task turned out to be wrong, and the test written for it now says so.** One case left over from an earlier task was to prove that committing with no file list sweeps datom's own half-written files into the human's commit -- accepted behaviour, not a bug -- and then that the repair verb leaves the repo consistent. The first half holds. The second cannot for a table: a write that died before uploading produced no data bytes anywhere, and git never holds data, so there is nothing to restore from. The repair puts both records back and the data gap stays, named as the only defect left. A **set** is the case where that instruction would have been right, because git does hold a set's contents. **Two of my own probes were invalid and the standard caught both**, which is the rule earning its place: a probe that reddens nothing is ambiguous rather than passing, and in both cases the thing that was wrong was the probe. A control was also run -- a comment-only edit, which reddened nothing -- because without it every red count in the table could have been noise. **Two criteria carry an honest qualification rather than a clean pass**, both stated in their own rows: one cannot have a test until a verb that does not exist yet is built, and one asserts a composition of two things pinned elsewhere, so no single break reddens it. **A new end-to-end script, `dev/e2e-sets.R`**, 51 asserted claims, fully offline, no credentials of any kind. Nothing under `dev/` touched sets before it. It walks the lifecycle a product actually has rather than just build-and-read: assemble, write with your own code and lockfile in the same commit, read it back **with no clone at all**, re-write unchanged and get nothing, then an input moves -- repoint it, drop a retired one, and check the commit message names both edits. Its exit code was verified by breaking the package, not by reading the script: it exits non-zero on the same defect the unit suite could not see. **The cost is paid once from here on.** Each criterion's number is now written into the test that covers it, and `dev/check-spec.R` gained a check that every criterion is named somewhere in the suite -- so a criterion nobody matched to anything is now a failed build rather than a judgement to re-make. It went in last, because adding it earlier would have made the repo uncommittable for the length of the task. Four criteria are exempt with their reasons recorded: one is retired, and three are project gates discharged by running something rather than by asserting it. **ONE ITEM IS DELIBERATELY LEFT OPEN FOR A FRESH SESSION.** The criteria list has to be derived a second time by a session that is shown the requirements and nothing else, then diffed against the sweep's -- because a criterion I missed and a criterion I credited to the wrong test are different mistakes, and re-reading my own work catches neither. It belongs to whoever starts Task 17. **A review after it landed added one instance to a pattern and sharpened the residual, and following that up found two more holes of a kind I had found one of by luck.** The shape: a criterion names a **mechanism** as well as an outcome, and only the outcome gets asserted -- and in a healthy repo the named mechanism and the wrong one produce the same answer, which is exactly why no test separates them and exactly why the criterion bothered to say which. So all 37 criteria were re-read for a 'how' clause; ten name one, eight were already covered, **two were not and are now fixed**. One: the check that stops a set being written over a table of the same name must read the artifact's own record in storage, never the repo index, because the index can lag behind a write that stopped halfway -- reading the index instead left all 213 tests in that file green. Two: a set's stored contents must be refused **before** anything parses them, and moving the check after the parse left every assertion green, because the same error still comes out. Both now have a test that fails on exactly those substitutions. **Recorded as the known residual**, precisely rather than as 'still needs a reader': a test that exercises only part of what a criterion claims is caught by neither the new build check nor the break-it-and-watch rule, and the independent re-derivation will not catch it either, because that checks the list of criteria and not the assertions behind them. Also recorded: four criteria whose break reddens its own test plus a great many others, so a regression there would not point at the criterion -- they are where the next hour of this work goes. Tests 4270 -> **4292**, FAIL 0 / WARN 0 / SKIP 0; `dev/check-spec.R` **10/10**; `R CMD check --as-cran` 0/0/0 with examples, tests and vignettes run. **Pathway impact: none.** **NEXT IS TASK 17, the last one, and its body now carries a measured state block rather than just its original checklist** -- because roughly half of that task turned out to be already done and the checklist did not say which half. The reference index is complete, the release notes cover everything except the two newest verbs, and the technical specification is barely started and is most of the remaining work. **No pre-start audit was run and that was a decision**: this task ships no behaviour, its two mechanical staleness risks were checked directly and are clean, and the one risk an audit would really have addressed -- old claims in the design documents that this release falsified -- can only be found by reading those documents against the code, which is the task's own work. So the audit would have produced that reading twice. **The first thing Task 17 does is not a docs pass**: it is the independent re-derivation of the criteria list left open above, because a gap it finds is a test to write and Task 17 is where 'this spec is done' gets asserted. **THE SPEC GREW BY A PHASE ON 2026-09-16: Phase H, Tasks 27 and 28, editing a set that already exists -- scheduled into this release rather than deferred.** A set is now pleasant to build and to read and **unpleasant to edit**: the only route is list surgery on what the read returned, and two of the obvious hand-rolled spellings are silently wrong. Filtering members by name drops **every** version of that name, which quietly removes a deliberately frozen baseline alongside the live table; and repointing a member by hand loses its labels, which are part of the set's content. So two verbs join this release -- **repoint members at newer versions** (the fundamental one: a product with a hundred inputs where thirty upstream tables have moved) and **drop members**. The decision was made on value alone, because unusually these verbs touch no stored file, so deferring them would have cost nothing in forced upgrades later. **They run after Task 15 and before Task 16**, which is forced rather than chosen: Task 16 is the acceptance sweep and Task 17 is docs plus spec completion, so verbs landing later would leave the sweep testing a surface that then grew and the docs describing one missing two functions. Repointing is built first, even though it is larger, because both share a member selector that does not exist yet and the harder consumer is what shapes it correctly -- and because if the release squeezes, what drops is then the less fundamental verb. Design detail is in the spec (requirement R24, Phase H, and the 2026-09-16 Decisions rows). **TASK 28 DONE (2026-09-19) -- members can now be dropped from a set, and a set is finally pleasant to edit as well as to build and read. NEXT IS TASK 16**, the acceptance sweep across everything this release added, then Task 17, docs plus spec completion. **Task 16 was re-audited for a cold start on 2026-09-19 and three signposts in it were corrected**, all three stale because of the two verbs that had just landed: it still told a fresh session it had arrived too early, it still said one criterion had no test when one now exists, and its end-to-end script still described only the build-and-read path with no mention of editing. **The sweep's own standard was settled at the same time, and it replaces the escalation flag the task used to carry** -- there is nowhere to escalate to, and the flag was buying a better judgement where the task needs evidence. So: a criterion counts as covered only when a named test **goes red on a deliberate break of the behaviour it claims to cover**, and the deliverable is a table of what was broken and what reddened, so the verdict can be inspected rather than taken on trust. The reason that matters is a number: of the 41 criteria, 26 do not name themselves anywhere in the test suite, so their coverage would otherwise rest on reading each one and matching it to a test by meaning -- about 23 judgement calls once the three procedural gates are set aside. Two follow-ons are written into the task so the cost is paid once: each criterion gets its number written into the test as it is matched, and `check-spec` then gains an assertion that every criterion is named somewhere in the suite -- added last, because adding it before the annotation pass would turn a must-pass gate red for the whole task. Afterwards a fresh session derives the criteria list from the requirements on its own and diffs it against the sweep's, because a missed criterion and a mis-credited one are different errors and re-reading your own work catches neither. Dropping a member by hand is the second silently-wrong spelling this phase exists to replace: filtering the member list by name removes **every** version of that name, so a set holding a live table beside a deliberately frozen baseline loses both, and removing by position removes a different member the day somebody adds one. **It takes no connection at all**, which is the structural difference from the two verbs beside it: dropping a member only has to find a pointer the set already holds, while adding or repointing one has to go and resolve it. So it does no reading of any kind, and a test proves that by running it on a set read with storage-only credentials and no clone -- otherwise the missing argument is only a claim in the docs. **Its default is the opposite of the refresh verb's, for the same kind of reason**: asking to drop nothing in particular would mean dropping everything, so a selection is required, while asking to refresh nothing in particular sensibly means refresh everything. Three things it refuses rather than doing quietly: a name that matches two members, a selection that matches nothing (the hand-rolled version reports success for a typo), and a selection that is every member -- the write refuses an empty set anyway, so this only moves the refusal to where you can see which selection emptied it. **A name matching two members refuses here and is skipped by the refresh verb**, which is a real asymmetry and is documented in both places: skipping a refresh leaves a valid pinned version behind, skipping a removal silently does nothing at all. **A cold review of Task 27 found a seam worth closing before this verb existed**, and closing it is most of the work: each edit verb was going to own its own record of what it did, so editing both ways before writing would have produced a commit message naming the repoints and saying nothing about the removal -- and a removal is the edit a `git log` reader most wants named. There is now one log both verbs add to, each entry saying which kind of edit it records, so a chained edit produces one message describing all of it: `repoint 1 member, drop 1 member`, with the full list in the commit body. A third editing verb would cost a new entry kind rather than a new mechanism. **Nine deliberate breakages, each reddening its own test**, one of which turns nine cases red: reading that log after the write unpacks the set rather than before. Tests 4219 -> **4270** (+51), FAIL 0 / WARN 0 / SKIP 0; `dev/check-spec.R` 9/9; `R CMD check` 0/0/0 with examples, tests and vignettes run. **Pathway impact: none** -- no lookup, no traversal, no IO of any kind. **TASK 27 DONE (2026-09-19) -- a product's inputs can now be moved forward to their newer versions in one call, and what moved is reported before anything is written. NEXT IS TASK 28**, dropping members, then the acceptance sweep, then docs and spec completion. Until now the only route was rebuilding the member list by hand, and two of the obvious spellings quietly did the wrong thing. The new call takes the set plus one connection per project its inputs live in, and moves every input to whatever its own project says is current -- or only the ones you name, or only the ones carrying a label. **Nothing is stored**: you get the edited set back, read the report, and write it when you choose, so there is no "are you sure" prompt to answer. Re-running it when nothing has moved mints no version, which is asserted through the write because that is the only place "free" is observable. **The labels come with the pointer**, which is the whole reason this is a verb rather than three lines of list surgery: rebuilding an input's pointer by hand drops its labels, and labels are part of what the set records, so its identity would move for a reason nobody asked for. The trap is worse than it looks -- the obvious implementation loses only labels whose value is empty, so every ordinary test passes. **The shortcut button on a moved input is rebuilt too**, or it would hand back the old data while the pointer claimed the new version. **What it refuses and what it merely reports are different on purpose.** No connection for a project means nobody can tell whether those inputs moved, so the whole call stops and names the project -- and the projects a set spans can be listed with no connection at all. An input that no longer exists in its project is a known answer, so it is reported and left pinned: that version still reads, and refusing a whole refresh over one retired input is the wrong trade. An input whose name appears twice in one project -- a live table beside a frozen baseline -- is skipped, with both versions named and the two ways to move one deliberately; naming such an input explicitly is an error instead, because there the request cannot be honoured as typed. **A connection labelled for one project but pointing at another's storage is caught**, by comparing the rebuilt pointer's own recorded project against the one it replaced. **Sixteen deliberate breakages, each reddening its own test, and two of them found holes rather than spare guards**: the labels test could not see the realistic wrong implementation, and there was no test at all for a manifest that cannot be read -- which was quietly reading as a project with no artifacts, so every input would have been reported as retired. **One thing it fixes in passing**: a set write used to commit "Update {name}", which says nothing in a git log, and a write that came out of this call now names what moved, with the full list in the commit body. Three calls taken at their defaults and written down: a set nothing moved in keeps the version it was read as; moving an input onto a version another member already pins is left to the write's existing refusal; and an input's kind is not re-checked. Tests 4115 -> **4219** (+104), FAIL 0 / WARN 0 / SKIP 0; `dev/check-spec.R` 9/9; `R CMD check` 0/0/0 with examples, tests and vignettes run. **Pathway impact: yes** -- the set-read card gained a note saying where "what is current" is answered, and that it costs one read per project rather than one per member. **TASK 15 DONE (2026-09-19) -- every version now records the commit that produced it, and the place that was quietly erasing it was the ordinary write, not the repair. NEXT IS TASK 27**, the first of the two set-editing verbs, then Task 28, then the acceptance sweep. Until now you could ask a repo "what versions does this table have" and get no way to find the commit behind any of them unless you held a clone. `datom_history()` now shows one. **The value is worked out, never typed in**: datom takes it from the commit it just made, or recomputes it from your git history for a version that has none, and no argument anywhere sets it. The copy in your clone does not have it and cannot -- that file is committed inside the commit that would name it -- so only the copy in storage carries it, which is the whole point: it exists for the reader with no clone. **Three places upload that file and all three would have dropped the field.** The audit's headline held up: the one that matters is the ordinary write, which sends the clone's copy whole, so the second write of a table would erase the first version's commit id with no repair involved. All three now go through one shared piece of code, in a file of its own, that keeps what storage already has and recomputes only what is missing. Recomputing is **required rather than a nicety** -- the reason it is acceptable for an old build to strip the field is that it can always be worked out again, which is only true if something works it out. Cost is one-time: an upgraded repo fills in its back history at its next write and then pays nothing, because the version being written arrives with its commit in hand. **Eleven deliberate breakages, and the eleventh found a hole rather than a spare guard.** One rule says "do not move a commit id that is already recorded", and deleting it left all nine existing tests green -- because it never fires on an ordinary write, which always mints a *new* version. The situation that reaches it is reverting a table to earlier content: the version already exists, so nothing is added to its history, but a new commit is made and handed over, and without the rule that version starts pointing at the revert instead of at where it came from. That now has a test, and the lesson is written down: build the situation that makes a guard fire, because a guard whose tests all take the common path is untested however many of them there are. **One behaviour is worth knowing before it gets reported as a bug**: a version means content, not code. Refactor your build script, re-run it, get identical data, and nothing new is minted -- the recorded commit still points at an earlier one that does not contain the code you just wrote. It names a commit that provably produces that version, not every commit that could. Tests 4067 -> **4107** (+40), FAIL 0 / WARN 0 / SKIP 0; `dev/check-spec.R` 9/9; `R CMD check` 0/0/0 with examples and vignettes run. **Pathway impact: none** -- no new lookup route; an existing history read gains a field. **Reviewed after it landed; one finding, accepted and fixed.** Reading the copy of the history that storage already holds could fail -- an unreachable store, or bytes that will not parse -- and the code treated that the same as storage simply having nothing. Those are not the same thing: the second is an ordinary first write, the first means the values only storage had are now unknown, and the upload replaces that file whole. So a version git cannot account for lost a good value, in exactly the case where nothing else could bring it back. It now **says so**, naming which versions lost their commit link, and only when one actually did -- if git could supply them all, nothing was lost and nothing is said. **It reports rather than refusing**, and that was checked rather than assumed: refusing would have left a repo whose stored copy is corrupt with no way to replace it, since the repair verb goes through the same code. That file is a summary for readers with no clone and rebuilding it is the point; doing it silently is not. **The review's own suggested test reddened nothing at first, which was the review being right twice**: the line I reached for was not the one doing the swallowing, and probing the two separately turned up a case with no test at all -- nothing stored *and* nothing derivable, where silence is correct. Fifteen deliberate breakages now, each failing its own test. Six existing tests gained one line of setup and no changed assertion: they drive an old code path against a store whose reads were never stood in for, so the new report was correct and the fixtures were incomplete. Tests 4107 -> **4115**; `R CMD check` 0/0/0; `dev/check-spec.R` 9/9. **TASK 27 IS AUDITED AND READY TO START COLD (2026-09-19), NOTHING OPEN.** Task 27 adds a verb that points a product's inputs at their newer versions -- today the only way is to rebuild the list by hand, and two of the obvious spellings quietly do the wrong thing. Thirteen findings, five decisions settled by the owner, and **two claims the audit itself made turned out to be wrong and were corrected in the same round**, which is the part worth knowing. **The one that mattered: I justified the argument order by saying it lets you chain read, edit, write in one line. It does not -- the write verb's first slot does not accept a set, so that chain stops dead.** Two more checks finished the claim off: chaining into a later argument needs a version of R newer than datom supports, and the editing recipe this project already documents was never a chain in the first place, just three ordinary lines. The order stays, on a rule that is actually true: a verb that **edits** something you already hold takes it first, a verb that **looks something up** takes the connection first -- which the package already does, and which explains this verb needing both. **The second correction rejected an alternative that looked like it solved three problems at once**: returning a part-built set instead of a finished one. It would have -- except a part-built set carries the connection it will eventually be written through, and that has to be a connection to the product's own repo, while this verb is handed connections to the projects its *inputs* live in. A product whose inputs all live elsewhere would get back something it cannot write. **One decision needed a rule written down rather than just a choice**: asking to move inputs forward will now pick up whatever is newest unless you name a version. That sounds like it contradicts an existing rule that datom never guesses at 'newest' when recording a pointer, so the boundary is now stated where the contradiction would be read: a verb that says nothing about time must not guess; a verb whose whole name means *move this forward* may, and then has to report what it picked. The guarantee is not traded away -- the set it writes still records exact versions, so anyone reading that set later gets the same thing every time. Also settled: an edited set no longer claims to be the version it was read as; the shortcut buttons on moved inputs are rebuilt so they cannot point at old data while claiming new; and consistency is made impossible to represent rather than checked for. Two traps are written down for whoever starts: keeping an input's labels the obvious way silently drops any empty one, and the test fixture the task said already existed does not -- it proves the opposite case. **TASK 14 DONE (2026-09-18) -- the health check now understands both kinds of artifact, and it can put a set's payload back. NEXT IS TASK 15**, which records which commit produced a version, then the two set-editing verbs, then the sweep. The check looked for a data file of the wrong type for a set, so every set reported its data missing from storage, every time. It now looks for whatever that artifact's own record says it is. A set gets three more questions asked of it: does every table it cites still exist at the exact version cited, does the set record the fingerprint a reader needs to verify its contents, and is what it cites something this version of datom can even read. **It stops at one level**: a set that cites another set confirms that set exists and never opens it, so checking a set costs the same whatever sits underneath. A cited table belonging to a **different** project is checked only for being a well-formed citation, because one connection sees one project's storage and looking for it here would report every cross-project citation as broken. **The repair option gained the one upload that is safe to give it**: git holds a set's contents, so contents that storage has lost come back from the clone -- only when storage really has none, only when the clone's copy matches the fingerprint already recorded, and never re-fingerprinting. Before this, a set whose upload failed after the commit could not be repaired by anything: re-running the write correctly does nothing, because the set's contents did not change. **A stored copy that is already there is never overwritten**, since a version points at those exact bytes. **Seven deliberate breakages confirmed each test fails when it should** -- and one of them caught a test of mine that was passing for the wrong reason: it was checking the never-overwrite rule through a different guard entirely, and stayed green with that rule deleted. Rewritten to watch for the upload itself. Three calls taken at their defaults and written down: no way to hand in another project's connection (running the check against that project is better anyway), no download to re-verify bytes (the read path does that where it matters), and the payload restore is in even though it was beyond the task, because without it the rule about not overwriting could never have been tested. Tests 4012 -> **4067** (+55), FAIL 0 / WARN 0 / SKIP 0; `dev/check-spec.R` 9/9; `R CMD check` 0/0/0 with examples run. **Pathway impact: none** -- no new lookup route; an existing check reads one more field. **A review found one thing and it was accepted in full**: the payload restore fires on **two** public routes, not just the repair -- the other is the "mirror everything to storage" form of the write verb -- and that is correct behaviour with the wrong words around it. Four descriptions said metadata-only or said "table" while counting sets, and the internal function that does it was called "sync table metadata" when it handles either kind and can upload a payload; all now say what they do, and the second route has a test of its own. Also recorded as a **rule** rather than a second anecdote, because this is the second task where it bit: a test for a guard must fail when that guard alone is removed, and the way to know is to delete it and watch. **TASK 13 DONE (2026-09-18) -- a set can now carry the code and environment that produced it. NEXT IS TASK 14**, where the repair verb learns that not every artifact is a table, then 15 onward. A set write takes an optional list of your own paths -- your code, your lockfile, your build state -- and puts them in the **same** commit as the set, so checking out that commit gives you the pointers to the data *and* what produced it. Nothing records a link between the two: the commit is the link. Storage never sees those files. **The promise the audit said nothing guarded now has a test that looks at git**: re-writing an unchanged set makes no commit however dirty your listed files are, and deliberately breaking that -- committing them on the unchanged path -- turns that test red while leaving the two older tests green, which is exactly why they could not have caught it. **Four ways a listed path is refused, all of them before anything is hashed or written**, so a refusal leaves nothing behind: it does not exist, it points outside the repo, it is a file datom owns and stages itself, or your project's ignore rules exclude it. That last one is the subtle one: git leaves an ignored file out without saying so, and because datom's own files are in the same commit the commit still succeeds -- so the version would claim to contain a file it does not. Naming a bad path is an error **even when the set turns out to be unchanged**, because the checks run before the work that decides whether anything changed. **Nine deliberate breakages confirmed each test fails when it should.** Two things left as they are and written down: committing your own work through the other verb still drops an ignored path in silence, which is correct for a verb that promises to stage what it is given and mints no version; and a file that is both tracked by git and matched by an ignore rule is not refused, because git really does commit it. Tests 3974 -> **4012** (+38), FAIL 0 / WARN 0 / SKIP 0; `dev/check-spec.R` 9/9; `R CMD check` 0/0/0 with examples run. **Pathway impact: yes** -- the set-write route card gained a gate, and its commit step now says what else is in the commit. **TASK 13 WAS AUDITED AND STARTABLE COLD (2026-09-18), on a deliberately narrow pass.** The task adds an argument that lets a set write carry the caller's own files -- code, environment -- into the same commit as the set, so checking out that commit gives you the data pointers *and* what produced them. The review scoped the audit to the one promise in it that nothing currently guards: **re-writing an unchanged set must stay a no-op even when those extra files have changed.** Committing them anyway would be the failure datom spent a whole task preventing, arriving through a side door -- a data write that quietly commits a person's unfinished work. **That promise holds today, and the two tests that look like they check it cannot fail**: they ask the write what it *reported*, and a write that committed the code and then reported "no change" answers exactly the same. So the new test has to look at git itself, and has to confirm the file really was modified both before and after, or it passes in a repo where nothing happened at all. **One scope decision was made and is recorded**: a file the caller lists which their project's ignore rules exclude is now **refused**, rather than being left out in silence -- git stages nothing and reports nothing in that case, and because datom's own files are in the same commit the commit still succeeds, so the version would claim to contain something it does not. Also settled: naming a path that does not exist is an error even when the set is unchanged, because the checks run before the work that decides whether anything changed. Nothing is open, no model escalation is owed, and unusually for this spec no new function is added -- so no website or namespace step. **TASK 12 DONE (2026-09-18) -- there is now a proper way to commit content datom does not own, and the two things datom was already doing right are finally guarded. NEXT IS TASK 13**, the joint commit, then 14 onward. A product repo holds a package's code and its environment beside datom's own files, and until now nothing offered to commit any of that -- so a downstream package's only options were to drive git directly behind datom's back or to abuse a data write. Two new functions close it: one commits (and by default pushes), the other only pushes. **Committing with no file list means "everything", exactly as `git add .` does** -- which is deliberately the opposite of what datom's own writes do, and the difference is the whole point: datom commits at moments *it* chose, possibly in the middle of somebody's editing session, so it names its files explicitly; a person asking to commit their work means all of it. **Two functions rather than one, because "just push what I already committed" has to be sayable without risking a commit.** With only the committing verb, pushing later would mean attempting another commit -- and since that means "everything", a caller who merely wanted to push could get a commit of whatever happened to be lying in the tree. **Calling either twice is safe**, and one detail there is worth knowing because it prevents a silent failure: when there is nothing to commit, the push still happens if the local branch is ahead. Skipping it would mean that one failed push leaves the remote behind **for good**, since every later call finds a clean tree and stops early. Both verbs also refuse a repo whose git checkout is in the odd state where commits belong to no branch -- they succeed, print a number, and become unreachable the moment you switch branches -- and refuse a repo with no remote by saying so instead of failing with a cryptic error from the git layer. **The other half of the task was proving two things that were already true**, which sounds like paperwork and was not: datom's own commits must never sweep up a person's unfinished work, and datom must ignore files it does not own. The existing test for the first one substitutes a stand-in for the very function whose behaviour is the guarantee, so it would have kept passing through exactly the change it was meant to catch; the new test reads the real commit. And the folder used to test the second one has to be one that is not already on an internal skip list, or the test passes for a reason the guarantee does not depend on. **Seven deliberate breakages confirmed each test fails when it should**, and one of them changed the code: an extra safety check added to the push verb turned out to be unreachable, so it is gone and the reason is written where somebody would otherwise re-add it. One thing left untested on purpose and handed to the acceptance sweep: committing "everything" can also pick up datom's own half-written files from an earlier failed write, which is documented, intended, and repaired by the existing repair verb. **Reviewed after it landed; no findings, and nothing is owed** -- noted because every other task here records its review, so silence would read as a review still pending. The review re-derived the removed check's reasoning independently, confirmed the two tests behind that decision are real coverage rather than an absence, and traced what happens on the odd branchless checkout when a push pulls first: it fetches and changes nothing in the working tree before refusing. Tests 3909 -> 3926 -> **3974** (+65), FAIL 0 / WARN 0 / SKIP 0; `dev/check-spec.R` 9/9; both new examples run with their output read. **Pathway impact: none** -- no new lookup or traversal. **TASK 12 IS AUDITED AND STARTABLE COLD (2026-09-18) -- ten findings, and its one open decision is now settled.** The decision: the verb that commits a human's own work does **not** refuse when the shared remote has moved, because committing locally should not depend on somebody else's push or on being online. A follow-up review also overturned something the audit itself claimed -- that an existing check already covers the detached-HEAD case, making a separate guard redundant. It does not: that check gives up early in four situations, including the most common one of all, being up to date with the remote. So the separate guard is the only one that runs most of the time, and the note now says so where somebody would otherwise delete it as duplication. The task is mostly about proving two things that are already true, and the audit's headline is that both proofs are easy to get wrong. **The first**: datom's own commits must never sweep up a human's work-in-progress. That holds today, but the existing test replaces the very function whose behaviour is the guarantee with a stand-in and then inspects what was handed to it -- so it would keep passing through exactly the change it is meant to catch. The test has to inspect the real commit instead. **The second**: datom must ignore files it does not own. That also holds, by two different mechanisms on two different surfaces, so a single test covers half of it -- and the folder used in the test has to be one that is not already on a hardcoded skip list, or the test passes for the wrong reason. **Two questions about the git library were settled by running it rather than reading about it**, both of which change the implementation: committing "everything" can reuse the existing helper after all, and the shortcut an author would naturally reach for would quietly commit files the project's ignore rules exclude. One thing the task body omits: it adds two public functions, so the website's function index needs them too. **TASK 11 DONE (2026-09-18) -- a repo can now declare that it builds its data rather than taking it in from files. NEXT IS TASK 12**, foreign-content discipline, then 13 onward. Creating a repo takes two new arguments that record, in the project config, that this repo builds its artifacts and owns one named collection of them. **What that buys is a clear refusal where there used to be a confusing answer**: the two verbs that onboard source files now stop on such a repo and point at the two verbs that do work there, where before they reported "no files found" and handed back an empty result -- which describes a repo with nothing to import rather than one that does not import, and which would have imported a file left in the input directory by accident. Writing derived tables is untouched, because that is what these repos are for. The status report names the mode and stops describing an input directory the repo will never use. **The two new arguments are required together**, because a repo that declares itself a product and names no collection would pass the first check on every write and fail the second, which is a repo that looks set up and is not. **And a product repo's storage location is always checked for a prior occupant, on every storage backend, with no way to override it.** The reason is what happens on deletion rather than access control: tearing down a project removes a whole location, so a product sharing one with the study it was built from means deleting the product can delete the raw data. **It took three commits, and the middle one is a behaviour change worth knowing about**: creating a repo now stops when it cannot check that location, where it used to say so and carry on. Tracing that path showed the old behaviour did not postpone the check, it removed it -- nothing later performs it -- and creating a repo cannot finish without storage anyway, so nothing that worked before stops working. **Reviewed after it landed; one finding, accepted and fixed.** The refusal you get when the storage location is already taken ended by telling you to pass the override flag -- and a product repo ignores that flag, so the message sent exactly those people to something that does nothing, two files away from a test proving it does nothing. The general shape is worth keeping: a shared check cannot know whether its caller honours an override, any more than it knew which storage backend it was talking to, so both are now told to it. Passing the flag on a product repo is also an **error** now rather than being quietly dropped, because dropping it leaves you believing you took a location over when you did not -- the same reason this release refuses a version supplied beside a pointer that already carries one. And the refusal says *why* there is no override rather than only omitting it, since a bare "use a different prefix" leaves you hunting for the flag. Two smaller fixes fell out of the same trace: the advice given when the upload fails named a verb that cannot upload anything, and the refusal said "S3" and printed an `s3://` address even for a local directory. Tests 3841 -> 3854 -> 3860 -> 3900 -> **3909** (+68), FAIL 0 / WARN 0 / SKIP 0; `dev/check-spec.R` 9/9; `R CMD check` 0 errors on docs, and the four reworked examples run with their output read. **Pathway impact: none** -- no new lookup or traversal; the config was already read where the new checks read it. **TASK 11 IS AUDITED AND STARTABLE COLD (2026-09-17) -- ten findings, two to settle before the first line, each recorded with the default it takes if nobody answers, so a fresh session can begin without a decision round.** The two: whether creating a repo writes the new `mode` key always or only for a product repo, and whether the mode travels on the connection or is read from the config file wherever it is needed -- the second being a decision Task 9 deliberately left to this task. Three things worth knowing before starting it. **No stored file changes shape**, so there is none of this spec's usual format-number, vocabulary and forced-upgrade work -- unusual enough that somebody would otherwise go looking for it. **The namespace guard the requirement calls "one remaining hole" is two holes**: besides the documented force-override, a repo on a local store gets no namespace check at all, which is the store every set test uses, so the acceptance criterion cannot be met until that widens. And **four worked examples in the help pages hand-edit the config file to declare product mode**, so they would go on teaching the superseded route unless they move to the new argument in the same commit. **TASK 23 DONE (2026-09-17) -- the project config file now says what shape it is in. NEXT IS TASK 11**, project mode gating the import path, then 12 onward. `.datom/project.yaml` holds settings a writer has to **obey** rather than merely read -- the oldest datom a repo accepts writes from, and shortly the declaration that a repo owns a set -- and it had no way to say "this needs a newer datom". A build that has never heard of such a setting walks past it and behaves as though the repo had asked for nothing. The file now declares a format number, and every place this build reads it checks that number **before** reading a field out of it. **The number is the file's own, not the one every other datom file carries**, and that is the decision worth remembering: a per-file number stays put through every change to the manifest or to artifact metadata, so an upgrade elsewhere in datom can never refuse a config whose shape never moved -- and a refusal here is expensive, because the check runs while a developer's connection is built and would take reads that would have worked. Nothing existing changes behaviour: a file with no number reads as version 1, which is every repo written so far, and `datom_init_repo()` stamps the field from now on. **A key datom does not recognise is still fine here, deliberately** -- this file is hand-edited, so a stray key is as likely a typo or a note of your own, and the stricter check that refuses unfamiliar fields applies to the files datom writes for itself and is never pointed at this one; a test now says so, because that tolerance is otherwise incidental and a later tidy-up would remove it. **Every place datom reads this file is checked**, and the list is worth deriving rather than counting, because the next task adds one: opening a developer connection, the re-read after a data migration pulls a fresh copy, the set-write checks (which read the two settings this whole requirement exists for, and which would otherwise, on a future shape, tell a user to hand-edit a file that is already correct), and the verb that repoints a project at a new data store. **That last one was missed in the first commit and added by review the same day**, and it was the worst one to miss: it is the only verb besides repo creation that *writes* this file, it merges a storage block in on assumptions about the file's shape, and it then commits and pushes -- so a shape this build cannot read would be edited wrongly and handed to everyone sharing the repo. The argument for leaving it out -- that opening a connection already checked -- was refuted by something the same commit already said elsewhere: the file can be hand-edited or pulled between opening a connection and writing through it, and this is the storage-migration verb, so it is called exactly when someone is editing that file by hand. A reader never sees this file at all, having no checkout. **One thing left open on purpose and written down where the code is**: the oldest-writer setting is read from the copy of the file that existed before a migration pull, so a raise arriving in that pull is missed for that one session; the next connection sees it. **The answer for the very next task is written in the new tripwire test itself**: Task 11 adds two keys to this file, which is an addition rather than a break, so the expected key list grows and the number stays at 1. **Task 11's audit then found a limit on that test worth knowing**: it creates a repo in the ordinary mode, so it only notices keys written on every repo creation -- if the two new keys are written only for a product repo, the test stays quiet about exactly the addition it was written for, and needs a second case. Tests 3781 -> **3836** (+55), FAIL 0 / WARN 0 / SKIP 0; `dev/check-spec.R` 9/9. **Pathway impact: yes** -- the "can this build read this repo" route now covers a third file, with its own ceiling and its own call sites. **TASK 25 DONE (2026-09-16) -- a set is now pleasant to build as well as to read. NEXT IS TASK 23**, where the project config file starts declaring its own format, then 11 onward. A set can now be assembled a member at a time in a pipe: open a draft, add one member per line, and finish at the write with nothing typed. The payload it produces is byte-identical to the one the single-call form produces -- what is new is **where an error surfaces**. A malformed member, a label map that is not text, or an artifact that does not exist at the version given all stop on the line that declared them and name that member, instead of stopping once the whole list has been built and indexed; and the single-call form repeats the connection on every member inside a nested list, which is bracket-heavy enough that a human miscounts. The build-script path keeps the direct form; this is the human path. **A draft carries the connection it was opened with, and that is structural rather than convenient**: checking a member as it is added means reading that artifact's own file, so a draft that held no connection could not check anything -- which is the entire reason the path exists. Two consequences, both now pinned by tests. The add verb must never take a connection of its own, because two connections on one draft would end the property; and a draft belongs **in memory only**, since a connection may carry a credential -- which is what its print method says out loud, in the place a user actually meets a draft rather than in a document nobody opens. **The way to include a member from another project is to pass a member record built on that project's connection**, and that is a capability rather than a convenience: a draft holds one connection, so a name can only ever be looked up in one project. The test asserts both halves -- the written file records the other project, and the by-name route fails there -- so a later session cannot mistake the record shape for sugar and drop it. **The version to pin stays required**, and the refusal names the member and points at the history verb: inferring "the current one" would make a build script produce a *different set* on each run from unchanged source, which is a different guarantee from the one pinning already gives. **The audit's one must-decide-first was taken at its stated default and the audit was right about it**: a draft goes in the write verb's **first** argument, while the second keeps the shape it gained when a set read back became writable -- two independent widenings on two parameters, so the test asserts three **routes** to one write (a plain list, a set read back, a draft) rather than three shapes of one argument, and all three produce the same content hash. Two further findings were corrections rather than choices, and both shipped as corrected: the add verb's second argument is the *member* rather than a name, since it accepts a name, a record or a callable link; and the connection's print method does **not** mask a token -- it lists named fields and never reaches one -- so the draft's print method follows the same allowlist shape, which is safe by default because a secret added to a connection later cannot leak through a method that never iterates it. The fixture puts a recognisable token on the connection so that assertion is not vacuous. Two smaller refusals rather than guesses, each one test: a version or labels supplied beside a record or a link, which already carry their own and could only disagree; and a draft handed a member list as well, which would write a set the caller did not describe. **REVIEWED after it landed; one finding, accepted and fixed.** A draft printed a member count the write could silently change: the write drops an exact repeat -- the same version with the same labels -- and the draft appended it, so forty lines with one accidental repeat printed 40 and wrote 39 with nothing said about the drop. Adding the same version with **different** labels was worse, because that is already an error at the write: it surfaced after all forty lines had run rather than on the line that introduced it, which is the one thing this path exists to prevent. Both now settle as the member is added, and the two cases are answered differently on purpose. An exact repeat is **skipped and said out loud** -- refusing it would make a draft stricter than the equivalent list, so a generated list that happens to repeat would start failing where it works today, and skipping in silence would move the surprise rather than remove it. A same-version disagreement **stops**, naming both label sets, under the same error the write uses so a caller can catch the rule rather than the place it fired. **One thing the first fix over-claimed, and the correction is the reusable part**: it tidied each member on the way in, on the theory that two orderings of one label set would otherwise read as a disagreement -- and breaking that deliberately reddened nothing, because the content hash already treats a label map as sorted keys over sorted, deduplicated value sets. So the tidy was doing nothing for the guarantee while changing what a caller reads back out of a draft; it is gone. What is load-bearing is comparing **hashes** rather than the records themselves, and swapping in a plain comparison reddens exactly the test that pins it. **Deliberately not extended to the self-reference check**, which the reviewer flagged and which holds: a draft's name may be unresolved until the config file is read at the write, so a draft cannot know whether a member is itself. Also fixed: the empty-set refusal pointed only at the single-call verb, which is the wrong one for anyone who arrived through a draft. Tests 3697 -> 3770 -> **3781** (+84), FAIL 0 / WARN 0 / SKIP 0; `dev/check-spec.R` 9/9; `R CMD check` 0/0/0 on docs and code/documentation agreement, and the new example runs with its output read. **Pathway impact: none** -- this is a second front door to the write, and the write's own sequence is unchanged. **TASK 24 DONE (2026-09-15) -- a set that reads correctly is now pleasant to use.** Three new verbs, all over the object the set read already returns. **Fetch one member**, naming it by name, by the record the read handed you, or by the callable link on it -- all three through one accessor, so a console call and a loop use the same verb. A name is deliberately **not** a key: the same artifact at two versions is a legal pair of members, so an ambiguous name stops and lists the candidates with their versions and labels rather than answering with the first, and you narrow by label or pin a version. **List members**, one row per member per label, as a plain data frame -- so filtering is ordinary R and datom grows no query language of its own; an unlabelled member still gets a row, which is what makes the name column the complete member list rather than the labelled part of it. **Group members into a navigable view**, `dp$output$adsl(conn)`, where the branches are label values and the leaf is the member's own name holding its link. **A member with two labels on the grouping key appears under both branches**, which is the entire reason labels replaced folders here -- a folder holds an item once and a label does not -- so the leaf count legitimately exceeds the member count, and that is asserted rather than assumed. Two requests are **refused** rather than guessed at, both because the alternative is a member the consumer cannot find and cannot see is absent: two members asking for one leaf name (the set stays legal; only that grouping of it is refused, and the message names both members with their versions and points at adding a second grouping key), and a bucket name for members missing the key that collides with a real label value -- refused whatever the members currently look like, so the view does not start failing the day someone adds a member without that key. **The highest-value message in the whole design now exists**: fetching a member that lives in another project fails, because access is per project, and the error now says which project the member's **own writer** recorded instead of presenting as a missing file. It is a hint **on failure and never a check that runs first** -- a connection's project name is a label nobody validated, so refusing on a mismatch would abort fetches that work, which is pinned from both sides; and it lives in the shared link core rather than in the fetch verb, because a leaf of the grouped view is a link and never reaches that verb. **REVIEWED after it landed; one finding, accepted and fixed.** A label map carrying the **same key twice** silently lost every label after the first, in all three verbs: the listing showed one value twice, the grouped view put the member under one branch instead of two, and filtering reported **not found** on a label the stored file says the member carries -- the last of which reads as missing data rather than as a bug. The cause is that the single expansion step read its own input **by name**, and asking a list for a key that appears twice returns the first match every time. It is reachable rather than hypothetical: the check that refuses a duplicate key runs on writes only, by an earlier deliberate decision that a reader validates no label map, and the JSON parser keeps duplicate keys as two entries rather than collapsing them -- both verified rather than assumed. The valuable part of the reviewer's framing is kept: this is the same silent-first-match failure the one-expansion rule exists to prevent, arriving on the **key** axis instead of the value axis, written inside the very function that was supposed to leave it nowhere to live. Fixed by iterating positionally, and by routing the filter through the same expansion so a member's labels have genuinely one access path. Four tests, built from parsed JSON so the fixture is the shape that actually arrives; restoring the by-name read reddens 5 assertions across 4 tests. A blank label key, reported as incidental by the reviewer, is fixed by the same change. Tests 3585 -> 3686 -> **3697** (+112), FAIL 0 / WARN 0 / SKIP 0; `dev/check-spec.R` 9/9; `R CMD check` 0/0/0 on docs and code/documentation agreement, and all four affected examples run with their output read. **Pathway impact: none** -- no new lookup and no new traversal; the fetch verb performs exactly the reads the two read verbs already performed, and the other two verbs do no IO at all. **TASK 24 WAS AUDITED AND READY TO START COLD -- ten findings, nothing open**, both scope questions approved at their stated defaults on 2026-09-15, so a new session can begin implementing without a decision round. Three of the ten change what that task's written plan says. **The one that would otherwise read as a regression**: Task 24 justified its no-refusal rule partly on the claim that a reader is never told which project name the writer used, and Task 26 made that false -- a member's project now comes from the artifact's own file. The conclusion is unchanged, because what makes a refusal wrong is the **connection's** side of the comparison, which is still a name nobody validated; the sentence is restated in place so a cold session does not conclude the premise died with the defect and add the refusal. The other two: the third argument of the member-fetch verb is the member, not a name, since it accepts a name, a record or a callable link; and a bucket name for members missing the grouping key is **refused** when it collides with a real label value, rather than silently merging two branches. **Task 26 was reviewed after it landed and NO CODE CHANGED** -- one finding withdrawn by the reviewer, one suggestion rejected with its reason recorded (reading the project index file directly instead of through the checked reader would take a value out of a file whose format was never verified, and that value is folded into a set's identity), and three documentation items accepted: the connection-repointing hazard rehomed as a constraint on the unbuilt governance package, a stale promise in that same document marked, and the triage rule the spec had been applying without stating it written down. **TASK 26 DONE (2026-09-15) -- a project name that lands in a stored file now comes from the repo that wrote it. NEXT IS TASK 24**, the read-side ergonomics, then 25, then 23, then 11 onward. Every artifact's metadata file now records which project's space it was written into, taken from the writing repo's own config file rather than from the name typed on a connection -- and **no existing artifact gains a version**, because the field takes no part in version identity. The defect it closes: a **reader** connection's project name is a string the caller types, the storage location comes from their store settings, and nothing compares the two, so a name nobody had checked was being reported as a set's project and written into a member record -- where it enters a stored file, is folded into that set's content hash, and is cited afterwards. The member and lineage constructors now take the name from the artifact itself, falling back to the project's index file for anything written before this release, and to the connection's name only if neither has it -- **which it says out loud**, so an unchecked name is never recorded silently. The lineage constructor is the one that can move a version, since lineage is part of a table's identity: a table whose parents were declared through a mislabelled reader mints one new version at its next write, which is the correct outcome and is now in NEWS. **Two of the nine probes are the valuable ones because they caught nothing**, and both contradict what the audit had claimed: the empty-object hazard lives in how the field is declared and not in the guard beside it, and moving the new argument up the signature breaks nothing, because every caller already passes the rest by name. Both records are corrected rather than repeated. **The worst case now reproduces**: with governance attached, a reader who means one project and types another's name is silently repointed at that project's space and gets **its rows**, reported as a data migration -- the same message a real migration gives. Recorded as a characterization test, and **deliberately not filed as a datom issue**: nothing in datom can create the situation any more, because the function that registers a project's location has no caller left here -- registering moved to the governance package, which is not built. So it is a constraint on a component nobody has written, and it lives in `dev/datomanager_overview.md` section 4a, directly beneath the section that states the rule it exploits, where whoever builds that component will actually meet it. **One process failure, a new variant of the rule the last task wrote**: the probe harness restored from its own copy as required, but crashed partway, and the re-run took a fresh copy of the already-broken tree and called it pristine -- two deliberate defects ended up applied at once and one source file was repaired by hand. The rule is now in `dev/engineering-notes.md`: take the copy once, at a fixed path, and only from a tree that parses. Tests 3557 -> **3585**, FAIL 0 / WARN 0 / SKIP 0; `dev/check-spec.R` 9/9. **TASK 10 DONE (2026-09-14) -- a set is now readable end to end. NEXT IS TASK 26**, appended 2026-09-14 and running before Task 24: a project name that lands in a stored file must come from the repo's own declaration, never from a label typed on a connection. It goes first because the cost of adding a field is that everyone sharing a repo must upgrade before they can write to it again -- and this release already forces that upgrade for another reason, so the field is free now and costs a second forced upgrade if it waits. Task 24, the read-side ergonomics, follows it. **Task 26 was cold-start audited the same day it was written and is startable: nine findings, two open scope questions, each recorded with the default it takes if nobody answers, so a new session can begin cold without a decision round.** The two questions are whether the same fix covers the lineage constructor, where the identical one-line defect puts an unverified name **inside the version identity** rather than only in a citation (default: yes, same commit), and whether the fallback reads the manifest through the checked reader, which can escalate to listing the whole namespace when that file is unusable, or reads it raw, which is cheap but takes a value out of a file whose format was never verified (default: the checked reader). Three things a cold session should know first: neither metadata builder receives a connection, so the name arrives as a new argument that must go last or a dozen positional test calls shift silently; the claim that a set's metadata has exactly seven fields is written in six places and pinned by a test, so changing the builder alone reads as contradicting the spec; and the field moves no version, unlike the artifact-kind field did, which is a classification difference rather than a difference in the edit and is worth one assertion. `datom_get_set(conn, name, version = NULL)` hands back a set's name, project, version, content hash, labels and members -- references and labels and no data, which is why the verb is `get` and not `read`. The version it reports is the one **recorded** in the history rather than one it recomputed, so an 8-character version goes in and the full one comes back, which is what makes a set citable after a read. The stored file is **downloaded, hashed, and only then parsed**, and a version that records no hash for its stored file is an **error** rather than a skipped check -- the convenient reading function parses, so using it would leave nothing to hash but bytes the reader re-serialized itself, which is the write path's worst defect inverted. Reading one kind of artifact with the other kind's verb now stops and names the verb that fits, in **both** directions, from one function and one error class -- without the converse a healthy table read as a set is reported as a missing file. **Every member carries a callable link**: call it with a connection to that member's project and it resolves the pointer -- a table member yields data, a set member yields another set. The link **pins the version it was read at**, carries its own pointer so a consumer holding only a projected view can still cite what they used, prints readably, and survives being saved and reloaded. The write verb now accepts what the read returned, so read-append-write is a loop: the links are dropped for you and the set's own labels come along unless you pass your own. **Six things a later change must not undo, and eleven probes, are in Task 10's record.** The two worth knowing before touching it: **the read never tidies anything** -- the canonicalizer sits right there and changes nothing on a healthy file, so reaching for it passes every test today and surfaces much later as a repair that rewrites reshaped bytes over a stored file whose recorded hash describes different bytes; the tests therefore hand-write files that are deliberately not in canonical form, because a healthy fixture cannot see the difference. And **the link builder is a top-level function with every argument forced**, pinned by serializing a member and searching the bytes for the fixture's fake token: a builder defined inside the read would put the connection on the closure's chain and write the credential into the file. Probing that deliberately broken shape found nothing at first, and the reason is worth knowing -- R's lazy arguments meant the connection was still an unevaluated promise, so it did not travel; forcing it reproduced the leak exactly, which is why both halves of the rule are load-bearing rather than one. **One process failure, recorded because it cost an hour**: the probe harness put the code back with `git checkout`, which restored the last commit and deleted the whole of this task's uncommitted work. It was rewritten from the session transcript and the already-generated help pages and verified identical by line count and by a green suite, so nothing shipped differently -- and the rule is now in `dev/engineering-notes.md`: a probe restores from a copy it made itself, never from git, because the code under probe is by definition uncommitted. **REVIEWED after it landed; one finding, half accepted and half refused (tests -> 3557).** The link builder took a project name it forced and then never used, while this task's record claimed the link compared the member's project against the connection's. The dead argument is gone and the record is corrected. The reviewer's fix -- add the comparison -- was **refused on evidence**: for a reader connection, which is exactly who reads sets, the project name is a label the caller types and nothing checks it against the repo, so a mismatch is the ordinary case and a refusal there would abort a fetch that works. Reproduced with a reader labelled `a-label-nobody-validated` reading a set written by another name, and two tests now pin the no-refusal behaviour. The reviewer's structural point is kept and moved to Task 24: the *message* belongs in the shared link core rather than in the named lookup verb, because a projected view's leaf is a link and never reaches that verb. Two documentation gaps from the same review are closed: the version can legitimately be empty when a repo's history is truncated, and the project field on a read set is the connection's label rather than a recorded fact -- a member's own recorded project is the one to cite. **One thing surfaced for you rather than fixed**: nothing verifies a reader's project name against the repo, which is why the above holds, and it also means a member declared through a mislabelled connection writes a wrong project name into a citable file permanently. Candidate fix is to verify it at connection time against the manifest; it changes behaviour for existing readers and wants its own issue. Tests 3418 -> **3557** (+139), FAIL 0 / WARN 0 / SKIP 0; `dev/check-spec.R` 9/9; `R CMD check` 0/0/0 on docs, code/documentation agreement and examples, both new examples run and their output read. **Pathway impact: yes** -- a new route card for the set read, plus a correction to the table read card. **TASK 10 REPLANNED AND RE-AUDITED 2026-09-13 after a design round on ergonomics; everything below is decided and nothing is open.** Three things were renamed or reshaped, each for a stated reason. **(1) The set read is `datom_get_set()`, not `datom_read_set()`** -- it hands back pointers and labels and no data at all, and this package already used `get` for exactly that (the parent and lineage readers). That is also the answer to "why not one read verb that dispatches on kind": they are two operations, not one with two implementations, so one name would promise content for one kind and references for the other. The implementation is shared regardless -- only fifteen lines differ per kind. **(2) Every member carries a callable link, `$fetch(conn)`**, which resolves that pointer to whatever it points at. Named `fetch` because a table member yields data and a set member yields more references, so both other verbs would lie for half the cases. It is the one polymorphic door in the design and it sits at the member level, where a caller iterating members genuinely cannot know each kind in advance. The link also **carries its own pointer** as an attribute and prints readably, which closes the loop: someone holding only a projected view can still cite what they used and add it to a new set. That has to ship with the factory, since links built without it cannot be repaired later. **(3) The ergonomics split into two appended tasks** so each can be deferred without unpicking the others: Task 24 is read-side (resolve one member by name, tabulate members and their labels, build the navigable view), Task 25 is write-side (assemble a set step by step in a pipe, with each step validated on its own line). Both execute after Task 10. **A naming rule was derived from the 40 existing exports rather than invented**, and is now in design.md with the rejected names and why -- the drift was in what was being proposed, not in the codebase. The verbs: `get` returns references, `read` returns content, `fetch` resolves a pointer, `list` returns a data frame, `assemble`/`add` build, `structure` shapes a view. **One correction to the record**: a claim that re-serializing a link would fail loudly is **false** -- verified, the JSON writer emits the function's source text silently -- so the protection stays where it was, in the rule that a read never reshapes content. **The re-audit checked eight things against the tree, all clear**, the load-bearing ones being that no existing test compares the version resolver's whole return value, and that both places which refuse an unexpected member field are live, which is why the link is stripped in the write verb and not carved out of the validator. **TASK 10 COLD-START AUDITED 2026-09-13: startable, with TWO SCOPE QUESTIONS OPEN.** Both are marked open in the task with the default it will take if you say nothing, so nothing is blocked. **(1) The function's arguments and what it hands back are written down nowhere** -- not in the task, not in the requirement, not in the design -- and the task's phrase "return members plus payload" is circular, because the payload IS the members plus the labels. Both are public shape, which is why they are not defaults to take quietly. Proposed: the same three arguments the table read already uses, and a plain list carrying the set's name, project, version and content hash alongside its labels and members -- the four identifying facts because a caller who asked for "the current version" otherwise cannot say which version they got, and being citable is the whole point of this artifact kind. **(2) The check that refuses reading one kind as the other is free** -- the document it needs is already read -- **but the function to reuse is worded for a write**, saying "already exists" and "pick another name" and naming the two write verbs. Proposed: give it a read/write word that selects the wording, following the format check, which took that shape for the same reason; the alternative puts one rule in two functions. **Nine further findings are in the task.** The two that would have cost real time: the integrity check **forbids the convenient read**, because the convenient function parses -- so the payload has to be downloaded, hashed, and only then parsed, and the convenient function was verified to return an identical result, which is exactly why a naive implementation reaches for it and is then left hashing bytes it re-serialized itself, which is the write path's worst defect inverted; and the existing version resolver hands back the **parquet** integrity hash while a set needs its **document** one, so it gains a field argument or a sibling, the precedent being Task 9's one shared scan with a thin wrapper per kind. Also recorded so neither is settled by accident: only **one** of a set's two hashes is a read-time check -- the content hash must not be recomputed, since it would refuse a payload a newer datom wrote and catches nothing the byte hash did not -- and the nested-set test is cheapest with a hand-built member, where a traversing implementation **errors** rather than silently flattening, which is louder than a count. Confirmed rather than assumed: reading a set with today's table read really does fail with "file not found" naming the store root and nothing about sets; a connection with no clone really does reach a set's metadata and history; and the byte hash really does reproduce from the downloaded file. **TASK 9 DONE (2026-09-13) -- a set is now writable end to end. NEXT IS TASK 10, reading one back.** `datom_write_set(conn, members, tags = NULL, name = NULL, message = NULL)` is exported and lives in a new source file. What it does, in order: two checks read the repo's config file and refuse a repo that has not declared itself a product repo or that names a different set; the forward-compatibility door runs next; then the caller's labels are cleaned up, then checked, then the member list is deduplicated and ordered; then three refusals only a whole-payload view can make -- no members, the same artifact version listed twice with conflicting labels, and a set listing itself. The payload is then written **twice from one file**: git keeps it at one stable path so git carries the history and a diff shows which members changed, storage keeps the same bytes under a content address so a reader with no clone can fetch an exact version. Content already in the history is never written again -- the stored copy is left alone and its recorded byte hash is carried forward, which is the defect that passes every per-chunk test and surfaces much later as a refused read of a valid version. The manifest row carries the member count **instead of** a byte size, on the healthy path and on the reconstruction alike. **REVIEWED after it landed; one finding, fixed (tests -> 3418).** Key order in the written payload was made canonical at two levels and needed three: the set's own label map and each member's pointer were sorted, the member record's own two keys were not. The hash reaches both halves of a member by name, so the two spellings are identical to it and only the file differs -- and the case where that costs something is the one the reuse machinery exists for, a revert to content already in the history, where the clone's copy is rewritten while the stored copy is deliberately left alone. Git would then hold bytes that do not match the recorded byte hash while storage holds bytes that do, which reads as a refused read of a valid version from the copy that looks correct. One line, in the function that already sorted the level below, and no existing payload changes because the canonical order is the one the member constructor already emits. The sort is guarded and the guard has its own test: sorting a record that has no names at all would empty it rather than leave it recognisable for the checker to report. Probed both directions, one assertion each. **Both approved scope calls shipped as approved**: test fixtures hand-write the config file and the two checks read it directly rather than carrying those facts on the connection object; and the commit-then-push-then-upload sequence was **extracted** out of the table write into one shared function, so the table write path was touched. **Six things a later change must not undo, and eight probes, are in Task 9's record.** The two worth knowing before touching it: the file's member order is deliberately **not** the hash's member order -- sorting the file by hash would relocate an entry whenever its labels changed, so a diff would report a delete plus an insert instead of one changed field -- and the cleanup step is **tolerant on purpose**, passing anything it does not recognise as text through untouched so the checker reports it with a message naming the key; a cleanup step that reached for a sort would fail with a base-R message naming nothing, and the fix for that is not to check first, because checking first makes every cleanup rule unreachable. **One probe is worth recording as a method note**: sorting the file by hash first reddened **nothing**, because the fixture had two members and the two orders agreed by chance -- a coin flip masquerading as a guard. The fixture is now five names pinned at one version where hash order is the exact reverse of name order, asserted in the test itself. **Two forcing functions fired as designed and neither was weakened.** The manifest-row vocabulary test now writes a row of **each kind**, because a set row carries the member count where a table row carries a byte size, so no single row can carry every name on the list; the alternative was an exception list, which hides the asymmetry instead of asserting it. And the reconstruction's field-for-field test now writes a set, which is what makes its set-specific branch reachable at all. **Deliberately inert, and it has to be said**: nothing writes the two config fields yet, so a set write is unreachable through the public path until a later task adds them at initialisation. That is the same shape the reader-side format check shipped with. The runnable example hand-writes them, which is the honest form of the example for now. Tests 3227 -> 3413 -> **3418** (+191), FAIL 0 / WARN 0 / SKIP 0; `dev/check-spec.R` 9/9; `R CMD check` 0/0/0 on docs, code/documentation agreement and examples. **Pathway impact: yes** -- a new route card for the set write, plus two corrections to the write-entry card. **The citation sweep found the usual class again**: insertions into four source files and two test files staled 31 live citations, of which the gate caught 4; the rest were repointed by content, and **three were already wrong before this session** -- two spec files cited the history scan as though it were the function that decides whether to reuse a stored object, with line ranges belonging to neither. Eighth consecutive session for that class. **THE TASK 9 COLD-START AUDIT IS KEPT BELOW, because both calls it raised are what shaped the result.** **TASK 9 COLD-START AUDITED 2026-09-11: startable, and nothing is owed before it -- the two scope questions it raised were both approved at their stated defaults the same day, so they are decisions rather than defaults that happened to hold.** The two, so a fresh session does not reopen them: test fixtures hand-write the project config (the execution order stands, and the gates read that file directly rather than carrying the two facts on the connection object), and the commit-then-push-then-upload sequence gets pulled out of the table write so both write verbs share one copy, with the commit saying the table path was touched. The audit is in Task 9's body; nine findings, two of which would have cost real time. **(1) The two gates the set write opens with read `project.yaml` fields that nothing writes until Task 11 -- which runs AFTER Task 9.** Verified: there are exactly two places that write that file and neither writes `mode` or `set`, and nothing in the package reads them either. So the moment the set write lands it is **unreachable through the public path** -- no repo can declare itself a product repo, so every set write is refused at its own door, and the same applies to Task 10's read. That is the same deliberate inertness Task 4's format gate had, and it is fine, but it has to be said or it reads as a defect. Pulling Task 11's init half forward is the wrong fix: Task 11 is blocked on Task 23 precisely because writing that field needs a released build that already reads the file's format number, so moving it up drags Task 23 with it. Default: keep the order, fixtures hand-write the config. A second half of the same item, unstated anywhere until now: those two fields do **not** ride on the connection object the way the writer floor does, so Task 9 has to choose between reading the config at the gate and adding two connection fields -- and Task 11 inherits whichever is chosen. **(2) "Reuse the existing write's steps 7 to 10" is not a function call.** Those steps are inline comments inside one 100-line function body, so the task either extracts the commit-then-push-then-upload sequence or writes a second copy of "git must succeed before storage is touched" -- the largest scoping decision in the task, currently phrased as though it were free. Default: extract, and say in the commit that the table write path was touched. **Also found:** a new write verb inherits nothing from the three existing write doors and must call the forward-compatibility entry check itself, which is the same route-was-the-gap finding for the fourth task running; the **healthy** writer has the same set-row defect the task attributes to the rebuild alone, writing a byte size of zero and no member count, and the healthy path is the more damaging of the two because a rebuild only runs on a broken index; the function that stamps the row still hardcodes the artifact kind, a line now handed forward three times; and one code citation named the wrong function outright -- it pointed at the scan rather than at the decision function that returns the reuse verdict, and the gate could not see it because the cited lines are real prose. Two claims were verified rather than taken on trust and both hold: change detection genuinely does work unchanged for a set, and the rebuilt-set-row analysis is exactly right. **REVIEWED after it landed; one finding, and it reverses one of the decisions above (tests -> 3227).** The member constructor read the version-pinned metadata snapshot **without checking the format that document declares**, on the audit's stated default of matching the lineage constructor, which does the same. The default asked whether the two siblings should agree and stopped one question short of what the missing check was holding up: **the kind fallback**. Reading an absent kind as "table" is right for a document written before the field existed and wrong for one written by a build this version cannot fully parse -- there a **set** is recorded as a table, and the misreading is durable rather than momentary, because it goes into the member record, into the stored file, and into the set's own content hash, so a citation names the wrong kind of artifact permanently and nothing fails. **The codebase had already settled the pattern**: the manifest rebuild checks the per-artifact document and then applies the identical fallback, so of the three places that read a kind out of such a document, one checked and two did not -- which makes the two the anomaly, not the check the innovation. Both are now gated, in one commit, and the lineage constructor was included rather than left as the sibling that justifies the gap: its two fields are durable at one remove too, since one becomes a storage address and the other is folded into the lineage of whatever table declares the parent. Two facts were verified before agreeing rather than assumed: the snapshot is written from the same object as the artifact's current metadata, so it does carry the format number and the check is live rather than theatre; and a version-pinned **read** never touches the snapshot at all -- it resolves from the version history -- so the gap really was exactly two functions. **The behaviour change is accepted, not incidental**: a snapshot declaring a format newer than this build supports now stops with the usual upgrade message where it previously half-worked. That is the owner's standing position applied -- the released versions are experimental and unannounced, so support for them must not buy fragility for what comes next -- and the posture the conventions file already states, that breaking loudly is acceptable here while degrading silently is not. Nothing changes for any document datom has ever written: a snapshot with no declared format is still read as version 1. Probed: removing the check at both sites reddens 5 assertions across 3 tests. The new test asserts **both halves together** -- no declared format plus no kind yields a table, newer format plus no kind is refused -- because either half alone reads as arbitrary. **The fallback itself stays, and that is a use case rather than sentiment**: every version written before this release has a snapshot with no kind, so requiring the field would make those versions uncitable, and the spec's own worked example is a current table sitting beside a **locked baseline** -- which is by definition an older pinned version. NEWS carries the refusal. **TASK 8 DONE (2026-09-11) -- a member of a set is now constructible, so the pieces a set write needs are in place. NEXT IS TASK 9, `datom_write_set()`.** `datom_member(conn, name, version, tags = NULL)` resolves one artifact version through one project connection and hands back plain data: which project, which artifact, which kind of artifact, which version, plus optional labels describing that member's role in the set. It carries **no content hash** -- the version already pins the content, and a second copy of that fact is a second thing to keep consistent, which is a deliberate divergence from the lineage constructor it otherwise mirrors step for step. Reading the version's own metadata snapshot is not a formality either: it is the entire reason a set cannot contain itself at any depth, because a member can only point at something that already exists. Shipped beside it: the checker every set write will run over a member list, and two small helpers holding the label rules in one place so the write reuses them for the set's own labels rather than growing a third copy. Nothing calls the checker yet -- that is Task 9, and it is fully tested from its own test file in the meantime. **Three things a later change must not undo.** (1) **A member with no labels omits the key rather than carrying an empty one, and no hash can tell the difference** -- an absent label map and an empty one hash identically, so nothing in the identity suite would redden, while the JSON writer turns an empty one into `{}` and would put that into every untagged member of the stored file. The trap is narrow enough to be worth stating exactly: assigning nothing to the field removes it, but building the record with the field listed keeps it. Same fact about the JSON writer that Task 7's review turned up, with the correct answer **inverted** -- there the field had to be present, here it has to be absent. The guard is therefore an assertion on the written bytes, with a companion test pinning the two hashes as equal so nobody later tries to catch it through identity. (2) **The field check is deliberately stricter than the lineage validator it mirrors**, which accepts a missing value: it is text, it has length one, and R's own emptiness test answers TRUE for it, so the obvious three-part test lets it through. A missing value in a member would be spliced into a storage address or written into a citable file. Rather than assert the divergence in prose there is now a test that calls the older validator with a missing value and records that it passes -- so a later tightening of that one fails there instead of leaving this note quietly wrong. (3) **Cleaning up runs before checking, and the checker deliberately accepts a label key whose value is empty**, because that is a cleanup case rather than an error; reversing the order makes the cleanup rule unreachable. **Six probes, each reverted, each naming what it reddened**: building the record with the labels field listed reddens 5 assertions across 3 tests including both byte-level ones; mirroring the lineage validator verbatim reddens 3; disabling the empty-label refusal reddens 8 across 5 tests; disabling the unknown-kind refusal reddens 3; removing the cleanup step reddens 4; disabling the fifth-field refusal on the pointer reddens 2. **Four decisions the task body did not settle.** A label key set to R's NULL is cleaned away exactly like an empty text vector, since both are the same nothing and refusing one while dropping the other is the inconsistency the clean-then-check rule exists to remove (the hash encoder still refuses a NULL read back from a file, and must -- there is no caller intent to clean toward). Type checking of label values **delegates to the hash encoder's own coercion** rather than restating its rules, because two copies of "what counts as text here" would eventually disagree and the encoder's copy is the one the published goldens freeze; what the new checker genuinely adds is the empty-label refusal, the four-field pointer shape, and the kind being one of exactly two values. A named text vector is refused rather than converted, because it cannot express a multi-valued label and accepting it would add a cleanup rule Task 9's canonicalizer would also have to know about. And label values are **not** sorted, deduplicated or unboxed here, so canonical form has exactly one implementation and it is the write's. One thing left as the audit recommended: no format-number check on the snapshot it reads, matching the lineage constructor -- closing it for one of two siblings would make them disagree about the same document. Tests 3077 -> **3218** (+141), FAIL 0 / WARN 0 / SKIP 0; `dev/check-spec.R` 9/9; `R CMD check` 0/0/0 on docs and code/documentation agreement; the runnable example was executed and its output checked, not merely built. New documentation section for set functions in `_pkgdown.yml`, which Tasks 9 and 10 extend. **No pathway impact** -- the same version-pinned snapshot read the lineage constructor already performs, so no new lookup and no traversal. Six live code citations in the spec were repointed by content after a new constant shifted every line below it in one `R/` file; the gate caught one of the six, which is the seventh consecutive session for that class. **THE TASK 8 COLD-START AUDIT (2026-09-10) IS KEPT BELOW, because both calls it raised are what shaped the result.** The audit is in Task 8's body. **(1) The self-reference refusal MOVED TO TASK 9 (owner-decided).** Task 8's own bullet had the member constructor refusing a set that lists itself, and it cannot: the criterion and the requirement both say "at write time", and the precondition is the set's own declared name -- a field verified to be neither written nor read anywhere in `R/` today, and one the constructor could not trust anyway, since the connection it takes belongs to the **member's** project rather than the set's. Task 8 keeps the half that actually delivers the guarantee this check is confused with: reading the member's snapshot, so a member can only point at something that already exists. **(2) An empty tag value is TIDIED, NOT REFUSED (owner-decided).** The spec said both, in two places, for the same spelling; the later tidy-then-validate decision wins. It mattered here because the constructor validates as it builds, so one reading aborts on a payload the write path quietly accepts. **Four copies corrected, two outside the spec** -- a roxygen comment in the sv1 encoder and an engineering note were both leaning on "validation refuses" as settled, and the design copy additionally lumped the exact-duplicate member in with the refusals when it is tidied too. **Three findings a fresh session would have hit late.** A member with no tags must **omit** the key rather than carry it as NULL -- the hash is identical either way so nothing fails, and the write path would then emit `"tags": {}` into every untagged member, the one spelling the spec says writers never emit (the Task 7 `document_sha` trap, one layer down, with the correct answer inverted). Mirroring `.datom_validate_parents()` verbatim ships a defect, because its field test accepts `NA_character_` (verified: `nzchar(NA_character_)` is `TRUE`) while AC27(b) requires missing values refused -- the mirror must be stricter than its model. And the sv1 encoder already refuses most of the tag grammar, so the new validator's real work is the empty-string refusal, the four-key pointer shape, and `kind` being one of exactly two values. Also verified: a new export owes four things, not one -- namespace, help page, an entry in `_pkgdown.yml` (all 38 current exports are listed and pkgdown is a required check), and a **runnable** example, since this package's examples really execute against a throwaway git remote and temp store. **TASK 7 DONE (2026-09-10) -- an artifact's own metadata now says which kind of artifact it is, and the set metadata builder exists. THE SET WORK PROPER HAS STARTED; NEXT IS TASK 8.** Four edits. (1) **`kind` in per-artifact metadata, and in the identity list.** `.datom_build_metadata()` stamps `kind = "table"` -- a field rather than a parameter, because a table write is the only thing that reaches that builder and a set has its own. It is identity so that a table and a set can never share a version string, and the price, known and accepted since 2026-08-23, is that **every existing table mints one extra version at its next write** on content that did not move: change detection recomputes identity from the stored document, and a document that gained an identity field disagrees with its recorded version. Bounded and in the safe direction -- `data_sha` does not move, so the storage address does not move, the parquet is reused and nothing is re-uploaded -- and now in NEWS with that reasoning. **Both pinned identity goldens moved**, which is the first licensed exception to "if a golden fails, the code drifted"; the old values sit in a comment beside the new ones so the next reader can tell a licensed move from a drift. (2) **`.datom_build_set_metadata(payload, document_sha = NULL)`**, deliberately beside the table builder rather than in a file of its own, because the two documents are close enough that a field copied from the wrong one is easy to miss. Exactly the seven fields the spec allows a set, and it computes `data_sha` itself from the set hash rather than taking it, so no caller can hand a set a table-regime hash. (3) **`document_sha` in the version-history entries**, on the same conditional-add terms as `parquet_sha` -- inert, because nothing computes one until the set write path lands, and there from day one so a set read can treat an absent one as an error rather than reproducing the skip-on-absent grace pre-cv1 tables need. (4) **Task 22's handoff discharged**: a rebuilt manifest row reads `kind` from the document and keeps `"table"` as the fallback for documents written before the field existed. The forcing-function test that made this happen was **replaced, not deleted**, by one asserting all three cases. **Two things a later change must not undo.** `document_sha` is **declared** in the set builder, not conditionally assigned -- the two spellings produce identical files while the value is NULL and differ only in the in-memory object, which is where the seven-key contract is asserted, so the tidy-up looks free and is not (probed: 3 tests redden). And `kind` stays on the **identity** list; moving it to the excluded one reddens 3 assertions and would let a table and a set share a version. **Four probes, each reverted and each naming what it reddened**: misclassifying `kind` as not-identity reddens 3, the set builder claiming `"datom-cv1"` reddens 1, reverting the rebuild's kind and disabling the `document_sha` history line reddens 1 each, an unclassified field on the set builder reddens 3 naming the field. **One probe was discarded as imprecise and the reason generalises**: deleting `kind` from both classification lists reddens 12, but through the write door's vocabulary check rather than through identity -- a probe that trips the wrong mechanism reads as confirmation while proving nothing about the guard under test. **Left for Task 9, stated at the site rather than left to memory**: a rebuilt **set** row recovers its `kind` but not its `member_count`, because that number lives in the payload rather than in either document the rebuild reads. Tests 3053 -> **3077** (+24), FAIL 0 / WARN 0 / SKIP 0; `dev/check-spec.R` 9/9; `R CMD check` 0/0/0 on docs and code/documentation agreement. **Pathway impact: yes** -- the reconstruction card's field-copying step and its closing warning both said `kind` was not in per-artifact metadata. **Reviewed after it landed; the shipped code was sound and two written claims were not, both now Task 9's to close.** (1) A rebuilt row for a **set** is wrong in **two** directions rather than the one recorded: a set's row carries a member count *instead of* a byte size, and the rebuild leaves the member count missing (it lives in the payload, not in either document a rebuild reads) while emitting a byte size of `0` (the default has length 1, so it survives the compaction step). The field-for-field test would catch both, but its fixture writes tables only -- the same shape as the `kind` hardcode this task just fixed, so Task 9's body now says to extend the fixture rather than trusting the comment. (2) **`jsonlite` does not omit a NULL element -- it writes `{}`**, so the claim that the declared and conditional spellings of `document_sha` "produce identical files" is false. Nothing shipped is affected (no set is written yet, and `parquet_sha` escapes because assigning NULL *removes* an element), but a set document written before that field is populated would satisfy the seven-key check while carrying an empty object. Corrected in four places, including the spec's design section, which has carried the same wrong claim since it was written. **The citation sweep was the largest yet and found the usual class again**: insertions into three `R/` files and three test files staled **62** live citations, of which the gate caught 4; the other 58 were repointed by content, and **nine were already wrong before this session** -- three places claiming "a no-change write returns at" pointed at a `column_hashes` assignment, six citing the not-identity list pointed at comment prose, and one claiming `meta$parquet_sha` pointed at a return-value field. Sixth consecutive session for that class. **TASK 22 DONE (2026-09-10) -- a reader that meets a manifest whose artifact list it cannot use now reconstructs the index instead of reporting an empty repo. EVERY PHASE E TASK THAT HAD TO RUN EARLY IS DONE.** Phase E later gained a sixth, Task 23, which does not run early -- it runs immediately before Task 11, the task it protects. **** The failure it removes is the quiet one: a manifest whose list has moved somewhere this build cannot see looks exactly like a repo with nothing in it, so every discovery command agrees and none of them errors. New `R/manifest-rebuild.R`, reached from inside `.datom_read_manifest()` and only when the caller is reading: one recursive storage listing to find artifact names, then each artifact's own `metadata.json` and `version_history.json` to rebuild its row, then the summary counters recomputed from those rows, then **one warning** naming which copy was rebuilt and pointing at the upgrade. **Two triggers, and they are the same two that make a writer refuse**: the artifact key is absent after the format conversion has run, or the document declares a format above what this build supports. Same evidence, opposite responses -- reads limp, writes stop -- so do not unify them. **Nothing is persisted, at any role.** Design 10.4 only required in-memory for a storage-only reader; writing the clone's copy from a read was available for a developer and deliberately not taken, because a read that quietly rewrote a repo's index is a larger surprise than the one it is fixing, and the recorded copy is repaired by the next ordinary write. **Four things a later change must not undo.** (1) The trigger is **absent**, never *empty* -- empty is what a brand-new repo and a truncated file both look like, so triggering on it would put a storage listing on every read of every healthy repo and hide corruption behind a plausible answer; a companion test pins that an empty **v1** manifest legitimately does rebuild, since the conversion leaves it with no artifact key at all. (2) A rebuilt row is stamped `kind = "table"`, because nothing in per-artifact metadata says what kind an artifact is until Task 7, and an untyped row is silently uncounted -- a rebuilt repo would list its artifacts while reporting zero of them. Task 7 owns that line and the comment at the site says so. (3) `current_version` is the version **recorded** in the history and never recomputed, and it is **not simply the newest entry** -- a write that reverts to content already in the history appends none, so selection narrows by `data_sha` and then by the `created_at` the history entry copies verbatim. Recomputing would reach for the identity code in exactly the situation a rebuild is for and could publish a version matching nothing in the history. (4) The artifact loop is `lapply()`, not `purrr::map()`. **Also shipped: `original_format` is now written into per-artifact metadata**, not only onto the manifest row, which is what made it recoverable; classified **not identity**, because the symmetric choice with its sibling `original_file_sha` would have re-minted a version for every imported table in every repo on content that did not move. **The audit's warning about the test surface was right, and stronger than it looked: all eight assertions that had to flip PASSED UNCHANGED after the code change, for the wrong reason** -- the mock storage in those tests had no listing, so the rebuild failed and the original refusal was re-signalled. A green run would have shipped a suite asserting the opposite of the design. Each flipped test now says AMENDED and states what did **not** change, since the reason they existed still holds: the outcome must be its own, never reworded into "could not read manifest" (AC32). Two new tests assert the writer still refuses on the same document a reader rebuilds, so the asymmetry is pinned in the one function that decides it. **Neither the writer's half nor R22.10's dispatcher guard was rebuilt** -- Task 21 and Task 6 already shipped them, as the audit found. **AC37(f) has one documented exception**: `last_updated` is the writer's wall clock at the moment it rewrites a row and is in no document, so the rebuild uses the version's own `created_at`; the test asserts every other field equal and this one present and plausible, and says so at the assertion so it reads as a decision rather than a gap. **Two R-level traps cost most of the debugging and are now in `dev/engineering-notes.md`**, both of which make a condition's class disappear on its way to a handler -- `stop(cnd)` inside one `tryCatch()` handler is caught by that same `tryCatch()`'s `error` handler, and `purrr::map()` re-signals a mapped function's condition as its own indexed error with the original demoted to a parent. The first turned every compatibility refusal into an IO failure; the second is why that loop is `lapply()`. Both go green in a suite that only checks something failed. **Four probes, each reverted and each naming what it reddened**: dropping the namespace-root strip on the storage listing reddens past testthat's failure cap, triggering on *empty* instead of *absent* reddens 9, removing `kind = "table"` reddens 9 across three files, recomputing `current_version` reddens 1. Tests 2965 -> **3050** (+85), FAIL 0 / WARN 0 / SKIP 0; `dev/check-spec.R` 9/9; `R CMD check` 0/0/0 on docs and code/documentation agreement. **Pathway impact: yes** -- a new route card, "Given a manifest whose artifact list this build cannot reach, still list the repo", plus one exception and a fifth step on the schema card. **Reviewed after it landed; three findings, two fixed and one recorded (tests -> 3053).** (1) The hardcoded `kind = "table"` on a rebuilt row was guarded by a comment, so the Task 7 revisit depended on somebody remembering -- and the failure it prevents is the one Task 6 exists to prevent: once a set exists, a rebuilt repo types it as a table, the set counters read zero, the artifact still appears in `datom_list()`, and nothing errors. It now has a forcing function in the rebuild's own test file, asserting the metadata builder emits **no** `kind`, with instructions for what to do when it fails. The review's "nothing fails" was slightly overstated -- `test-utils-sha.R`'s pinned fixture list does redden -- but it reddens in a test about identity hashing that says nothing about this line, which is the point. (2) `.datom_recorded_current_version()` fell back to the **newest** history entry when nothing matched the current content, which contradicted the test two lines from it pinning that three other unusable histories yield no version at all. Fixed in favour of the test, on the same trade the carry-forward rule already makes: a version describing different content is a wrong statement, and a wrong statement is worse than a missing one. The remaining ambiguous case still takes the newest **candidate**, which is different in kind because both candidates describe the current content. (3) **Accepted residual, recorded not fixed**: nothing memoises the rebuild, so it repeats on every call for as long as the index stays broken -- ~601 storage requests per command on a 300-artifact repo, twice in one call for a `datom_status()` on a repo broken on both sides, and the user sees it as datom hanging because the warning only arrives at the end. That is the cost the manifest exists to avoid (`dev/datom_specification.md:1694`), which is why the trigger is a broken index rather than anything a healthy repo can hit. Not memoised because a session cache is already deferred package-wide pending its invalidation design, so it would add session state to a library that has none in order to speed up a state the next ordinary write removes. Both fixes probed by reversion. **Four live code citations in the spec were stale again** (two in `R/read_write.R`, one in `R/utils-sha.R`, one in `tests/testthat/test-summary.R`) and fifteen test citations needed repointing because this change renamed tests in three files -- fifth consecutive session for that class. **ONE TASK ADDED, NOT NEXT: Task 23, `project.yaml` declares its format.** It sits last in the list but **executes immediately before Task 11**, because Task 11 starts writing `mode: product` into that file and Task 23 is what lets an older build notice -- a reading half cannot be added to a build that has already shipped, which is the same filter Phase E used. The durable part is the general rule it records: a **machine-written** document gets a **vocabulary check**, because an unrecognised key there *is* evidence a newer datom wrote it; a **hand-edited config** gets a **version number**, because an unrecognised key there is as likely a typo or a private note and refusing on one would block every write in the repo until somebody found it. `min_writer_version` already lives in `project.yaml`, so the file already carries policy an older build silently ignores; the number is what makes the next policy field enforceable rather than advisory. Three premises of the proposal were corrected in the task body: `project.yaml` is not the only document without a number (four others lack one; it is the only hand-edited one), the harm is a silent no-op rather than a corrupting write, and once a repo holds a set an older writer is already stopped by the vocabulary check -- so the window this protects is a product repo that does not yet hold a set, which is the state right after init. One verified finding the proposal left open: a single gate does **not** cover every read, because `.datom_resolve_data_location()` re-reads the file after a git pull, later than the parse the gate would sit on. Nothing about Task 23 blocks Tasks 7 through 10. **TASK 7 COLD-START AUDITED 2026-09-10: startable.** Two of its own statements were wrong and are corrected -- it cited a set-metadata field matrix in design.md section 4 that does not exist (R1.3 is the list), and it said to keep `document_sha` out of the metadata document, which contradicts R1.3 and would break the read gate, since the stored payload must be verified before parsing and the reader has nowhere else to get the expected hash. **The finding that would have done real damage: adding `kind` re-mints a version for every existing table**, verified by computing the hash both ways -- and it is an accepted owner decision from 2026-08-23 that lived only in a dated Decisions row, so a fresh session would have read it as a defect and reached for one of the two fixes that same decision closed off (`kind` cannot leave the hash, or a table and a set could share a version identity; and it cannot live only in the manifest, because the cross-kind check reads it from per-artifact metadata). Now in the task body, and **P36 was rescoped** because its first clause asserted the opposite. Three additions: Task 7 **legitimately moves two pinned identity goldens**, which is the first licensed exception to "if a golden fails, the code drifted"; the tests it reddens are **six** sites, now enumerated with live citations; and one of those six is a **handoff** rather than a failure to silence -- it exists to make Task 7 change the rebuilt manifest row's hardcoded `kind`, and deleting it instead leaves a rebuilt set reported as a table with the set counters reading zero. Also recorded: the set builder's two easy-to-copy-wrong values, that `document_sha` is inert until Task 9 so its plumbing is a choice with a stated default, and that all three acceptance criteria are assertable at Task 7 on the builder's output only. Next after it was **Task 7 (`kind` in per-artifact metadata, the set metadata builder, `document_sha` in the history entries)**, now done -- see the top of this cell. Task 22 left two things under it, both since discharged: the hardcoded `kind` line in the rebuild, and two classification forcing functions that go red the moment a builder gains a field. **TASK 21 DONE (2026-09-09) -- a write now stops when this build cannot fully account for the repo it is writing into.** Four refusals, one function, one moment: `.datom_check_write_entry()` in `R/forward-compat.R`, called after `datom_write()`'s connection check, above both routing returns, above any hashing or local write, so a refusal leaves nothing half-written. In order: the repo may declare a **minimum writer version** in `project.yaml` and an older build is refused (optional field, **absent means no limit**, so no existing repo changes behaviour); the **manifest's declared format** is checked and then converted, through the one shared reader, with the refusal worded for a write rather than a read; if the conversion **cannot reach an artifact list** the file is not overwritten; and a **top-level field this build cannot classify** refuses the write, naming it, on the manifest, on each of its artifact entries, and on each per-artifact `metadata.json` the write will touch. All clone copies -- local file reads, no network, because git is written first and gates the mirror, so a colleague's newer document arrives in the clone. **`.datom_check_write_schema()` is gone, absorbed rather than kept beside the new sequence**: two doors reading the same file at the same moment have no way to say which runs first, and the order is load-bearing -- a policy refusal belongs before anything about format is considered, and the format check must precede the conversion because there is no conversion step for a version this build has never heard of. **Two things a later change must not undo.** (1) The vocabulary check runs on the **converted** manifest, never the raw one: a pre-rename document keeps its list under the old key, so checking the raw document would refuse every existing repo -- the forward path deadlocking by a different route than the one the requirement warns about. (2) The shape refusal is worded "still absent **after** the conversion ran", not "absent", for the same reason. **One deviation from the written design, recorded rather than quiet**: design 10.7's step-1 fetch is not implemented. A fetch updates remote-tracking refs and not the working tree, so it cannot make any of the four checks read a fresher document -- each reads a file the route is about to write. The only thing that would is the behind-comparison, which **aborts**, and adding that would newly refuse an offline write that works today while making the mirror-everything route depend on `git2r` (a **Suggests** package) despite touching no git at all. **The staleness it was aimed at is solved directly**: `.datom_sync_metadata()` pulls from the remote as its first act, after the door has read, so that route re-runs the whole sequence against what the pull left on disk -- free, since every step is a local read and none mutates anything. That also **closes the hole Task 6's purity audit handed forward**, since the same route copies a per-artifact metadata document straight to storage and nothing had checked its format; the entry now schema-checks each per-artifact document too. **`tables` sits on the manifest vocabulary marked retired**, which is the append-only rule made mechanical: a build that pruned an old name would meet an **older** document, fail to place a key it should know, and refuse it -- blocking the upgrade direction, the one direction that always has to work. **AC36(c) is deliberately not implemented and not owed here** -- "setting a floor above the setting build's own version is refused" is a guard on the *setter*, and the setter is deferred to its own verb; the Backlog row for that verb owns it. **Seven probes, each reverted and each naming what it reddened**: the vocabulary check 6 tests, the floor 3, the shape refusal 1, emptying the per-artifact loop 5 (the clause an implementation checking only the manifest would fail), pruning the retired name 1, the per-artifact schema check 1, the re-check after the pull 1 -- the last three would each have shipped green without their probe. Task 21's refusal also makes **all three** of Task 20's coded carry-forward levels unreachable through `datom_write()`, which was predicted; the four round-trip tests now hold the door open with a named mock, and the genuinely live surface is the version-history entry, which the refusal does not scope. Tests 2905 -> **2959** (+54), FAIL 0 / WARN 0 / SKIP 0; `dev/check-spec.R` 9/9; `R CMD check` 0/0/0 on docs and code/documentation agreement. **Reviewed after it landed; three findings, two fixed in a follow-up commit and one recorded (tests -> 2965).** Both fixes were the same route: `datom_validate(fix = TRUE)` reaches storage by calling `.datom_sync_data_metadata()` directly, so it never passed `datom_write()`'s door -- no floor, no vocabulary, no per-artifact format check, while NEWS claimed the checks covered every write route. Bounded damage, because that route copies rather than rebuilds, but the floor exists partly for reasons that are *not about format* and cannot be enumerated ahead of time, so "it only copies" does not license skipping it. **Fixed by putting the entry on the shared function rather than on each caller** -- this is the third task running in which this route has been the gap, which is the more durable lesson: a repair verb that reaches storage without going through the write verb will keep being missed. The second fix was the wrong verb on that route's manifest read ("cannot read" on a write), and **a probe caught its test asserting the wrong thing**: the entry refuses a too-new manifest a few lines earlier with the right verb already, so the test passed with the fix reverted until it was made to mock the entry out. The third finding is an **accepted residual, now recorded rather than implied as solved**: on the table-write route the pull happens inside the push at step 7, after the metadata document is written and the manifest edited, so the door's answer can be stale there and re-checking cannot help -- the backstop is the push, which aborts on rejection or conflict with the storage steps after it. **Pathway impact: yes** -- a new route card, "Given a write request, decide whether this build may write here", and the schema card's write-side paragraph now points at it. **NEXT: Task 22 (the self-healing manifest rebuild plus persisting `original_format`).** Nothing is owed before it, and Task 21 leaves four things under it: the reader now needs the **opposite** response to the two conditions the writer refuses on (warn once and rebuild, not refuse -- do not unify them, the asymmetry is the reads-limp/writes-stop rule); the rebuild branch has a stated place to land, since Task 6 kept the conversion as its own statement inside the shared reader precisely for that; the Task 5 assertion in `test-query.R` that a too-new manifest aborts at all five readers has to be **amended**, not just re-read; and persisting `original_format` into per-artifact metadata will trip the classification forcing function deliberately. **TASK 20 DONE (2026-09-08) -- a field this build cannot place now survives a write instead of being deleted.** Writing a table rebuilds its metadata document and its manifest row from scratch, which silently discarded any top-level field the running version had never heard of -- exactly what a document written by a newer datom looks like after a pull. New `R/forward-compat.R` carries such fields across the rewrite. **Only two of the three levels needed code**: the manifest's *top level* already survives, because that document is read, edited and written back rather than rebuilt -- so the guarantee there is a property of editing, and it is tested anyway because a refactor to assembling a fresh document would remove it without failing anything else (proven: building from the skeleton instead of reading the file reddens that test). **The narrowness is the design, not a shortcut**: only unplaceable fields are carried, so a field datom knows still disappears when the write does not set it. `original_format` decides it -- a table imported from a CSV and later written straight from a data frame has no format to declare, and a blanket "keep whatever the new document omits" would leave that claim standing against a version it does not describe. A wrong statement is worse than a missing one; three tests redden under the blanket form. The merge sits **after** the version identity is computed, beside the `parquet_sha` assignment, so a carried field cannot reach a hash even if the identity list were later got wrong. The prior document is read from the **clone**, not storage -- it is the file being overwritten, it costs no network call, and it is where a colleague's newer document lands. One implementation detail that will bite anyone tidying it: `.datom_metadata_known_fields()` is a **function** rather than a stored vector because `R/` is sourced alphabetically and that file sorts before the one holding the two halves it joins, so a constant would be built from values that do not exist yet and the package would not install. Two forcing functions ship with it, one per vocabulary, each asserting that every field the corresponding builder writes is classified; **Task 9 will trip the row one deliberately** when it adds the set row's member count. **Seven probes, each reverted**: removing either merge reddens both halves of its round trip, git and storage; rebuilding the manifest reddens the top-level test and the row test; rebuilding a version-history entry from known names reddens both halves of the fourth-surface test; widening the merge reddens all three narrowness tests; an unclassified field added to either builder reddens that builder's forcing function naming the field -- the metadata one while every pinned identity hash stays green, which is the allowlist's failure direction and visible only there. **A review after it landed added two things, neither a defect.** (1) **A fourth surface the requirement does not name: an entry in `version_history.json`.** Safe by the same edit-don't-rebuild property (the list is read and the new version prepended, so an existing entry is never rebuilt) so it needed no code -- but pinned by a test, because Task 7 and Task 15 both add fields to those entries and a build that normalised an old entry to today's field set would destroy exactly those. The general rule worth carrying: **a guarantee that rests on the shape of a function rather than on a check needs a test, because a refactor removes it without failing anything else.** (2) **`document_sha` is the one name this build classifies and never writes**, verified mechanically (17 names known, 16 emitted), which makes it invisible to carry-forward -- the rule only rescues names it cannot place. A metadata document arriving with that field would lose it silently, since it takes no part in identity. Unreachable today and under Task 7 as specified, which puts it in `version_history.json`; recorded because moving it into `metadata.json` later would be a decision with a consequence rather than a tidy-up, and because the shape generalises: **classifying a field ahead of writing it buys identity stability early and costs carry-forward protection until a builder emits it.** **That last point is now guidance plus a guard** (owner-approved, developer-facing only -- a test and two dev documents, nothing in a user's session): the rule "classify a field when you start writing it, never earlier" is in `.github/copilot-instructions.md` and `dev/engineering-notes.md`, and the missing converse arm was added to both vocabularies that lacked one. The asymmetry is worth knowing: the identity list has always asserted that nothing on it is a field datom never writes, while the not-identity list and the manifest-row list did not -- and `document_sha` sits on the first of those two. Both now do, with an exception vector holding exactly one name and its reason, so the next early classification cannot happen without meeting that reason. Three probes, each reverted: a junk name on either list reddens that list's arm naming the field, and emptying the exception vector reddens it naming `document_sha`. The same edit fixed two stale claims in the conventions file -- the carry-forward bullet said three levels where there are four, and its heading said "two pitfalls" while carrying three. Tests 2867 -> **2905** (+38), FAIL 0 / WARN 0 / SKIP 0; `dev/check-spec.R` 9/9; `R CMD check` 0/0/0. No pathway impact. The task's five open calls were **explicitly approved** rather than defaulted on silence, and are recorded in Task 6's shape in `tasks.md`. **Also found and fixed: three live code citations in the spec were already wrong before this change** (two lines cited as `datom_write()`'s return value, one as its in-pipeline manifest read), verified against the previous commit before repointing -- check 5 only asserts a cited line is not blank, so the standing instruction to re-read citations by content after any insertion into `R/` is now proven for a third consecutive session. **Next after it was Task 21 (the writer refusals), now done -- see above.** Task 20 left three things under it: the vocabulary it will read already exists as the union of the identity and not-identity lists and is the same union the carry-forward reads, so the two cannot disagree about what "unrecognised" means; which copy to read is settled (the clone); and the refusal will make two of Task 20's three levels unreachable through `datom_write()`, which is expected -- it inspects top-level keys only, so an unfamiliar field inside a manifest row never trips it, and `datom_validate(fix = TRUE)` mirrors to storage without passing the door at all. **THAT SENTENCE WAS WRONG AND IS CORRECTED BY THE TASK 21 COLD-START AUDIT (2026-09-08).** R23.1 scopes the refusal to three documents and names manifest **entries** as one of them, so "top-level keys only" means do not descend into a value, not ignore entries. So **all three** of Task 20's coded levels become unreachable through the write door, and the case that stays live is the **version-history entry**, which is not in that scope list at all -- which makes the test written for it load-bearing rather than belt-and-braces. Swept in all four places the wrong claim had reached. The audit also found that Task 20 accidentally built **two of the three** vocabularies Task 21 needs (the per-artifact one and the manifest-row one), leaving the manifest's top-level list to write -- five names, verified against every top-level assignment in `R/`. **TASK 6 DONE (2026-09-08) -- the manifest's artifact list is renamed and every existing repo still reads.** `manifest$tables` is now `manifest$artifacts`, every entry carries `kind`, both manifests and every per-artifact metadata document declare `schema_version: 2`, and a write into a repo whose format this build does not know is refused before any hashing or file write. The thing that makes the rename safe is the conversion, in a new `R/manifest-upgrade.R`: a **read** converts an older document in memory and leaves the file alone (a storage-only reader has no git and could not repair a repo it was refused from), and a **write** converts the file itself and then stamps the version it reached, so no file is ever half in one shape and half in the other. **Five counters were widened, not three** -- the three in the stored `summary` block plus `datom_summary()`'s and `datom_status()`'s independently counted numbers -- each filtering on `kind == "table"`, with `total_sets` and `set_count` as the new numbers beside them; no existing counter changed what it counts. **Every guard was proven to fail before being trusted**, per this spec's own rule: disabling the conversion reddens all four frozen-fixture readers and both tolerance tests; removing the `kind` filters reddens five tests across the three counting sites; moving the write door below `datom_write()`'s routing returns reddens the two non-table routes; and dropping the conversion from the entry updater reddens the ten assertions that say a pre-existing table is still counted. The five open calls were taken at their stated defaults. Tests 2748 -> **2836** (+88), FAIL 0 / WARN 0 / SKIP 0; `dev/check-spec.R` 9/9. **Reviewed after it landed, and four findings were fixed in a follow-up commit** (tests -> **2861**, and **2867** after two loose ends a second review pass found): three counters aborted on a manifest entry the conversion step deliberately preserves -- `datom_status()` worst, since its count sits outside the handler that lets it survive a manifest it cannot trust; a conversion that persisted said nothing, so `datom_validate(fix = TRUE)` could blank discovery for every colleague on an older datom without telling anyone; `stop(read$error)` on the mirror route could fire as `stop(NULL)` with an empty message; and `datom_list()`'s empty result was missing a column its populated rows carry, so `rbind()` failed whenever one side was empty. That last one **reversed** one of Task 6's five defaulted calls -- the reason recorded for it ("changes a public shape nobody asked to change") was already spent, because the same commit added a `kind` column to that shape. The counters' fix routes all four kind selections through one helper rather than repeating a guard four times. **The purity audit E2 required is DISCHARGED** (2026-09-08), run last on owner's call so it audited what actually shipped rather than a state about to change. Both questions it existed to answer were settled mechanically: deleting the artifact key from the shared reader reddens 64 assertions across 36 tests, so the ~40-fixture sweep did not cost the suite its teeth; and making an untyped entry abort inside the selection helper reddens exactly one test -- the one that deliberately passes an untyped entry -- so no swept fixture ended up declaring the version while omitting `kind`, which would have counted as zero artifacts. Two tests were tightened for passing whatever the code did (a print test asserting a `Sets:` line exists, true with a count of zero; a pattern test asserting an empty result from a non-empty fixture, equally true if the manifest was never read). Nothing else changed; `R CMD check` 0/0/0. **A second review pass then found two loose ends, both closed**: the zero-row frame was still a column short when `include_versions = TRUE` (opt-in, so a caller can ask for it and get an empty repo -- fixed in the frame, not in the comment that wrongly said they could not), and the metadata-only write route pulls from the remote immediately after the door, making the door's manifest check stale on that route -- harmless here since it writes no manifest, and now recorded as a decision Task 21's entry sequence has to make. Tests **2867**. **One gap named rather than fixed**: the write door inspects the manifest only, so `.datom_sync_metadata()` copies a per-artifact document from the clone to storage unchecked -- pre-existing, narrowed by Task 6 rather than introduced, and it belongs with Task 21's entry sequence, which already reads all three documents at the door. **The submission freeze still holds** (0.1.2 is in flight, so `main` must keep matching what CRAN received): PR into `dev`, never `main`. Read `dev/README.md` "Branching During CRAN Submission" for current branch heads before assuming anything about them. **Tasks 0-7 and 18-22 of 23 done -- the count and the list are both kept true at the top of this cell rather than here, since this sentence has gone stale twice. Phase E (Tasks 18-22, plus Task 23 added 2026-09-10) added 2026-08-23: forward-compatibility controls for 0.1.1. EXECUTION ORDER IS NOT TASK ORDER -- `18 -> 19 -> 5 -> 6 -> 20 -> 21 -> 22 -> 7 -> 8 -> 9 -> 10 -> 26 -> 24 -> 25 -> 23 -> 11 -> 12 -> 13 -> 14 -> 15 -> 27 -> 28 -> 16 -> 17`; Phase E was appended rather than inserted to avoid a third renumber, and its preamble says why each edge exists. Next after it was Task 21 (the writer refusals) -- Task 6's purity audit is discharged and Task 20 has landed, so nothing is owed before it. **Cold-start audited 2026-08-29 and startable**: seven live code citations were repointed (only three were blank enough for `check-spec.R` to catch, and four were wrong before that session), the six tests plus the frozen `tests/testthat/fixtures/manifest-v1.json` that must STAY on the old key are now named with locations, and where the old-format conversion hooks into the shared reader is stated (read, version check, conversion, return). Two decisions settled: the frozen upgrade steps go in a new `R/manifest-upgrade.R` (a released step is never edited and they accumulate, so a file that is entirely "never edit these" is easier to protect), and `.datom_manifest_skeleton()` stamps `schema_version: 2` itself -- which covers only the no-file path, so an existing old-shape manifest still gets its number from the upgrade step. **Task 5 done** (`2026-08-29`): manifest reads collapsed from five places to one (`.datom_read_manifest()`) and hand-built empty manifests from three to one (`.datom_manifest_skeleton()`), both in `R/sync.R`. Contract-neutral -- nothing on disk changed, the key is still `tables`, no new export. It exists so Task 6's old-format upgrade has ONE place to live instead of five. **The design point is which failure is returned and which is thrown**: an unreadable manifest comes back as data, so each of the five callers keeps its own policy verbatim (two abort, `datom_status()` reports it unavailable and carries on, two clone readers fall back to an empty manifest), while a repo written by a NEWER datom is thrown from inside the reader -- so no caller can soften "upgrade datom" into "could not read manifest", which is what Task 4 had to hold with a comment at three sites and what `datom_status()` would otherwise have downgraded to a warning. The failure record carries the **whole condition** rather than its text, because the clone readers re-signal the original so a corrupt local manifest still fails with the parser's own error; text alone would lose the condition class the suite asserts on. `absent = TRUE` is a **positive claim** made only for the clone copy, where checking is free -- for storage it would cost an extra request on every read to distinguish a missing object from an unreachable store, and no caller distinguishes them. New frozen `tests/testthat/fixtures/manifest-v1.json` (plus a `fixtures/README.md` saying these are never edited) is the mechanical evidence that pre-rename repos still list their contents. **Both new guards were proven to fail before being trusted**: pointing `datom_list()` at `artifacts` reddens the fixture test, and moving the schema check inside a handler reddens three tests including a pre-existing one. `datom_status()`'s `read_ok` flag moved into the shared record rather than being dropped -- it existed because a manifest parsing to `NULL` must still count as read. The entry updater keeps its own direct read, since a refusal mid-write stops a write halfway rather than at the door; only its empty shape moved. Tests 2686 -> **2740**. No pathway impact. **One operator-facing fix followed it** (`2026-08-29`, not a task): `cli` abort messages carry terminal colour and hyperlink escape codes, and `conditionMessage()` returns them -- harmless when the message is printed, literal `\033[31m` noise in the three fields datom **returns** error text in (`datom_status()`'s, `datom_sync()`'s `error` column, and the per-table metadata sync result). `cli::ansi_strip()` on the stored copies only; printed messages keep their colour. Verified pre-existing by running the previous commit with colour forced on -- byte-identical string. Each test forces colour ON, since with it off cli emits no escapes and the assertion is vacuous, which is how this reached 0.1.1. Recorded in `dev/engineering-notes.md`. Tests 2740 -> **2748**. **Task 19 done** (`2026-08-28`): identity hashing selects metadata fields by **allowlist** instead of hashing everything-except-seven ([#100](https://github.com/amashadihossein/datom/issues/100)). The defect it removes: a build that has never heard of a field cannot know it was meant to ignore it, so it folds the field into the hash, disagrees with the recorded version, and reports a change on content that did not move -- on **every run**, not once. So any future bookkeeping field would have cost every older build a spurious version forever. Two namespace-level constants in `R/utils-sha.R` (`.datom_metadata_identity_fields`, ten fields seeded to exactly what was hashed before; `.datom_metadata_excluded_fields`, the old local `volatile` vector promoted so Task 21 can read the union as its write-side vocabulary). **Landed in two commits on purpose**: the pinned goldens first, under the unchanged code, then the change -- a pin computed in the same edit as the change it polices is worth much less. Both pins hold unchanged afterwards, which is the evidence that every existing identity is byte-identical; six document shapes were also compared old-vs-new before implementing. **An allowlist fails the OPPOSITE way from a denylist and that direction is the dangerous one**: a new field left unclassified is silently *excluded*, so identity quietly stops responding to real content. The classification test therefore derives its field inventory from the builders rather than hardcoding names, and was **proven to fail before being trusted** -- a junk builder field turns it red while every pinned hash stays green, which is exactly why no other test could catch it. It carries a converse arm too (nothing in the identity list may be a field datom never writes), which mechanically blocks the rejected shortcut of allowlisting `name`. **The function is now two functions**: selection, plus `.datom_metadata_sha_from_fields()` which sorts/serialises/digests. Reason: the locale-independence property (Property 13) needs a fixture whose names collate differently across locales, an allowlist drops all such names, and **all ten real field names sort identically under `C` and `en_US.UTF-8`** -- so the test would have passed while asserting nothing. Rejected the tidier-looking alternative (declare the list in radix order, drop the sort) because hash stability would then depend on how a constant is typed. Three tests were quietly going vacuous (fixtures built from `name` / `author` / `x`) and are re-fixtured onto real field names. The old `metadata_sha` golden `59f1f1d9...` pinned a fixture carrying `name` -- a field no builder emits -- so it was **reused** for the property that changed it (an unknown extra field is ignored), re-pinned to the no-`name` value. Docs swept in the same commit: two `dev/engineering-notes.md` entries inverted under an allowlist, and `vignettes/design-version-shas.Rmd` prints the algorithm and was **already stale from Task 4** (five excluded fields listed where there were seven). Deliberately NOT paired with [#98](https://github.com/amashadihossein/datom/issues/98) despite the backlog suggesting it: removing `jsonlite` from the identity path changes the hashed bytes, so every recorded identity moves -- the direct negation of this task's behaviour-preservation claim. Tests 2675 -> **2686**. No pathway impact. **Task 18 done** (`2026-08-26`): the `.datom_check_git_current()` fetch-failure defect ([#104](https://github.com/amashadihossein/datom/issues/104)) -- `return(invisible(TRUE))` sat inside the `tryCatch` error handler, so it returned from the **handler** and execution fell through to compare `HEAD` against **stale cached** upstream refs, hard-aborting an offline developer for being behind a remote they cannot reach. Fixed by capturing the fetch outcome and branching outside the handler. Two no-mock tests added (a real broken remote URL, not a stub): cached refs **ahead** + failed fetch, which **errors against the unfixed function** and so is proven non-vacuous, and cached refs **level**, which passes either way and exists to pin an accident as deliberate. The pre-existing "tolerates network errors gracefully" test never caught this because its fixture has **no upstream branch**, so the function returned before the comparison. Two notes carried into `dev/engineering-notes.md`: the handler-`return()` trap is a general R gotcha (the code after the `tryCatch` still runs, and the warning still prints, so the log looks correct), and a `cli_alert_warning()` is a **message** -- `expect_warning()` fails and reads as "it did not warn at all". **The fix deliberately narrows a live check**: an offline write now proceeds without knowing it is behind, accepted because `.datom_git_push()` pulls and aborts on a rejected push before the storage steps, so a write cannot land on storage from a stale base. Tests 2660 -> **2664**. No pathway impact. Five items are in 0.1.1 because they cannot be retrofitted -- allowlist hashing, carry-unknown-fields, the writer refusals, the floor's reading half, the rebuild -- and deferring any one strands every 0.1.1 install forever. The aim: 0.1.1 is the LAST release needing a transition plan. Writer refusal is a VOCABULARY check (refuse a write when a datom-owned document carries a top-level key this build cannot classify) chosen over a declared writer floor as primary because it cannot be forgotten; the vocabulary list is APPEND-ONLY with the same weight as a frozen upgrade step, and the two mechanisms cover complementary sets -- vocabulary catches additions and renames, the schema number catches removals, type and meaning changes, and restructures. Manifest schema numbering is DESIGN A: the number keeps bumping and stays true to the shape, and a too-new manifest makes a READER warn and rebuild while a WRITER refuses; Design B (freeze the number, dispatch on shape) was rejected because it cannot distinguish a truncated manifest from a future-shaped one. Every write-side refusal binds 0.1.1 FORWARD ONLY -- 0.1.0 gets a NEWS entry, not engineering. Historical Decisions rows are now FROZEN: a renumber adds one shift record and touches nothing else.** **Original Tasks 0-4 detail below.** **Tasks 0-4 of 17 done. NEXT: Task 5 (one manifest reader + one skeleton builder) -- contract-neutral, and it exists so Task 6 (the `manifest$tables` -> `manifest$artifacts` rename) has ONE place to put the old-format upgrade instead of five. Phase B was a single task until 2026-08-23 and is now two; old Tasks 6-16 shifted to 7-17. Task 6 still CARRIES ESCALATION E2, so per rule 5d surface its recommendation in that chunk's checkpoint message before implementing. Read `.kiro/specs/datom-sets/tasks.md` first: its "Where things stand" block at the top has the current state and the per-commit gate, and Phase B's own preamble says which half is which. Nothing is awaiting the owner.** **The E2 design audit (2026-08-23) found a BLOCKING gap: nothing upgraded an existing repo's manifest.** Every repo written so far keeps its artifact list under `tables` and declares no `schema_version`; the reader-side check tolerates that as v1, so the repo passes and then meets a reader looking for `artifacts` -- `datom_list()` empty, `datom_summary()` and `datom_status()` zero, no error, on every repo at once. `datom_read()` is unaffected, so it is a discovery blackout rather than data loss, and the disqualifier is the silence rather than the severity. Neither thing that might have self-healed it does: the entry updater stamps no version, and a no-change write returns before the manifest is touched. Remedy is new requirement **R22 -- reads upgrade in memory, writes upgrade on disk**, decided on *who can act*: a loud refusal would be the usual preference, but a reader holds storage credentials and no clone, so refusing a v1 manifest strands the people least able to repair it. Three alternatives rejected with reasons in design.md 15 (inline `%||%` fallback; refuse-plus-repair-verb; clean break on pre-release grounds -- the `datom-cv1` precedent does not carry, since that break was in content identity which a re-export regenerates, while the manifest has no rebuild verb). Also decided: **no missing-`kind` fallback** in the counters (a safety net that fires only when a read path skipped the upgrade would hide the one failure we want noisy), and **"surface `kind`" means a column for `datom_list()` and a `set_count` aggregate for `datom_summary()`**. Three audit corrections of substance: **five** counters widen to include sets rather than three; the `kind` column must reach `datom_list()`'s two empty-result returns; and the write-side refusal must sit above the two routing returns in `datom_write()`, since one of them mirrors the whole manifest to storage without reaching the manifest-writing step. Two existing tests are **v1-compatibility** fixtures that must not be swept to the new key, and the clone-copy readers need a third; the evidence moves into a frozen `tests/testthat/fixtures/manifest-v1.json` so "do not sweep this" is structural. **`dev/check-spec.R` gained check 8** -- every `Task N` reference must resolve to a task that exists, which the renumber made worth guarding; verified non-vacuous by reintroducing a dangling reference. **E2 is discharged without a model switch** (already on the most capable model available): the flag is answered by an independent close review of the spec before implementation, and the purity-audit half of the trigger still applies after Task 6 lands. **The `datom-sv1` goldens are published as of Task 2, so the set encoding is FROZEN** -- a failing golden means the code drifted, and a deliberate change is a `datom-sv2` bump with a new `hash_algo`, not an edit. Issue [#89](https://github.com/amashadihossein/datom/issues/89): second artifact kind -- versioned, citable **sets** of datoms (a reference layer, not a data layer). Branch `spec/datom-sets` off `dev`, PR [#97](https://github.com/amashadihossein/datom/pull/97) into `dev` (submission freeze). Prerequisite [#95](https://github.com/amashadihossein/datom/issues/95) landed separately via PR #96. **17 work tasks** (numbered 1-17, plus Task 0 for the spec itself; Phase B split 2026-08-23); two flagged for **Model Escalation** at plan time: Task 2 (`datom-sv1` canonical set-content hash -- gates the golden vectors) and Task 6 (`manifest$tables` -> `manifest$artifacts` -- **9 sites across 4 files** (3 write + 6 read) before Task 5 collapses the read half to one, silent writer/reader-disagreement failure mode). Independent review resolved 12 findings (`6954c46`); spec delta D1-D8 applied (`2944a8c`) recording that the **`mode: product` repo is the joint repo** (data + code + `renv.lock`, one commit graph) and adding `datom_repo_commit()` + `datom_write_set(include_paths=)`. A final adversarial review fixed 5 blockers + 15 lesser findings (`71f7d96`), and `dev/check-spec.R` now gates spec structure mechanically (`15f6401`). **Task 1 done** (`2026-08-17`): stale docstrings swept, three relative-key helpers added, 16/17 key sites migrated, [#98](https://github.com/amashadihossein/datom/issues/98) filed. Tests 2460 -> **2482**. Two decisions logged the same day: the Task 1 path-traversal guard **stays** (scope deviation approved), and **reader-side version diff needs no schema change** -- `version_history.json` already carries `data_sha`, so a git-less reader diffs two versions with three small JSON reads; per-member digests and moving tags into metadata are both rejected in design.md 15, the former deferred as additive-and-volatile. **E1 design review COMPLETE (2026-08-17)** -- the encoding is sound (domain separation, framing, radix collation, `id`/`tags` separation all verified) and Task 2 is cleared. Four deltas applied: member order out of identity, canonicalize-before-write, the `document_sha` byte-identity rule, no Unicode normalization, plus the empty-`strset` pin. Then a 19-finding sweep, whose decisive item was the sv1 pseudocode existing in **three** files with only one fixed. Owner decisions: **tidy-then-validate** (handle trivial errors silently; refuse only what needs intent guessed), the file sorting members by name while the hash sorts by digest, and **same name at different versions is legal** (the duplicate check keys on the full `id`). `dev/check-spec.R` gained a duplicated-content check, verified non-vacuous by reintroducing the defect. **Task 2 done** (`2026-08-18`): `datom-sv1` shipped -- new `R/hashable-set.R` (hash-of-hashes over three primitives, no serializer in the identity path, every collection sorted and deduped), new standalone `dev/datom_sv1_reference.R` (44 self-tests, prints the goldens, normative byte rules), `tests/testthat/test-hashable-set.R`, and `cv1-reference-parity.yaml` extended in place to cover both regimes on x86_64 + arm64. Goldens hard-coded and cross-checked against an independent SHA-256 tool; intermediates pinned too, so a divergence names the stage. No new export. One genuine find, caught by a test being written: **a named list in a value position had to be refused**, because element-wise `list(b = "c")` and `list("c")` are indistinguishable, so `{"a": {"b": "c"}}` would have hashed identically to `{"a": ["c"]}` -- one `data_sha` and one storage address for two different payloads. Tests 2482 -> **2572**; `R CMD check --as-cran` 0E/0W (3 environmental NOTEs: new submission, unverifiable clock, old HTML Tidy). **Task 3 done** (`2026-08-21`): the JSON **write** export was dropped before implementation (owner-decided -- `datom_write_set()` is how writing is spelled, so no consumer remained; revival trigger is in the Backlog below), leaving one export, `datom_storage_read_json()`. The substance turned out to be the guard rather than the wrapper: **no relative-key validator existed**, so `.datom_validate_rel_key()` is new. It catches two failures that differ in kind -- a `..` segment or leading `/`, which escapes the namespace on the local backend where the key is pasted into a path, and a **full key passed where a relative one belongs**, which does not error today but resolves under `{prefix}/datom/{prefix}/datom/` and reads to the caller as a missing object rather than a malformed key. The full-key test keys on a `datom` path segment, exact rather than heuristic because `datom` is a reserved name. Deliberately **not** folded into the Task 1 key builders: those compose keys from already-validated parts, so the check would be dead code -- the distinction is composed-from-parts versus supplied-whole-by-a-caller. Absent keys abort with one message on both backends (an upfront existence probe, since S3 would otherwise name the full key -- the exact transformation a confused caller got wrong). No role check, now pinned by a positive test so the family's policy-free symmetry cannot break silently. Tests 2572 -> **2612**. **Task 4 done** (`2026-08-21`): the reader-side `schema_version` check -- a repo written by a newer datom now stops with an upgrade message instead of returning an empty repo, which is what makes the `artifacts` rename the **last** transition able to degrade silently. New `.datom_check_schema_version()` + `.datom_supported_schema` in `R/utils-validate.R`, asymmetric (refuse newer, tolerate older, absent means v1), plus `schema_version` and `document_sha` into the `volatile` list -- inert for existing documents, since nothing carries either field yet, so no version is re-minted. Wired into **six** call sites, not the four the task named: the two extra are `datom_sync_manifest()` and `.datom_status_input_files()`, which read the **clone's** manifest, and that copy goes ahead of the installed build whenever a collaborator upgrades and this developer pulls. The four in-pipeline local reads stay excluded on a stated principle -- the check belongs where a document enters datom, so a refusal happens before work starts rather than partway through a write. **The real work was placement, not the check**: three of the six readers wrap their read in error handling that softens failures, and a check placed inside it reworded the upgrade instruction as "could not read manifest" -- `datom_status()` went further, downgrading it to a warning and continuing, so that one command would have stayed silent while the other five stopped. All three now read inside the handler and check outside it, with a test pinning that an ordinary storage failure is **still** tolerated. Two condition classes (`datom_schema_unsupported`, `datom_schema_invalid`) make "all six behave the same" provable rather than asserted. Spec code citations were re-derived by content afterwards, including every site in Task 6's checklist. The **write-side** check is deferred to Task 6, where the write steps are in view. Tests 2612 -> **2660**. | [.kiro/specs/datom-sets/](../.kiro/specs/datom-sets/) |

### Drafts (queued, not active)

| Draft | Doc | Captured | Notes |
|-------|-----|----------|-------|
| datomanager Phase 19: gov_migrate_data() | [draft_managed_migration.md](draft_managed_migration.md) | 2026-05-02 | Governed migration verb. Requires gov; atomic copy + `ref.json` switch + `migration_history.json` record; cross-backend s3<->local. datom Phase 22 (storage extension API) shipped 2026-06-10 -- the six `datom_storage_*` / `datom_repo_*` exports are the stable platform surface Phase 19 calls into. datomanager package scaffold is the remaining prerequisite. See draft for Phase 19 spec and acceptance criteria. |

### Completed Phases

| Phase | Completed | Tests | Summary |
|-------|-----------|-------|---------|
| Spec: datom-cv1-identity | 2026-07-26 | 2443 | Issue [#72](https://github.com/amashadihossein/datom/issues/72): **table identity redefined** as a canonical hash of a table's *values* (`datom-cv1`) instead of a digest of its parquet bytes -- serialization-based identity moved with the writer, so an `arrow` upgrade minted spurious versions. **Changes the on-disk contract; no migration path (pre-release).** Three SHAs now: `data_sha` (content identity, also the storage address), `metadata_sha` (the version), `parquet_sha` (stored-object integrity, verified on read *before* parsing). `original_file_sha` is file provenance, not identity. New `R/hashable.R` holds the single column classifier + single recourse source that the hash gate, the encoder, the new export `datom_check_hashable()`, and the vignette recourse table all bind to, so advice cannot drift from behavior. Also: `metadata_sha` field ordering switched to `method = "radix"` (locale-independent) with `parquet_sha`/`column_hashes`/`size_bytes` added to the volatile set; `column_hashes` persisted as an ordered per-column index (`data_sha` re-derivable from it, and the anchor for #73's `datom_diff`); full-history version dedup (no duplicate `version`, so `datom_read(version=)` can never be ambiguous); `parquet_sha` carry-forward on `metadata_only` and reuse-without-overwrite on revert-to-older; ingestion allowlist for `datom_sync` (flat tabular only); `file_sha` -> `original_file_sha` nomenclature sweep. **Deliberate narrowings** (recorded in NEWS): list/exotic columns refused with recourse, `.json`/`.rds` no longer imported directly, internal `sort_columns`/`sort_rows` removed (sorting needs a locale-dependent collation order). 452 new tests including the S1-S6 identity-contract integration suite on a zero-mock local-backend fixture (`test-identity-contract.R`) and 17 tagged correctness properties. `R CMD check --as-cran` 0E/0W/1 pre-existing NOTE. Vignettes `design-version-shas` + `getting-started` rewritten; new offline walkthrough `dev/e2e-cv1-identity.R`. **Pathway impact:** read route gains an integrity gate, no new lookup -- `dev/datom_pathways.md` updated. |
| Spec: pre-cran-mechanical-fixes | 2026-07-14 | 1991 | Issue [#74](https://github.com/amashadihossein/datom/issues/74): ten contract-neutral pre-CRAN defect fixes (A-J), no on-disk format change. **A** thread `pat` through all git pull/push callers (`datom_pull`, `.datom_sync_metadata`, `.datom_gov_clone_init` + `datom_clone`) so private repos work. **B** pass `session_token` to the `datom_init_repo()` namespace-check S3 client (STS creds no longer skip the "namespace occupied" guard). **C** `.datom_storage_rel_key()` strips the prefix literally (`startsWith`/`substring`), fixing regex-metachar prefixes. **D** `size_bytes` via `as.numeric()` (>2GB no longer NA-poisons the manifest summary). **E** `datom_clone()` sets a local git identity (first write after clone no longer fails on a no-gitconfig host). **F** confirmed-real bug: `.datom_git_push()` now sets upstream tracking after push (fetch + `branch_set_upstream`, warn-only on failure) so `datom_pull()`/stale-guard stop silently no-op'ing on the initializing dev's machine. **G** new `.datom_validate_sha()` (6-64 hex) guards `version`/`data_sha` splicing in `datom_get_lineage`/`datom_parent`/`.datom_read_parquet` against local-backend path traversal. **H** developer conn now cross-checks prefix (not just root), normalized, so wrong-prefix stores are rejected. **I** `.datom_mask_secret(reveal_prefix=)` fully masks AWS `secret_key`/`session_token` in print methods. **J** NEWS.md ships (`.Rbuildignore`), LICENSE 2025->2026, SECURITY.md link repointed + plaintext-cred warning. Local R CMD check 0E/0W/1 benign NOTE. **No pathway impact.** **Deferred:** cran-comments.md refresh + full `--as-cran` on all four environments -> combined pre-submission step after #72 (companion hashing rework). |
| Spec: vignettes-gov-liftout | 2026-06-27 | 1873 | Vignette-suite cleanup after the GOV_SEAM lift-out (docs-only; **no `R/`/NAMESPACE changes**). Trimmed the rendered pkgdown site to a minimal gov-free set. **Bucket A** (kept/fixed): `first-extract`, `month-2-arrives`, `folder-of-extracts`, `source-lineage`, `looking-ahead`, `design-datom-model`, `design-version-shas`, `design-serverless`, + new **`start-on-s3.Rmd`** (gov-free S3-native start; complements local-first `first-extract`). In-place fixes: `datom_decommission()` -> `datom_repo_delete()`, `gov =` store component dropped, gov framing reworded to companion-package/on-demand terms, all dead cross-links repointed. **Bucket B** (parked verbatim, build-ignored): 9 gov-interface vignettes + `resume_article_4-8.R` moved to `dev/vignettes-deferred/` with a parking README (reassembly map) -- blocked on datomanager's gov API. **Bucket C** (handed off): `governing-a-portfolio` + `auditing-reproducibility` `git rm`'d from datom (byte-identical copies preserved at `datomanager/dev/vignettes-from-datom/`). `_pkgdown.yml` `articles:` restructured to Bucket-A-only (9 indexed == 9 on disk); `reference:` untouched. All 7 verification checks pass (no removed-export refs, no dead links, ASCII clean, R CMD build clean, pkgdown clean, test count 1873 unchanged, R/+NAMESPACE clean). **No pathway impact.** |
| Spec: gov-seam-liftout (datom side) | 2026-06-20 | 1873 | GOV_SEAM lift-out, datom side. Removed the governed **write** surface (5 exports: `datom_init_gov`, `datom_attach_gov`, `datom_decommission`, `datom_sync_dispatch`, `datom_pull_gov`; 9 `.datom_gov_*` write helpers) — it now lives in `datomanager`. datom retains all gov **reads** (`datom_projects`, `datom_pull` (data-repo-only), the six gov-read helpers, `R/ref.R` resolvers). Additive: `gov_backend` 12th conn field + `.datom_conn_for(conn,"gov")` resolves gov dispatch from it (C6); new export `datom_repo_attach_governance()` (C4-compliant data-side `governance.json` write for `datomanager::gov_attach()`); internal `.datom_sync_data_metadata()` split from `datom_sync_dispatch` (data-only half; called by `datom_validate(fix=TRUE)` + `datom_write(NULL,NULL)`). `datom_init_repo()` decoupled from gov registration (solo-only). Added `dev/e2e-solo-local.R` (solo init→write→read→`datom_repo_delete`, passing). R CMD check 0E/0W/1 benign NOTE; dev version 0.0.0.9001. **No pathway impact** (route shapes unchanged). Companion-package starter code recoverable from git history before the merge. |

### Completed Phases (legacy)

| Phase | Completed | Tests | Summary |
|-------|-----------|-------|---------|
| Phase 22: Storage Extension API | 2026-06-10 | 1897 | Six exports: `datom_storage_list`, `datom_storage_delete_prefix`, `datom_storage_copy` (all 4 backend combos), `datom_storage_verify` (structural + content modes), `datom_repo_set_data_store` (read-modify-write `project.yaml`), `datom_repo_delete` (extracted from `datom_decommission`; gov guard). New files: `R/storage.R`, `R/repo.R`. `datom_decommission()` refactored to call `datom_repo_delete()`. 197 new tests. pkgdown + NAMESPACE clean. Spec updated with Storage Extension API section. |
| Phase 21: Governance-First Connection UX | 2026-05-29 | 1700 | Closes issue #24. Two deliverables: (1) `governance.json` dual-pointer file -- written to `.datom/governance.json` (git canonical) + `{prefix}/datom/.metadata/governance.json` (storage mirror) by `datom_init_repo()` and `datom_attach_gov()`; read back by the developer four-state matrix and reader data-first probe; cleaned up by `datom_decommission()`. (2) `datom_store_s3_creds(access_key, secret_key)` credentials-only S3 component -- readers on gov-attached projects no longer need to know the data bucket/prefix/region; location is resolved from `ref.json` at conn time. New exports: `datom_store_s3_creds()`, `is_datom_store_s3_creds()`. New vignette: `design-governance-json.Rmd` (dual-pointer pattern, schema, lifecycle). Updated vignettes: `handing-off.Rmd` (engineer handoff reduced to 2 items), `credentials-in-practice.Rmd` (`datom_store_s3_creds` section). Spec updated with `governance.json` schema + `datom_store_s3_creds` entry. E2E assertion blocks added to `e2e-test-local.R` (Flows 1-6). |
| Phase 20: Source Lineage | 2026-05-12 | 1602 | Transitive source lineage field in metadata.json. `datom_sync()` auto-populates a self-entry for imported tables. `datom_write()` structural mandate: `source_lineage` required when `parents` is non-null. New exports: `datom_get_lineage(depth = "source" | "parents")` (single-read, no DAG walk) and a dedicated lineage validator (union parents' lineages, diff against declared). Spec updated; vignette "Tracing Data Lineage" added; dpbuild lineage contract documented in daapr_architecture.md. (Superseded by the cross-project-parent-lineage phase: the dedicated validator and the `source_lineage` caller-mandate were removed; `datom_write()` now derives `source_lineage` from the parents' union; parents carry `data_sha`; the composable recompute recipe replaces the validator: `datom_get_parents()` + per-parent `datom_get_lineage(depth = "source")` + `datom_lineage_union()`.) |
| Phase 18: Governance on-demand | 2026-05-03 | 1528 | Made governance optional and on-demand. New flow: `datom_store(governance = NULL)` for solo projects; `datom_attach_gov()` for the promotion moment (typically alongside S3 migration). `datom_init_repo()` writes `project.yaml` without `storage.governance` when no gov store supplied. Gov-only commands (`datom_projects`, `datom_pull_gov`, `datom_sync_dispatch`) emit a uniform "no governance attached -- use `datom_attach_gov()`" error. `.datom_conn_for(conn, scope)` accessor replaces `.datom_gov_conn()` for all scope-switching. `datom_decommission()` no-gov branch skips gov teardown cleanly (latent bug fixed: local no-gov conns no longer accidentally delete `cwd/projects/{name}`). Vignettes rewritten: Article 1 drops gov entirely; Article 4 introduces `datom_attach_gov()` paired with S3 promotion. README primary example is no-gov. Sandbox learns `attach_gov = FALSE` mode and `sandbox_promote_gov()` helper. Acceptance criteria 1-10 all met. |
| Phase 16: Vignette overhaul | 2026-05-11 | 1530 | Replaced the legacy three vignettes with a 10-article user-journey track (STUDY-001 over six months: first extract -> monthly cadence -> bulk sync -> S3 promotion -> reader handoff -> second engineer -> portfolio governance -> audit/reproducibility -> daapr-stack outlook -> credentials reference) plus a 6-article design-notes track (D1 datom model; D2 ref.json; D3 dispatch.json; D4 two-repo split; D5 version SHAs; D6 serverless). All article IO chunks `eval = FALSE`; jump-in readers get continuity via `inst/vignette-setup/resume_article_N.R` (idempotent, env-var-overridable). Simulator extended with LB + AE domains. `_pkgdown.yml` reorganized into Get Started / Scale Up / Govern / Reference / Design groups. Coverage review confirms all 28 exports appear in at least one article. Phase-16 continuation (May 11): fixed two real package bugs surfaced by live-running the vignettes (datom_attach_gov synthetic data snapshot had empty root -- backend field mismatch; fixed in R/conn.R with regression test); added `buckets-and-prefixes.Rmd` convention article (Pattern A: bucket-per-study, empty prefix for raw; named prefixes for derived; dedicated gov bucket); aligned all 17 vignettes to Pattern A defaults and ASCII-only source (R CMD check safe); fixed broken `credentials.html` link in `looking-ahead`; expanded `credentials-in-practice` to 3 credential options; issued #19 (init_gov CWD default) and #20 (init_gov idempotence local-only). |
| Phase 17: Portfolio helpers | 2026-05-02 | 1416 | Added two manager/audit-facing helpers: `datom_summary(conn)` (single-project one-liner; reads `.metadata/manifest.json`; S3 class with print method; developer path includes data git remote URL) and `datom_projects(x)` (portfolio listing; accepts `datom_conn` or `datom_store`; returns data frame with name/data_backend/data_root/data_prefix/registered_at; clone-first read with storage fallback; corrupt entries warn-and-skip). Internal additions: storage list dispatch (`.datom_storage_list_objects` + `.datom_s3_list_objects` mirroring existing local helper); `.datom_gov_list_projects()` pure-read helper in `R/utils-gov.R` (NOT a `GOV_SEAM` -- reads stay with datom, only gov writes are seamed). Pre-release schema bump: added `current$type` field to `ref.json` so readers can identify the data backend without already holding a store. Unblocks Phase 16 Chunks 5-6. |
| Phase 15: Separate Gov Repo (+ audit cleanup) | 2026-04-29 | 1343 | Split governance and data into two independent git repos with separate histories, remotes, and local clones. New shared governance repo (one per organization / gov bucket) holds `projects/{name}/{ref,dispatch,migration_history}.json`; data repo holds tables + manifest. New exports: `datom_init_gov()`, `datom_decommission()`, `datom_pull_gov()`. Refactored: `datom_init_repo()` (data-first, gov-register), `datom_clone()` + `datom_pull()` (two-repo), `datom_sync_dispatch()` (commits on gov). Breaking: `remote_url` -> `data_repo_url`. New `# GOV_SEAM:` contract isolates gov-write helpers in `R/utils-gov.R` for future companion-package handoff. Role-aware ref reads (developer reads from local clone, reader from storage; write-time guard always hits storage). Sandbox supports scoped teardown (`scope = "all" \| "project" \| "gov"`). **Pre-CRAN audit cleanup** folded in (5 chunks): hard-abort on post-push gov/manifest failures with gov-clone rollback (C1+C2); `datom_decommission()` repo deletion via `httr2` instead of `gh` CLI (H1); backend-blind UI in `datom_sync_dispatch()`, symmetric pull errors, dropped dead pre-Phase-15 fallback (H2-H4+L3); deduped `gov_local_path` resolution / `datom_init_gov()` config / `.datom_gov_unregister_project()` commit logic (M1+M3+M4+M5); style polish (L1+L2+L4+L5). E2E-driven fixes: NA-safe prefix guards in `.datom_local_delete_prefix` / `.datom_s3_delete_prefix`; sandbox mops up data store root after decommission (caller-owned root principle); idempotent `gh repo` deletion in sandbox teardown. |
| Phase 14: Public Release Prep | 2026-04-21 | 1177 | Pre-public cleanup: fix LICENSE/`.Rbuildignore` (`tbit` rename remnants), add `NEWS.md`, drop CRAN badge, fix R CMD check warning (non-ASCII in `R/ref.R`), add `CONTRIBUTING`, `CODE_OF_CONDUCT`, `SECURITY`, issue/PR templates, GitHub Actions (R-CMD-check + pkgdown), untrack `dev/archive/` + `dev/datomaccess_overview.md` via `.gitignore`, scrub Phase/Chunk refs from `R/` comments, centralize `dev/dev-sandbox.R` credentials to `Sys.getenv()`, fix `_pkgdown.yml` missing `datom_store_local` entries. |
| Phase 13: Reader Ref Resolution | 2026-04-21 | 1177 | Conn-time ref resolution wired into both `_get_conn_reader` and `_get_conn_developer` (S3 + local). New helpers: `.datom_resolve_data_location()`, `.datom_check_data_reachable()`, `.datom_check_ref_current()`. Developer migration mismatch → auto-pull git + re-read `project.yaml`. Reader mismatch → warn + proceed with ref-resolved location. Reachability check (HeadBucket / `dir_exists`) gates conn creation. Conn-time ref failure is warn-only; write-time ref failure is a hard abort (prevents orphaned data). `datom_status` and sandbox UI now backend-aware (`Tables on S3`/`Tables on local`, `Data:`/`Governance:` labels instead of hardcoded `s3://`). `.sandbox_wipe_local_component()` added so `sandbox_down()` handles local backends too. Named-lookup pattern adopted for backend UI labels (anticipates GCS/other backends). |
| Phase 12: Filesystem Backend | 2026-04-19 | 1153 | `datom_store_local()` constructor, `.datom_local_*()` backend functions (`utils-local.R`), dispatch wiring (`switch` arms in `utils-storage.R`), `conn$bucket`→`conn$root` rename, `new_datom_conn(backend=)`, `datom_init_repo()`/`datom_get_conn()` local paths, `project.yaml` type:local, `.datom_store_backend/root/region()` accessors, `.datom_build_init_conn()` helper, `ref.json` backend-neutral, README template backend-neutral, E2E test script (`dev/e2e-test-local.R`), `sandbox_store_local()`, `devtools::check()` clean. |
| Phase 11: Routing Separation | 2026-04-18 | 1039 | `routing.json` → `dispatch.json`, credential wiring (no env var bridge), gov/data store split, storage abstraction layer (`.datom_storage_*()` dispatch), `ref.json` replaces `.redirect.json`, conn fields `client`/`gov_client`/`backend`, `.datom_build_storage_key()`, spec + copilot-instructions updated, README updated, sandbox fixed, `devtools::check()` clean. E2E passes. |
| Phase 10: Store Abstraction | 2026-04-18 | 1083 | `datom_store_s3()` + `datom_store()` constructors, `.datom_create_github_repo()` via httr2, `datom_init_repo(store=, create_repo=TRUE, repo_name=)`, `datom_get_conn(store=)`, `datom_clone(path, store)`, two-component `project.yaml` (governance+data), `.datom_install_store()` env-var bridge, HeadBucket validation (STS removed — not in paws.storage), print methods with masked secrets, vignettes rewritten. `devtools::check()` clean. | Full package rename: all function prefixes (`datom_`/`.datom_`), S3 class (`datom_conn`), env vars (`DATOM_`), S3 path segment, `.datom/` config dir, metadata field (`datom_version`), package identity, docs. `devtools::check()` clean. |
| Phase 7: Multi-Developer Collaboration | 2026-03-28 | 964 | S3 namespace safety check in `datom_init_repo()`, pull-before-push discipline (`.datom_git_pull`, `.datom_check_git_current`), `datom_pull()` export (git-only, no S3 refresh — git is source of truth), `datom_clone()` export, team collaboration vignette, credentials vignette, `project_name` in manifest, `.force` bypass, `datom_validate()` project_name cross-check |
| Phase 8: Metadata Enrichment & Table Types | 2026-03-28 | 905 | `table_type`, `size_bytes`, `parents`, `original_file_sha` in version_history, `datom_get_parents()`, `endpoint` param, `.access/` namespace safety. Post-phase bug fixes: manifest update in `datom_write`, `datom_init_repo` S3 push, idempotent SHA computation (JSON canonical form, volatile field exclusion, version_history dedup guard) |
| Phase 6: Sync & Validation | 2026-02-15 | 130 | datom_sync_manifest, datom_sync (rio import), datom_sync_routing, datom_validate (git-S3 consistency), datom_status |
| Phase 5: Read/Write Workflows | 2026-02-15 | 143 | datom_read (version resolution), datom_write (change detection, parquet+metadata), .datom_sync_metadata, datom_list, datom_history |
| Phase 4.5: S3 Refactor | 2026-02-15 | 99 | Refactored S3 utils from (s3_client, bucket, s3_key) to (conn, s3_key), added mock_datom_conn helper |
| Phase 4: Connection & Init | 2026-02-15 | 115 | datom_conn S3 class, datom_get_conn (developer/reader), datom_init_repo, credential derivation |
| Phase 3: Git Operations | 2026-02-14 | 47 | git2r wrappers: check, author, branch, commit, push (fetch+merge+push) |
| Phase 2: S3 Operations | 2026-02-10 | 99 | S3 client, upload/download/exists, JSON read/write, redirect resolution |
| Phase 1: Core Utilities | 2026-02-09 | 131 | SHA, paths, name validation, repo validation |

### Developer Tooling

**dev/check-spec.R**: Mechanical consistency checks for a Kiro spec

- `Rscript dev/check-spec.R [spec-dir]` (defaults to `.kiro/specs/datom-sets`). Base R only, no
  package dependencies. Exit 0 clean, 1 on any failure, so it works as a gate.
- Nine checks, each one derived from a defect that actually shipped into a spec and had to be
  caught by a human reader. (This list read "six" until 2026-08-23, having missed two additions --
  which is the same staleness class the checks exist to catch, in the document describing them.)

  | Check | Catches |
  |---|---|
  | dangling references | an `R*`/`I*`/`P*`/`AC*` token that resolves to nothing after renumbering |
  | orphaned criteria | an AC / invariant / property defined but referenced by no task -- i.e. unimplemented |
  | task numbering | task numbers not contiguous from 0 |
  | task acceptance coverage | a task with no `Acceptance:` clause |
  | code citations | a `R/file.R:NNN` **or `tests/testthat/test-file.R:NNN`** reference pointing outside the file, or resolving to nothing (every line in the cited range blank). Test files were added 2026-09-09, because Task 22 has to name the individual assertions it flips and the nine it must not touch, and a stale number there sends the reader to the wrong assertion in a file of a thousand lines. It found three stale citations on the round that added it, and was verified by planting a wrong number and confirming a FAIL. |
  | superseded wording | retired phrasing surviving as a **live instruction** rather than marked historical |
  | duplicated content agrees | the same fact stated in several files with the copies disagreeing, or one copy deleted; plus any hardcoded AC upper bound; plus **the execution order** (`18 -> 19 -> ...`), which is written out three times -- the spec's state block, Phase E's preamble, and the status cell in this file. Added 2026-09-10 after adding Task 23 swept one copy and left two saying the order ended at Task 7, caught by a reader. Copies are compared against each other, never against an expected sequence, since the order changes legitimately. This is the one clause that reads `dev/README.md`, because that is where the third copy lives. Verified by reintroducing the exact defect. |
  | ascii | non-ASCII that will trip `R CMD check` once the prose is copied into roxygen or NEWS |
  | task cross-references | a `Task N` mention with no such task -- the usual cause is a half-swept renumber |

- **When to run it**: at each chunk checkpoint, next to reporting the test count. A `PostFileSave`
  hook would fire constantly mid-draft and train you to ignore it.
- `SPEC_CHECK_SHOW_CITATIONS=1` prints every cited code line so a reviewer can confirm it says what
  the spec claims. **The citation check catches the blatant half only, and the split is worth
  knowing**: a citation whose whole range is blank now **fails** the run (added 2026-08-28, after an
  insertion into `R/utils-sha.R` staled five citations while all nine checks passed -- two had drifted
  onto blank lines and were reported only in the verbose output, without failing). A citation that
  drifted onto a **real but unrelated** line still passes, because no mechanical check can tell that a
  comment is not the constant the spec claims. So after any insertion into `R/`, re-derive by content
  with the verbose flag; a green run is not evidence. Two exemptions, both deliberate: a range whose
  *first* line is blank but which spans real content is fine, and a citation appearing only inside a
  dated Decisions row is exempt, since those are frozen and some exist to record a number that was
  already wrong.
- **Maintenance rule**: the `retired` denylist is the only part that needs upkeep. When a design
  decision is reversed, add its old phrasing there -- that is what turns "we removed this" into
  "and it cannot come back silently".
- **What it cannot do**: reasoning defects. In the review round that prompted it, these checks would
  have caught about half the findings; the rest were things like "a required payload field has no
  public parameter" and "the integrity gate has no acceptance criterion". It reduces review load, it
  does not replace review. The script says so on every run.

**dev/e2e-cv1-identity.R**: Offline `datom-cv1` identity walkthrough and smoke test

- `Rscript dev/e2e-cv1-identity.R` -- **no GitHub PAT, no AWS, no network**. Builds a real git
  repo with a local bare remote plus a real `backend = "local"` store in `tempdir()`.
- Three sections: the table contract (`datom_check_hashable()` on a clean and an offending
  table), what is and is not identity (container class, storage type, factor levels, tzone,
  NaN/`-0`, `NA_real_`, row/column order, NFC vs NFD, last-bit doubles -- each line labelled
  with its expected verdict), and a real project walking full -> metadata_only re-export ->
  skipped re-scan -> new content -> revert (no duplicate version, no re-upload) -> a flipped
  byte (read refuses) -> an unhashable table refused with no state left behind.
- Every claim is asserted (22 of them); the script exits non-zero on any mismatch, so it
  doubles as a smoke test.
- **Caveat**: the conn is assembled directly rather than via `datom_init_repo()` /
  `datom_get_conn()` (that is what removes the PAT requirement), so `project.yaml`, GitHub repo
  creation, and ref resolution are NOT exercised. Use `dev/e2e-solo-local.R` for those -- it
  goes through the public entry points and does need a PAT.

**dev/dev-sandbox.R**: Automated setup/teardown for testing workflows

- `sandbox_up()`: Creates GitHub repo + runs `datom_init_repo()` + populates with example data
- `sandbox_down()`: Wipes S3 namespace + deletes GitHub repo + removes local directory
- `sandbox_reset()`: Full teardown + setup in one call
- Replaces manual 5-step workflow (create repo, walk vignette, delete S3, delete repo, delete local)
- Requires `gh` CLI, AWS credentials, `GITHUB_PAT`

### Backlog (Deferred Features)

Items discovered during development but intentionally deferred. Review periodically.

| Item | Discovered In | Reason Deferred | Priority |
|------|---------------|-----------------|----------|
| `datom_write()` parent `data_sha` enrichment fails for same-project parents (issue #52) | gov-seam-liftout (E2E) | Pre-existing path bug: the lookup prepends `p$source` (project name) but same-project tables have no project segment. Benign (write succeeds, `validate_lineage()` unaffected). Fix tracked in [#52](https://github.com/amashadihossein/datom/issues/52). | Medium |
| renv::init() in datom_init_repo | Phase 4 | Adds complexity, tangential to core data versioning | Low |
| Manifest manipulation APIs (descriptions, staging, QA tagging) | Phase 7 | Two-step scan+sync is sufficient; richer manifest APIs belong in a sister package or future datom release | Medium |
| datomanager package creation | Phase 15 | Companion governance package (settled name: `datomanager`). Scope doc: `dev/datomanager_scope.md`. Owns GOV_SEAM write surface (renamed `gov_*` on lift-out) + `gov_migrate_data()` (Phase 19). datom Phase 22 (storage extension API) complete 2026-06-10; datomanager scaffold is the remaining gate. Effort: ~2 days for lift-out, then Phase 19. **Starter code for the gov-write reimplementation:** the tried-and-tested helper bodies + their tests are preserved in datom git history at the commit just before the `gov-seam-liftout` merge -- retrieve with `git show <sha>:R/utils-gov.R`, `git show <sha>:R/decommission.R`, `git show <sha>:tests/testthat/test-utils-gov.R` (find `<sha>` via `git log --oneline -- R/utils-gov.R`). Reimplement behavior-equivalent with git2r + **its own storage IO for the gov namespace only** (corrected 2026-08-18: an earlier wording said "own storage IO" without qualification, which contradicts the **Authority Principle** in `dev/datomanager_scope.md` -- "data-repo mutations always route through datom ... datomanager never touches the data repo directly". datomanager owns the gov repo and gov storage; every **data-side** write goes through a purpose-built datom export: `datom_storage_copy/verify/list/delete_prefix`, `datom_repo_set_data_store`, `datom_repo_delete`, `datom_repo_attach_governance`); contract C5 (commit strings) + C8 (storage layout) pin the observable behavior. | High (next major) |
| Public `datom_storage_write_json()` (JSON write on the Storage Extension API) | datom-sets spec, Task 3 (2026-08-18) | **Dropped from the spec, not merely postponed.** It was requested in [#89](https://github.com/amashadihossein/datom/issues/89) so a downstream package could write *its own document* into datom's namespace -- that document was a set, and `datom_write_set()` now writes sets as first-class artifacts, so no consumer remains. Exporting it anyway would cut against the Authority Principle above, whose expression is a purpose-built verb per need rather than a generic byte channel (`datom_repo_attach_governance()` is the precedent: datom gave datomanager a named export instead of a generic write for the `governance.json` data-side mirror). Retired with it: R12.4a's managed-key refusal list, I14, AC23. **Revival trigger**: datomanager needs to write JSON into **its own gov namespace** and would otherwise reimplement s3/local dispatch. That is a *different* export -- gov-scoped, no managed-key rules -- so **re-derive the refusal list rather than inheriting R12.4a's**; the analysis is preserved verbatim in `requirements.md` R12.4a as the starting point. Adding an export later is additive; removing one after release is breaking, which is why the default was to not ship it. | Low (trigger-driven) |
| ~~**Identity hashing: denylist -> allowlist**~~ ([#100](https://github.com/amashadihossein/datom/issues/100)) -- **SHIPPED 2026-08-28 as datom-sets Task 19.** The pairing with #98 suggested below did **not** happen and should not: #98 changes the hashed bytes, so it moves every recorded identity, while Task 19's whole claim was that none moved. | datom-sets spec, E2 design audit (2026-08-23) | **Pair with [#98](https://github.com/amashadihossein/datom/issues/98)** -- same function, different defect (#98 removes the JSON emitter from the identity path; this fixes field selection), so one identity-affecting change to verify instead of two. The function hashes every field in a metadata document minus a fixed exclusion list (`R/utils-sha.R:415-416`). A denylist cannot be forward-compatible: a build that has never heard of a field cannot know it was meant to ignore it, so it folds the field into the hash, disagrees with the recorded version, and reports a change on content that did not move -- **on every run, not once**. So today *any* new metadata field costs every older build a spurious version forever. Remedy: hash an explicit named list and ignore anything else. **Set the list to exactly the fields hashed today and every existing identity is byte-identical**, so it is behaviour-preserving now and forward-compatible later; optional fields (`parents`, `source_lineage`, `original_file_sha`, `custom`) need marking as optional since they are conditionally present. Honest limit: it makes *bookkeeping* additions free, which is the common case, not *semantic* ones -- a field that genuinely is content must be in the hash either way. Touches the function every artifact's identity depends on, so it wants the cv1 golden suite green before and after and a test that a document carrying an unknown field hashes identically to one without it. | High (unblocks the forward-compatibility guarantee) |
| ~~**Self-healing manifest read + persist `original_format`**~~ ([#101](https://github.com/amashadihossein/datom/issues/101)) -- **SHIPPED 2026-09-10 as datom-sets Task 22.** Two departures from the sketch below, both deliberate: it triggers on a **too-new declared format** as well as on an absent artifact key, and it writes nothing at **any** role rather than only for a storage-only reader | datom-sets spec, E2 design audit (2026-08-23) | Two halves, one issue. **Order after [#100](https://github.com/amashadihossein/datom/issues/100)**: persisting a metadata field is only free once identity hashing uses an allowlist. (a) When the artifact key a build expects is **absent**, rebuild the index from a storage listing instead of concluding the repo is empty. Trigger on **absent, not empty**: an empty list is what a new repo looks like and what a truncated file looks like, so rebuilding on empty costs a listing per call on healthy repos and hides corruption; a post-rename repo is distinguishable because the expected key is missing entirely. Two constraints -- a storage-only reader can only rebuild **in memory for that session** (no git, must not write to storage), and it must **warn once** pointing at the upgrade, because a silent repair is a silent degradation. (b) `original_format` is written into the manifest entry (`R/sync.R:753`) and **never** into per-artifact metadata, making it the one field a rebuild would lose; persisting it makes the rebuild lossless, and it is additive so free once identity hashing uses an allowlist. Payoff: any future rename of the artifact key is survivable for every build from that release onward, with no dual-write and no legacy copies. Applies to **derived** files only -- per-artifact metadata cannot self-heal (see `.github/copilot-instructions.md`, "Which files may break"). | High (paired with the allowlist item) |
| **`datom_repo_set_data_store()` is under-tested around the config file it rewrites** | datom-sets spec, Task 11 review (2026-09-17) | **Not a missing feature -- a coverage gap that has produced a finding in two consecutive review rounds**, which is the signal worth acting on rather than fixing one symptom at a time. Round one: the verb parsed `project.yaml` and never checked its declared format, while committing and pushing the result to everyone sharing the repo. Round two: its test asserts only that two fields survive the rewrite, so a refactor from read-modify-write to rebuilding the document would silently drop most of the file. Both were fixed at the point of discovery. What is deferred is the review of the verb as a whole: it is the only writer of that file besides repo creation, it commits and pushes, and its tests were written before the file carried anything a writer must obey (a minimum writer version, a format number, and shortly a project mode and set name). Worth going through once with the question "what happens to each key in that file", rather than waiting for a third round to name the third gap. | Medium (two findings in two rounds; no known live defect) |
| **Two acceptance statements in the datom-sets requirements have no id, so nothing gates their tests** | datom-sets spec, Task 17's independent criteria re-derivation (2026-09-20) | **Not a coverage gap -- a gating gap, and it is the inverse of the failure the new build check catches.** An independent session re-derived the criteria list from `requirements.md` alone and matched Task 16's exactly, but it also found two acceptance statements written in prose beside their requirements with no `AC` number: a written set's metadata carries exactly the fields R1.3 lists, asserted with `setequal()` on the written file, and a hand-assembled member list is refused with a message pointing at `datom_member()`. **Both have real tests today** (`tests/testthat/test-write-set.R:878` and `:498`), so nothing is unasserted -- but `dev/check-spec.R` check 10 walks `AC` ids, so deleting either test fails nothing. Not fixed in Task 17 because giving them ids means **two** edits, not one: defining the criteria, and referencing them from an already-closed task's acceptance line, since check 2 fails an AC no task references. Do it at the next spec that touches set writing, when a live task can own them. | Low (tested, merely ungated) |
| **Dual-write as a repo-level compatibility policy** ([#102](https://github.com/amashadihossein/datom/issues/102)) | datom-sets spec, E2 design audit (2026-08-23) | **Spec it; do not build it until a break actually needs it.** The escape hatch of last resort: the writer emits the previous shape alongside the current one for a declared window. It is the **only** mechanism that helps builds already released, since it asks nothing of them. Design constraints, all driven by the fact that the danger is a **stale** legacy copy rather than a leftover one (an old reader consuming an out-of-date file looks like it worked): repo-level config in `project.yaml`, **not** a per-call argument, so every write honours it and one forgetful call cannot strand a copy; turning it off **deletes** the legacy copies in the same operation; the sunset release recorded **inside** the legacy file so anything reading it can tell it is scheduled to disappear. Scope limit: **derived files only.** Dual-writing per-artifact metadata backfires -- a legacy-shaped copy hashes differently from the recorded version, so older writers mint a version on every run; it would help old readers by breaking old writers. Chosen over the richer alternative (a declarative transform file travelling with the repo) whose full reasoning is in the spec's rejected-alternatives table: the interpreter would have to ship in the build that predates the change, and its mapping format would be frozen the day it shipped. | Medium (trigger-driven -- build it when a break is unavoidable) |
| **Publish the schema-version to package-version mapping** ([#103](https://github.com/amashadihossein/datom/issues/103)) | datom-sets spec, E2 design audit (2026-08-23) | Docs only. One schema version spans many releases, so neither `schema_version` (the contract) nor `datom_version` (provenance) answers the only question a refusal message raises: *which datom do I need?* A table in the package documentation mapping each schema version to the release range that reads it. Add the row at each schema bump; the bump is rare by policy, so upkeep is near zero. | Medium (ship alongside the first release that carries `schema_version`) |
| **Writer floor: the raising verb and its tooling** | datom-sets spec, E2 design round (2026-08-23) | The **reading half SHIPPED 2026-09-09** as datom-sets Task 21: `min_writer_version` in `project.yaml`, absent meaning no limit, compared against the running build at the write entry, with a malformed value refused rather than read as no floor. It shipped first because it cannot be retrofitted -- a build that does not look for the field can never be bound by it, which is exactly why nothing can stop a 0.1.0 writer. Deferred here: a **purpose-built verb** to raise the floor (a normal commit cannot validate that the raiser satisfies the new value, and the R15 precedent in the spec is named verbs over generic writes), plus tooling and docs. **This row now also owns AC36(c)** -- "setting a floor above the setting build's own version is refused" is a guard on the setter, so it has nothing to bind to until the setter exists; Task 21 recorded the gap rather than faking it. The floor is the **escape hatch**, not the primary mechanism -- the vocabulary check is primary because it cannot be forgotten. Its own trigger cases, recorded so a later reader knows why it exists: a **meaning** change that adds no field, and a policy block for a non-format reason ("0.1.4 wrote bad hashes"). | Low (trigger-driven) |
| **Ease recovery from a push-rejected write, and make it rarer** ([#105](https://github.com/amashadihossein/datom/issues/105)) | datom-sets spec, Task 18 (2026-08-26) | A rejected push leaves a local commit, untouched storage, and a git merge conflict in machine-generated JSON -- recoverable, but only with git literacy a data developer may not have. **The finding that reframes it**: `.datom_check_git_current()` is called from exactly **one** place, `datom_sync()`; `datom_write()` never calls it. So the main write path writes files, commits, and only then pulls inside `.datom_git_push()` -- discovering divergence *after* manufacturing it. A fast-forward before the commit makes the push a fast-forward with no merge at all, so much of this is an **ordering artifact**, not inherent to offline work. Also `datom_status()` reports uncommitted files and the branch but **not** ahead/behind, so nobody can see it coming. Five options, filed as one issue because they trade off against each other: **(1)** ahead/behind in `datom_status()` -- near-free, `ahead_behind()` is already used, `[[1]]` is ahead; **(2)** fast-forward-only pull at `datom_write()`'s door -- highest leverage on likelihood, never auto-merge, stop before doing work rather than after committing, and **cheapest inside the datom-sets Task 21 window** since that entry sequence already fetches; **(3)** `datom_repo_resolve()`, the recovery verb -- take the remote copy of datom's derived files, complete the merge, report that the write should be re-run; safe to automate *because* those files are derived, and it must **refuse** when a conflicted path is not datom-owned (Task 13's `include_paths` commits user code, so that boundary is load-bearing) plus dry-run by default; **(4)** decompose the repo-wide `.metadata/manifest.json` into per-artifact entry files so two writers of *different* tables stop colliding -- right idea, wrong release, and deferral is bounded rather than compounding because #101's rebuild plus the 0.1.1 upgrade chain make it one more upgrade step later; **(5)** warn at write entry when the base is unverifiable, the honest complement to #104's loosening. **What stays hard and is not worth engineering away**: two people writing the same table from stale bases must have someone decide the version ordering. datom can only make the *mechanical* half mechanical. Residual after all five: genuinely offline, or a race between pull and push. | Medium (1 + 5 near-free; 3 is the main deliverable; 4 trigger-driven) |
| Backend rename: `local` -> `filesystem` | Phase 18 | "Local" implies laptop disk, but the backend supports network mounts and cloud-mounted FS too. Atomic schema bump touching store constructor, predicate, dispatch arms, `project.yaml`, `ref.json`. Defer until there's a second compelling reason to touch the schema. | Low |
| `gov_migrate_data()` (managed migration) | Phase 15 | Today migration is manual (`aws s3 sync` + `datom_sync_dispatch()`). Atomic data-copy + ref.json update + `.datom_gov_record_migration()` deferred. Lives in datomanager (Phase 19); calls datom `datom_storage_*` / `datom_repo_*` helpers (all six now exported in Phase 22). | Medium |
| Restore `ubuntu-latest (devel)` in `.github/workflows/R-CMD-check.yaml` | Phase 17 | Disabled 2026-05-02 because Posit PPM has no R-devel Linux binaries; every PR paid a 15-25 min source-compile tax for arrow / paws.storage / friends. Restore as part of the pre-CRAN checklist (CRAN expects devel to pass). | Pre-CRAN |
| CODEOWNERS automation on `projects/{name}/` | Phase 15 | Self-serve project ownership; belongs in companion package, not datom. | Low (companion) |
| Gov repo concurrency primitives | Phase 15 | Pull-before-push handles current contention. Advisory locks deferred until contention is observed. | Low |

**Backlog lifecycle**:
1. Discovered during phase work → add here with context
2. When planning next phase → review for inclusion
3. If promoting to spec → move to `datom_specification.md` "Deferred to v2" section
4. If abandoned → delete with brief note why

## Quick Context for New Sessions

When starting a new development session:

1. Check the **Active Specs** table above
2. Open the active spec under `.kiro/specs/{feature}/` (requirements, design, tasks)
3. **Read the state block at the top of `tasks.md`** -- an active spec carries its branch, its PR
   target, the current test count, the next task, and anything still open with the owner there. Then
   find the next unchecked task.
4. Continue from where we left off

If a spec touches `R/`, read `dev/engineering-notes.md` before editing. If it has a
`check-spec.R`-able structure, run `Rscript dev/check-spec.R` as part of each chunk (step 3a of the
Chunk Delivery Checklist).

---

## Collaborative Development Workflow

Within each spec, we work in **chunks** — small, testable units of work (each chunk = one task, or a small related group, in `tasks.md`).

### Chunk Lifecycle

```
1. DESIGN    → Propose scope, functions, acceptance criteria
                 ↓
              User approves or adjusts
                 ↓
2. DEVELOP   → Implement code + tests
                 ↓
              User reviews, runs tests, QA
                 ↓
3. FEEDBACK  → User confirms or requests changes
                 ↓
              Iterate until approved
                 ↓
4. COMPLETE  → Mark chunk done, pick next chunk
```

### Chunk Size Guidelines

- **1-3 functions** per chunk (testable in one session)
- **Clear boundary** — chunk should work independently
- **User can QA** — real tests they can run

### Communication Pattern

At each stage, I will:

| Stage | I Provide | You Provide |
|-------|-----------|-------------|
| DESIGN | Proposed functions, signatures, tests | Approval or adjustments |
| DEVELOP | Implementation + test file + debug snippet | Run tests, step through, try it out |
| FEEDBACK | Respond to issues | Confirm done or request changes |

### Chunk Delivery Checklist

After each chunk is implemented, I deliver **five things in order**:

1. **Write tests** — full test coverage for the chunk's functions
2. **Run tests** — execute and fix until all pass (green suite)
3. **Minimalist walkthrough snippet** — a clean, self-contained R snippet for you to paste into the console and step through interactively (use `debugonce()` to drop into any function)
3a. **Run `Rscript dev/check-spec.R`** — alongside reporting the test count. Structural
   consistency of the spec itself: dangling references, orphaned criteria, tasks with no acceptance
   clause, code citations pointing outside the file, and retired wording surviving as a live
   instruction. It is a gate (exit 1 on failure), and it is not a substitute for reading.
4. **Update the spec as part of the same commit** — every chunk, no exceptions:
   - Check off the completed task(s) in `tasks.md`
   - If the chunk changes metadata schema, storage layout, governance refs, lineage, access control, role resolution, migration, or decommissioning, update `dev/datom_pathways.md` or explicitly record "no pathway impact"
   Also update the `dev/README.md` Active Specs table status line. `tasks.md` + commit history are the audit trail; if they're not updated, the chunk isn't done.
5. **Commit after walkthrough** — once you've kicked the tires and confirmed it works, I commit (code + `tasks.md` status together) with a concise message, then you push

> **`tasks.md` is the at-a-glance dashboard.** Task checkboxes show status at a glance; a fresh session reads `tasks.md` first to find the next unchecked task. Commit history is the detailed audit trail.

### QA Methods

- **Run tests**: `devtools::test(filter = "chunk_name")`
- **Debug walkthrough**: Step through with breakpoints or `browser()`
- **Try interactively**: Playground snippets provided with each chunk

### Code Style for Debuggability

To support step-through debugging:

- **Meaningful intermediates**: Avoid long pipe chains; use named variables
- **Small functions**: Each does one thing, easy to step into
- **Playground snippets**: Each chunk includes copy-paste code to try interactively

### Branch Workflow

Every spec gets its own feature branch:

1. **Create branch**: `git checkout -b spec/{feature}` (from `main`)
2. **Develop on branch**: All commits for the spec go here
3. **PR when complete**: Open a pull request to `main`
4. **Merge + delete**: Squash-merge or merge, then delete the branch

The spec is created/edited *on the branch* (not on `main`). Specs persist after merge — the
Spec Completion Procedure does **not** delete them (this replaces the old phase-doc deletion).

### Git Commit Cadence

Within chunks (on the spec branch):

- **Commit frequently**: After each logical unit (function + test, fix, etc.)
- **Push at milestones**: Task complete, spec complete, or good stopping point
- **Message format**: `{feature}: brief description` (optionally reference the task)

Example:
```bash
# Start the spec branch
git checkout -b spec/store-relocate

# Within the first task
git add R/remotes.R tests/testthat/test-remotes.R
git commit -m "store-relocate: datom_remotes_s3 constructor + validation"

# More work...
git commit -m "store-relocate: .datom_install_remotes + env var bridge"

# Spec complete — PR to main
git push -u origin spec/store-relocate
# Open PR, merge, delete branch
```

## Maintenance Rules

1. **Keep `tasks.md` current** as you work (status, decisions, blockers)
2. **Split large specs** when scope exceeds a few sessions
3. **Never let the spec go stale** — if context changes, update requirements/design immediately
4. **Specs persist** — do not delete them on completion (they are durable documentation)
5. **Update this README** when spec status changes
6. **Capture deferrals immediately** — when you skip something, document it in the Backlog
7. **Review backlog** before starting each spec
8. **Keep pathway map current** — schema/routing changes must update `dev/datom_pathways.md` or record "no pathway impact"

## Spec Completion Procedure (formerly Phase Completion)

When all of a spec's tasks are done, perform these steps **in order before starting the next spec**:

1. **Harvest persistent content** from the phase doc:
   - Design decisions that affect the overall API → migrate to `dev/datom_specification.md`
   - Canonical lookup/traversal route changes → migrate to `dev/datom_pathways.md`
   - Coding patterns/conventions discovered → migrate to `.github/copilot-instructions.md`
   - Ecosystem learnings → migrate to `dev/daapr_architecture.md`
   - Deferred items → move to the **Backlog** table in this README

2. **Update this README**:
   - Move phase from Active → Completed table (with date, test count, summary)
   - Update backlog if needed

3. **Specs persist — do NOT delete them.** (Replaces the old "delete the phase doc" step.)
   The spec under `.kiro/specs/{feature}/` is durable documentation: mark its tasks complete
   and leave it in place. Harvest only *additional* durable learnings into the permanent docs
   per step 1.

4. **PR + merge + delete branch**:
   - Open a PR from `phase/{n}-{name}` to `main`
   - Merge (squash or regular)
   - Delete the feature branch (remote and local)

**Rule**: Kiro specs persist as durable documentation — do not delete them. (Legacy
`dev/phase_*.md` files, if any are ever created, still must not survive past completion —
migrate their content and remove them.)
