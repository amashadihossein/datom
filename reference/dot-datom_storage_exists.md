# Check if Storage Object Exists

Check if Storage Object Exists

## Usage

``` r
.datom_storage_exists(conn, key)
```

## Arguments

- conn:

  A `datom_conn` object.

- key:

  Relative storage key (after `prefix/datom/`).

## Value

`TRUE` or `FALSE`.
