# Get Current Branch

Returns the name of the currently checked-out branch. Aborts on detached
HEAD (datom requires a branch).

## Usage

``` r
.datom_git_branch(path)
```

## Arguments

- path:

  Repository path.

## Value

Branch name as a string.
